# casdey

Note: "casdey" is always written lowercase, including at the start of a sentence — never capitalized.

## Project structure
- `CLAUDE.md` — project context and conventions: business context, decisions, pricing, outreach status, and working conventions (everything else in this file).
- `.claude/` — Claude Code's own project configuration: subagents, skills, settings, and any tooling config specific to this project. `.claude/launch.json` starts the local dev server for the browser preview tool. Skills of note:
  - `.claude/skills/cold-outreach/` (invoke with `/cold-outreach`) — read before touching outreach automation. Its `SKILL.md` is the detailed operating spec (environment detection, credentials, salutation rules, batch history); this file only carries the high-level status.
  - `.claude/skills/frontend-design/` — required reading before any frontend/visual work, see `brand assets/CLAUDE_DESIGN.md` for the accompanying screenshot-driven review workflow.
  - `.claude/skills/update-project/` (invoke with `/update-project`) — keeps this file and other project docs (README, `web/SAAS_HANDOFF.md`, `web/SAAS_ROADMAP.md`) current after project changes, so context survives without relying on chat history. Distinct from `/session-handoff` (chat-only, ephemeral) and from user memory (this user's own cross-session recall, not part of the repo).
  - `.claude/skills/push-github/` (invoke with `/push-github`) — commits and pushes to GitHub. `main` is connected to Vercel and auto-deploys on push, so this skill always asks for fresh explicit confirmation before the actual push, even when it's the skill itself being invoked (see the Vercel bullet under Infrastructure).
  - `.claude/skills/hormozi/` (invoke with `/hormozi`, or auto-triggers on business-decision work) — Alex Hormozi's frameworks (*$100M Offers* / *Leads* / *Money Models*) distilled into `references/` and mapped to casdey, with a decision `ledger.md` tracking where casdey follows him and where it deliberately diverges. Davide's chosen business mentor, so this is the default lens for offer/pricing/outreach/lead-gen/monetisation calls. Book PDFs live in `Alex Hormozi/` at the repo root (gitignored, copyright; image-only scans).
- `brand assets/` — brand identity. `casdey-brand-guide.html` (v5, "Charcoal Product System", Sep 2026) is the source of truth for colors, type, the logo mark, motion and voice; everything visual must match it. The current product register is a #09090A charcoal ground, one #191A1D raised card/sidebar plane, quiet 10px dot texture outside cards, and gold reserved for intent and recovery. Its tokens are mirrored in `web/src/app/globals.css` and `web/src/styles/product.css`. `casdey Logo.png` is the v4 mark on its own (1080x1080, the four pinwheeled bars on Chalk, one of them Leaf gold), exported by Davide 2026-09-19 to replace the old v2/v3 wordmark-only file; use it where an image file is needed (profile pictures, uploads). In the product the lockup is still drawn in code (`web/src/components/wordmark.tsx`), which scales and re-themes with the tokens. `CLAUDE_DESIGN.md` is Davide's own frontend workflow rules (screenshot-compare against references, brand-asset-first, anti-generic guardrails). `landing page inspiration for casdey.webp` is the reference the current design was built from (Finpay-style fintech layout, adapted with casdey's own palette/copy/illustrations, not copied verbatim).
- `web/` — the casdey website: landing page (`/`), waitlist (`/waitlist`), and eventually the SaaS itself in the same app (Next.js 16 App Router + Tailwind v4, deployed on Vercel with root directory `web`). Waitlist signups are stored in Supabase (EU region). See `web/README.md` for setup, env vars, the waitlist mechanics, and two Supabase gotchas already hit once (bare project URL, explicit `service_role` table grants).
  - **`web/SAAS_V1_PLAN.md` is the single source of truth for the path to a ready V1** — one sequenced plan (Track A build / Track B ops / Track C prod-verify / Track D Davide's final walkthrough) that merges the still-open items from `SAAS_ROADMAP.md`, the readiness gates from `SAAS_ONBOARDING.md`, and the ops tasks. Read it before starting any V1 build work. `SAAS_HANDOFF.md` is the current gym product/deployment reference (rewritten 2026-09-03); `SAAS_ROADMAP.md` is retained as the dated pre-pivot build history (items #1-#10), superseded for V1 purposes by the plan.
  - **`web/SAAS_V1_1_PLAN.md` is the source of truth for V1.1**, which is a different kind of release: V1 asked whether the product works, V1.1 asks whether anyone activates. Four tracks (H Trial With Penalty / I the lapse-window default / J the reply-side guarantee / K turning gym #1 into a case study), out of the Hormozi diagnosis of 2026-09-10. Read it before touching trial, activation or signup behaviour.
- `wiki/` — casdey's business knowledge base (Obsidian vault, not git-tracked, see `.gitignore`): market research, growth/outreach frameworks, competitor notes, strategy exploration too exploratory or too rarely-needed to keep loading into every session via this file. Set up 2026-08-15, schema and conventions in `wiki/CLAUDE.md`, read that before working inside it. Start any query at `wiki/index.md`.
- `IMPROVEMENTS.md` — Davide's improvements list of 2026-09-19 (17 items: the US market, one live place for the business view, a to-do section, a more professional UI, and many product fixes), recorded **verbatim** under the same rule as `web/D1_WALKTHROUGH.md`, with a status board below it. His priority order: the US market first, then the one-place view with its to-do section. Read it before starting any of those items.
- `AGENTS.md` and `.agents/skills/` — the entrypoint for **other AI agents** (Codex today, possibly more later), added by Davide 2026-09-19 so every agent works from the same knowledge of the business. They are **thin pointers, not copies**: `AGENTS.md` carries the safety rules and says where to read, and each `.agents/skills/<name>/SKILL.md` points at the canonical `.claude/skills/<name>/SKILL.md`. So this file stays the single source of truth, and keeping it current keeps every agent current. **Standing rule (Davide): whenever the project changes, every agent's entrypoint has to keep up.** In practice: a new or renamed skill in `.claude/skills/` gets a matching pointer in `.agents/skills/`; a new authoritative doc, a moved file or a new standing rule gets a line in `AGENTS.md`; business facts go here, never duplicated into `AGENTS.md`, where they would drift. `/update-project` checks this every time. `web/AGENTS.md` is different: it is generated by `next dev` and must not be hand-edited.

## Business overview

**casdey HQ is `/admin` since 2026-09-19 (IMPROVEMENTS.md #2 and #4).** It is the one place for how the business is doing, live, and it replaced four sources that each went stale on their own clock: the old `/admin` numbers page, the casdey HQ Google Doc, the Marketing Plan Google Doc and the check-up artifact. Five tabs: **Overview** (to-dos with an Added and a Due date each, goals with live progress, who does what, a link to the latest check-up), **Numbers** (the previous `/admin`), **Marketing** (outreach, the weekly test review, Instagram, the plan) and **Business** (the offer, prices from the checkout's own catalogue, cost lines, per-gym economics and break-even worked out by `src/lib/unit-economics.ts`) and **Check-up** (each Sunday's analysis as its own note, `checkup_<sunday>`, its proposed to-dos, and earlier weeks). Nothing is pushed by hand: numbers are computed on every load, and everything written lives in casdey's own tables (migration `0041`: `hq_notes`, `hq_todos`, `hq_goals`, `hq_inputs`, `hq_costs`), edited by Davide on the page or by Claude with **`npm run hq`** from `web/` (`npm run hq -- summary` prints all of it; every command is listed at the top of `web/scripts/hq.mjs`). To-dos come from three places: **live signals** worked out on each load (`src/lib/hq-signals.ts`: an interested gym, an overdue test review, Instagram days with nothing approved, unsent DM drafts, a real gym's first week ending, a goal past its deadline), **proposals from the Sunday check-up** that Davide accepts or dismisses, and ones added by hand. A signal is recorded the first time `/admin` sees it (that is its Added date) and a ticked one stays ticked by its key. Goals are set by Davide in the Sunday review, not linked to Notion. Replies Davide sends himself are not logged anywhere casdey can read, so "follow up with this gym" stays until he ticks it. The Sunday routine writes its analysis (`npm run hq -- checkup set`, one note per week) and proposed to-dos there and sends a short "ready" email; see `.claude/skills/check-up/SKILL.md`. The marketing half is computed by `src/lib/marketing-summary.ts`, a port of `web/scripts/check-up-marketing.mjs`: **the two must stay in step**, or `/admin` and the Sunday review will disagree. **Everything below in this paragraph and the next is the history of the retired Google Doc.** It used to read: `casdey HQ` is the one-page business summary. Source of truth is **`casdey-hq.md` at the repo root**; the readable copy is a Google Doc at https://docs.google.com/document/d/1qJWaqeWSjsG_osRH8NmS2JxP2URtfccCsRaPWypTGOI/edit, **owned by info@casdey.com**. Seven short sections: the offer, prices, a software-and-monthly-cost table, per-gym unit economics, break-even, current status, and what is open. **Revise it whenever the numbers or the status move** — a price change, a new paid service, the first paying gym, a plan upgrade — and keep it short, because the point is that it stays skimmable rather than complete. It holds the business view; this file stays the engineering and decision record, and the two should not duplicate each other. It sits alongside the **Marketing Plan** Google Doc (see "Marketing plan" below), which owns outreach specifically.

**(Deleted 2026-09-19, with `casdey-hq.md`, `npm run doc:push` and `web/scripts/google-doc.mjs`; all three are in git history. The service-account technique below is still the one to reuse if a Google Doc ever has to be written again.)** **How it was updated: edit `casdey-hq.md`, then `npm run doc:push` from `web/`.** That rewrites the Doc in place, keeping the same file id and link, so any edit made in the Doc itself is overwritten by the next push. **Why it works this way, because it is not obvious:** the Google Drive *connector* can create, read and trash files but cannot write content (its `update_file` handles title and parent only), so a doc meant to be revised constantly could only ever be replaced, changing its id and breaking every pointer. The outreach routine had already solved the same problem for Sheets, and the answer is the same here: the **service account** `casdey-routine@casdey-gws-cli.iam.gserviceaccount.com` mints a token with the JWT Bearer grant and calls the Google API directly. `web/scripts/google-doc.mjs` uses Drive media update rather than the Docs API batchUpdate, because media update converts Markdown wholesale so headings and tables survive, while batchUpdate would mean assembling every cell by index.

**Ownership needs a manual step, and will again for the next document.** A file created through the connector is owned by whoever the connector authenticates as, which is Davide personal account; there is no way to create one as info@ (the connector offers no owner role, and the service account cannot impersonate a user without domain-wide delegation, which needs Workspace). Ownership also cannot be taken from the receiving side: signed in as info@, the share dialog shows no "Transfer ownership", because only the current owner can offer it. So: create it, share it with the service-account address as **Editor** (without that, pushes return PERMISSION_DENIED), then transfer ownership from the owning account. The leads Sheet has the same shape and always did — info@casdey.com owns it, the service account is a writer on it; the routine never created that file, it only edits one info@ already owned.

- Company name: casdey (always lowercase) — deliberately generic, not tied to any specific product, so the business can pivot without changing identity/domain/email.
- Current product: a SaaS tool for **lapsed-member win-back at gyms and fitness studios** (pivoted from dental — see "Niche pivot to gyms/fitness" below; the pivot is fully executed as of 2026-09-03). It finds members who lapsed or cancelled, re-engages them automatically over email and (Pro plan) WhatsApp, and handles the whole loop through to booking: replying in the gym's name and putting the class/session straight into the gym's Google Calendar, not just flagging a reply for staff.
- Guarantee mechanics: the "profit or nothing" guarantee (a Pro-plan feature) requires knowing revenue actually generated per gym, so casdey needs write access to the gym's calendar (to book) and its membership/price list (to value each booking), not just read access to member records.
- Target market: **gyms and fitness studios in the United States and across Europe**. **The US was added on 2026-09-19 (IMPROVEMENTS.md #3), Davide's call**, because European sourcing had thinned out to small cities and generic inboxes: the product sells there now (US signup with a timezone, USD billing, month-first date import, visitors shown their own currency), and once a postal address exists for US cold-email law (CAN-SPAM), **all 100 first-touch emails a day go to the US**, with European leads already contacted still getting their follow-ups. Until then Europe keeps running as before. Before that, the market was Europe only (Italy, Ireland, Germany, France, Spain, Netherlands, etc.): Davide is Italian and thinks of the market as Europe, not UK-first. Pricing leads in **EUR**; GBP kept for UK as its own round numbers, not a live conversion. (Early CrossFit/community-box signal from outreach, see Stage 1.)
- Price (early flat rate, first outreach wave): £199/month.
- Price (single "Premium" tier, Aug 2026, now superseded): £250/mo GBP or €290/mo EUR after the free trial week; £225/mo GBP or €262/mo EUR annual.
- **Price (current, 3-tier, decided 2026-09-03 — Track F). Pricing leads in EUR, with the US on its own dollar prices since 2026-09-19.** Authoritative table + cost basis in `web/SAAS_V1_PLAN.md` §F0. **US prices, 2026-09-19:** Standard **$99/mo** ($990/yr), Pro **$299/mo** ($2,990/yr), round US numbers rather than a conversion, and the paid first week is **$1**; the 20% early-adopter coupon is a percentage, so it covers dollars too. **The price shown is what the gym pays: no VAT is added at checkout** (no Partita IVA to charge it under; the pricing page said "Prices exclude VAT" until 2026-09-18, corrected). Summary: **Free** €0 (import + see lapsed, 50-member cap, 5-row list, no send); **Standard €99/mo** (~£89; €990/yr — email win-back + at-risk, 200-member cap, no WhatsApp, no guarantee); **Pro €289/mo** (~£249; €2,890/yr — everything incl. the WhatsApp channel and the profit-or-nothing guarantee, 2,000-member cap). Early adopters get a **lifetime 20% off** either paid tier (a single currency-agnostic `STRIPE_COUPON_PERCENT`, replacing the old £50/€59 fixed coupons). Existing "Premium" accounts map to **Pro**. The free trial week grants the full Pro feature set.
- Core validation rule: validate with real payments/commitments, not with polite interest. "Sounds useful" doesn't count — a deposit or explicit "I'll pay £X on launch" does.
- See "Offer evolution (beta → V1 → V2)" below for how this price is wrapped for waitlist/beta contacts specifically, including a lifetime early-adopter discount.

## Niche pivot to gyms/fitness (raised 2026-08-17, EXECUTED — the product, brand, site and outreach are all gym-native as of 2026-09-03; this section is the history of how the decision was reached)
The core insight (a business loses real revenue when past customers go quiet and nobody follows up) stays. What's in question is whether **dental practices** are the right buyer for it, not whether the reactivation mechanic works.

**Why this came up.** The automated cold-outreach system had sent 216 emails with zero genuine replies as of 2026-08-15 (see Stage 1 below). A session on 2026-08-17 concluded that reads as a demand signal, not a messaging problem: dental practices are a documented industry case of slow, reluctant technology adoption (resistance to workflow disruption, high onboarding friction), which matches the outreach result. Combined with the weight of handling health-adjacent patient data (GDPR Art. 28/44 obligations, no legal entity yet to carry that liability), Davide raised whether dental is worth the combined cost of low buyer engagement plus high compliance burden, even though the underlying reactivation problem is real.

**Decision.** Keep casdey as the business (name, domain, brand, the reactivation-plus-booking-plus-guarantee mechanic), but reconsider the target niche. Candidates discussed: e-commerce, fitness/gyms, SaaS churn. Quick research ruled out e-commerce (win-back is a bundled feature already inside tools stores pay for, Klaviyo alone does roughly $937M revenue on this) and SaaS (no real domain experience beyond casdey itself). **Fitness/gym studios, targeting cancelled or lapsed members, is the current leading candidate:** gym platforms (Mindbody, Glofox, ABC Fitness) manage active members but drop cancelled ones into a real, documented workflow gap; reactivation rates are already proven and quantifiable in that market (10-20% for members lapsed 30-90 days, 5-10% for 90-180 days); gym/studio owners are demonstrably more marketing- and tech-engaged than dentists (already buying automation tools, already paying agencies to run manual win-back campaigns); and membership data isn't health data, which meaningfully shrinks the GDPR burden that was part of the original discomfort.

**What ports over from the current build almost untouched:** dormancy/lapse detection, campaign sending, the manual price list feeding the revenue-recovered calculation (membership tiers instead of treatment prices), the Google Calendar booking loop (class booking instead of appointment booking), CSV import shape, the guarantee mechanic itself.

**What needs rethinking, not just relabeling:** the "dormant" signal is fuzzier for gyms (an active member who stopped attending is a retention problem, not a reactivation one; a cancelled member is the clean, unambiguous target and the one the research gap actually points at); buyer-facing copy needs a full voice audit (dentists think in "patients/appointments", gym owners think in "churn/MRR/class fill rate"); the outreach channel itself may need to shift (gym owners may be more reachable via Instagram DM or industry Facebook/Slack groups than cold email).

**Status (updated 2026-08-19): execution has started on the site, product build has not.** The daily dental cold-outreach routine is confirmed **not live** (corrected 2026-08-19; earlier entries here assumed it was still running under Davide's own pause request, that assumption was wrong). With outreach not live, Davide had `/waitlist` reworked for the gym/fitness pivot (hero, steps, FAQ, form labels, and the confirmation email now speak to gyms and studios, not dental practices, commit `f4ca0cf`), then had the rest of casdey.com **unpublished**: `next.config.ts` now temporarily redirects everything (homepage, `/app` the SaaS product, `/login`, `/auth/*`, `/book/*`, `/reset-password`, `/terms/*`, `/u/*`) to `/waitlist`, so casdey.com currently shows only the gym-facing waitlist page and `/privacy` (kept reachable since the waitlist form links to it). `/api/*` is untouched, the waitlist endpoint, Stripe webhook and the cron send job all still run, none of it is browsable UI. Commit `323b622`, pushed and live. Fully reversible: it's a `permanent: false` (307) redirect block, deleting it republishes everything as it was. No product code has been rebuilt or retargeted, no outreach has gone out under the new positioning yet. **Superseded 2026-08-23 (see `next.config.ts`):** this fuller redirect block was narrowed so only the public marketing homepage `/` still redirects to `/waitlist` in production; `/app`, `/login`, `/auth/*`, `/book/*`, `/reset-password`, `/terms/*`, `/u/*` and `/privacy` are now all reachable in production, for the gym free-trial/feedback loop with invited gyms. Going fully public (republishing the homepage too) is a separate later decision. So the "casdey.com shows only the waitlist" claim in this paragraph and the "production stays unpublished (still only `/waitlist`)" claim in the 2026-08-21 bullet below are both stale as of 2026-08-23.

**Execution continued the same day (2026-08-19): visual rebrand shipped. Superseded 2026-09-04 by brand v4 "Chalk & Struck Gold" (see "Brand v4" below); this paragraph is kept as the history of how v3 came about.** Davide asked for a darker, gym-appropriate identity ("black-ish/dark-ish and some gold"). The result is v3, "Iron & Brass": a dark iron ground (`#17140F`) with a matte brass primary (`#A9814A`), replacing v2's light paper/teal system, built from the weight room's own material world (cast iron, brass fittings, chalk) rather than the shiny "elite gym" gold cliché. Every token in `web/src/app/globals.css` kept its v2 name, only the hex values moved, so the swap re-themes the whole product without touching component code; `brand assets/casdey-brand-guide.html` was bumped to v3 to match. Commit `667610c`. The shared header/footer chrome the earlier `/waitlist` rework hadn't touched (nav label, footer tagline, meta keywords) was also cleared of dental references (commit `280e481`), and the header/footer nav was stripped down to just a wordmark and a "Join the waitlist" CTA, since every other link (How it works, Why casdey, Sign in) pointed at a route the unpublish redirects had already killed (commit `ef6acf0`). A real production deploy outage was found and fixed the same day while getting all of this live, see the Vercel bullet under Infrastructure.

**Execution continued (2026-08-21): the product itself was rebuilt for gyms, locally.** The pivot is no longer just site/branding: the whole SaaS was retargeted from dental to gyms/fitness studios in one pass, kept entirely local (no push, no deploy, production still serves only `/waitlist`). What changed:
- **Full dental→gym rename, code AND database.** The domain model was renamed throughout (~120 files): `practice`→**gym**, `patient`→**member**, `practice_members` staff logins→**gym_users**, `appointment`→**booking** (kept generic, since gyms sell both memberships and one-off classes), `practice_services`→**services** ("Prices" in the UI), `dormant`→**lapsed**, `reactivated`/`rebooked`→**returned**. DB tables/columns/functions/policies renamed to match; the app routes moved too (`/app/patients`→`/app/members`). The direct-integration stub was relabelled from Dentally to **Mindbody** (`src/lib/ingestion/mindbody.ts`); CSV import stays the real path. The canonical migrations (`0002`–`0010`) were rewritten gym-native.
- **WhatsApp removed entirely.** Davide's call: WhatsApp does not fit gyms (members would find a gym WhatsApp message strange; email is the right channel), so the whole channel was deleted, code and UI (`src/lib/whatsapp/*`, the webhook, the settings page, the campaign/member WhatsApp UI, the `Channel` type narrowed to `"email"` only). Recoverable from git history if the niche ever wants it. **Reversed 2026-09-03, and the revival has shipped (code):** an engaged outreach lead (JD) asked for WhatsApp, so it was revived and brought into V1 (channel confirmed WhatsApp, not SMS) — Track E1 in `web/SAAS_V1_PLAN.md`, commit `cd0fd70`. The channel code was restored from `c3c9a25` and adapted gym-native (`gym`/`member`/`gym_id`, `is_gym_user` RLS); the 5 never-fixed reply-loop audit bugs are fixed; a Settings → WhatsApp tab, an Email/WhatsApp switch on the new-campaign form, a WhatsApp test-send, and a member-page conversation card are wired; Twilio + Anthropic are back on the DPA sub-processor list. **Migration `0014_whatsapp_channel_gym.sql` is APPLIED to the live DB (2026-09-03, transaction, verified)** — re-adds the four `whatsapp_*` tables, `members.consent_whatsapp`, `gyms.whatsapp_enabled`/`whatsapp_template_name`, `campaigns.whatsapp_template_name`, widens `campaigns.channel` back to `('email','whatsapp')`, adds `claim_whatsapp_ai_turn()`. **Progress 2026-09-04 (B8 done):** the Twilio account is upgraded off trial (`type: Full`, $20 balance, verified via the Twilio API, not the dashboard), and `TWILIO_ACCOUNT_SID` / `TWILIO_AUTH_TOKEN` / `ANTHROPIC_API_KEY` are set in Vercel Production and Preview. **Still needed before it works in prod, and it is no longer a billing problem:** a **Meta-approved WhatsApp sender and an approved template** (the account holds 0 phone numbers, 0 messaging services and 0 content templates), the inbound webhook pointed at `https://www.casdey.com/api/whatsapp/webhook` (**www, not the apex** — see the Stripe webhook note under Infrastructure: the apex 308-redirects to www, Twilio validates its signature against the exact URL it posted to, and a redirect breaks that outright) (configured *on* that sender, so it comes with it), `TWILIO_WHATSAPP_FROM` set to the real sender, then a prod verification pass. Until then the channel degrades cleanly (a "disabled" provider: settings save, sends error clearly) — which is exactly why `TWILIO_WHATSAPP_FROM` was deliberately left unset rather than pointed at Twilio's shared Sandbox number.
- **DB migration `0011_rename_dental_to_gym.sql` — applied to the live cloud project (confirmed 2026-09-03, plan item B3 done).** It renamed the pre-launch SaaS tables in place (`practices`→`gyms`, `patients`→`members`, `appointments`→`bookings`, etc.), dropped the four `whatsapp_*` tables, and deliberately left `waitlist_signups` untouched (its columns keep `practice_name`/`practice_software`, so the live waitlist form keeps working). A read-only schema probe over `SUPABASE_DB_URL` on 2026-09-03 confirmed `0011`, plus `0012_at_risk_campaigns.sql` (`gyms.at_risk_after_days`, `campaigns.kind`) and `0013_cancellation_reason.sql` (`members.cancellation_reason`/`cancelled_at`, widened `member_events_type_check`), are all live. There is no local Postgres/Docker here, so local dev points at the same live cloud DB.
- **Local visibility:** the `next.config.ts` unpublish redirects are now gated to `process.env.NODE_ENV === "production"`, so `next dev` shows the full gym landing + `/app` locally while production stays unpublished (still only `/waitlist`) exactly as before. Verified locally: gym landing renders, `/app` reaches its own login (not the waitlist redirect), `/waitlist` still works; `tsc`, `eslint`, `vitest` (110 tests) and `next build` all green. **Superseded 2026-09-07: the redirect block is gone and casdey.com is published** — see the Stage 2 bullet on publishing.
- **Not done / deferred:** nothing pushed or deployed (kept local per Davide); the GDPR/legal docs and `web/SAAS_HANDOFF.md`/`SAAS_ROADMAP.md` were not fully re-audited for gym wording this pass; the marketing landing copy was retargeted but not design-reviewed against references.

**Execution continued (2026-09-03): the gym product was pushed to prod and the V1 build largely finished.** Everything since 2026-08-21 is now on `main` and deployed. In one session: Track A (self-serve build) completed and pushed; **B1** (Google OAuth published, no verification), **B2** (Calendar env in Vercel), **B3** (migrations `0011`–`0013` verified live), **B5** (Vercel = Hobby), **B6** (stray Vercel project deleted); **A6** calendar hardening finished incl. a **GiST exclusion constraint against adjacent-slot/buffer double-booking** (migration `0015`); **Track E1** — the WhatsApp channel revived gym-native with its 5 audit bugs fixed (migration `0014`); **Track F** — a **3-tier plan model** (Free / Standard / Pro) replacing single "Premium", EUR-first pricing, `gyms.plan_tier` (migration `0016`), tier-aware Stripe + billing UI. `0014`/`0015`/`0016` all applied to the live DB in verified transactions. **B9** answered: the engaged lead JD runs **LegitFit**. **Closed 2026-09-04, and Track E2 has now formally exited to V2:** LegitFit publishes no developer API (no keys, no webhooks), and its Zapier app is **trigger-only** — 4 triggers, 0 actions, 0 searches — so there is no way to ask it for a gym's existing member list. Triggers only fire forward, and casdey needs the *historical* members who already lapsed, so a Zapier connection would surface zero of them on day one and thereafter report only active bookers. **CSV import (A3) is the V1 path, and for LegitFit gyms it is the only one**; getting JD's real LegitFit export is now the priority half of B4. Full evidence and the two open questions for JD are in `web/SAAS_V1_PLAN.md` E2. **All of this is now closed (see the 2026-09-07 publish bullet under Stage 2); at the time of writing it read: still Davide's before V1 is truly ready:** B8 (upgrade Twilio for WhatsApp), F2 (create the live Stripe products/prices/coupon + set 9 env vars), Track C (prod verification of Stripe/Resend/Calendar/WhatsApp), Track D (Davide's own end-to-end walkthrough). The full board is in `web/SAAS_V1_PLAN.md` §7. **Brand identity refresh was parked here, then unparked and shipped the next day**, see "Brand v4" below. Outreach positioning still unchanged — no outreach has gone out framed around 3-tier pricing yet.

**Brand v4, "Chalk & Struck Gold" (2026-09-04, commit `f524f86`), superseded visually by v5 below.** The parked brand-identity refresh was unparked early on Davide's direction: a white-ish ground, gold as the primary, Outfit for titles. It replaces v3 "Iron & Brass" and follows the same discipline as v2→v3: every token in `web/src/app/globals.css` keeps its name, only the hex values move, so nothing in component code had to change. What it is:
- **Ground is Chalk `#F7F7F4`**, deliberately cool rather than cream: warm cream under gold is the predictable pairing, and it makes the gold read as ambient warmth instead of intent.
- **Gold split into two honest values**, because one value cannot do both jobs, because any gold light enough to look like gold is too light to read as text. **Struck Gold `#8F6A10`** is the READ value (links, icons, small text, borders; 4.6:1 on Chalk); **Leaf `#D4AF37`** is the FILL value (button fills, panels, the mark's arc; 8.7:1 under ink, always with near-black on it, never the reverse). All contrast ratios were computed rather than eyeballed, which caught two silent failures in the first pass.
- **Type: three families, four roles**, down from four families. **Outfit** carries every title *and* the wordmark (retiring Raleway, which existed for exactly one word); **IBM Plex Sans** takes everything that gets read, picked for contrast of temperament against Outfit and because it was drawn for the kind of dense tables the member/campaign screens use; **IBM Plex Mono** replaces JetBrains Mono for anything literal.
- **casdey's first real logo mark**: four bars pinwheeled around an open centre, one of them gold (Leaf). Deliberately abstract, it neither spells the name nor illustrates lapsed members. An earlier attempt (an open ring with a gold arc closing the gap) was dropped because it collapsed into a plain ring the moment it was printed in one colour. Drawn in code at `web/src/components/wordmark.tsx` (`Mark` / `Wordmark` / `Logo`) plus `web/src/app/icon.svg`, so it scales and re-themes with the tokens. Header, footer, app shell and login use the full `Logo` lockup. (Corrected 2026-09-10: this bullet had described the abandoned ring mark.)
- **`.on-deep` is load-bearing.** The one inverted plane needs its neutral and gold roles remapped in a scope block, because `--ink` went from chalk-light to near-black in v4: any `text-ink` sitting on `bg-deep` without that class is invisible. Two such bugs were caught and fixed while shipping; check for it on any new dark band.
- Danger is a token now (`--danger` / `--danger-soft`), replacing a hardcoded salmon that had been tuned for v3's dark ground and washed out on Chalk.
- **The product dark theme's ground is `#09090a`, YouTube's own dark ground** (changed 2026-09-12, commit `f1ee179`, Davide's call: true `#000` looked harsh on a real screen). Measured off youtube.com in the browser rather than eyeballed from a screenshot: its app layer paints `#09090a` everywhere between the cards, while the `#0f0f0f` usually quoted for YouTube sits on the root underneath and never shows. The sidebar (`--deep`) is `#0f0f0f`, and the contrast figures in `globals.css` and the brand guide were recomputed for the new ground. Before that it was true `#000` (2026-09-09, commit `7630e24`), and before that it had been a warm near-black (`#16160f`) carried over from v3 "Iron & Brass", which read as brown on a real screen. Scoped to `[data-theme="dark"]` in `globals.css` only, so the light marketing site is untouched; every neutral was pulled to a clean warmth-free grey so gold stays the one warm thing, and `--sea` plus `.on-deep`'s hardcoded neutrals got a dark-scoped override. `casdey-brand-guide.html` gained the dark-palette section it never had.
- **The signup confirmation email carries the mark (2026-09-13, D1 finding #71).** Mail clients refuse SVG, and the logo only exists as code-drawn SVG, so `web/public/email/casdey-mark.png` is the v4 mark rasterised at 144px and served at `https://www.casdey.com/email/casdey-mark.png`. The template itself lives in the **Supabase dashboard** (Authentication, Emails, Confirm sign up), not in this repo: a Chalk ground, a white card, the mark beside a live-text "casdey", a Leaf button, and a line telling the owner to open the link on the device they will use (D1 #72). Confirm sign up carries the branded design. **Reset password** was changed on 2026-09-18 for a different reason: its link now points at `https://www.casdey.com/auth/confirm?token_hash={{ .TokenHash }}&type=recovery`, so it works on any device (see the password-reset bullet under Stage 2). Magic link and change email are still Supabase's plain ones. Supabase's own preview shows the image as broken because its sandboxed preview blocks remote images; that is not a fault in the email.

**Brand v5, "Charcoal Product System" (2026-09-21), current on the local `codex/product-ui-refresh` branch.** Davide approved a fuller product and landing refresh inspired by the restraint and motion discipline of Apple, Attio, Shopify, ElevenLabs and ChatGPT. The signed-in workspace, founder-only `/admin` HQ, and public landing now share a near-black `#09090A` ground, a single elevated `#191A1D` card/sidebar plane, cool neutral dividers and a quiet 10px dotted field kept behind content. `/admin` defaults to dark like `/app`, while the saved light-mode switch remains respected. Gold stays functional: primary action, progress or recovery, never ornamental. Product UI uses Manrope for a compact, confident interface face; the wordmark and marketing display treatment remain Outfit. Motion stays reserved: transform/opacity only, short 180–220ms easing, and the landing's product story advances one focused step at a time inside its card-height scroll stage with a visible gold progress line. The authoritative rules are `brand assets/casdey-brand-guide.html` v5; this change is local and unpushed until Davide explicitly asks to push it.

**Next step:** Davide's manual research (started 2026-08-17) produced a go-to-market plan, see "Go-to-market plan for the niche pivot" below. No outreach has started under the new positioning yet, that plan is also not yet executed.

## Go-to-market plan for the niche pivot (drafted 2026-08-18, not yet executed)
Davide's own notes from a video plus Alex Hormozi's teachings (his business mentor), organized here, nothing added. Sits on top of the section above: this is how the pivot would actually get validated and grown, once the niche and product rework are settled. Not started, no communities joined, no outreach sent under this plan yet.

**Guiding principles**
- Look at competitors first.
- Get the waitlist live before publishing V1, and refine V1 itself before that.
- In general, sell before you build. In casdey's case the product is already almost ready, so this mostly applies to the rework/positioning that still needs doing.
- Get feedback at every level, from the very first cold outreach conversation up through 100+ customers. Get data, improve, repeat.
- Treat every feedback conversation like the person is a business partner, and actually implement what they say they need.
- Price on the value the customer is getting, what competitors charge, and casdey's own costs. Keep usage limits in place until real customer costs are known.

**V1**
- Build relationships: talk directly with gym owners.
- Give the product away for free (the existing free trial) in exchange for feedback.
- Find people in communities, and reach business owners directly by DM and email, offering the free trial.
- Try converting some of them into paying clients, and keep asking for feedback throughout.

**V2**
- Use the reviews and testimonials collected in V1 to rewrite the copy.
- Use the revenue generated to fund paid ads.
- Keep running outreach for the next wave of clients.
- Repeat the whole process.

**Converting to paying clients**
- Start with manual cold outreach purely to collect data: ask things like "what do you think of this tool?" and "how much would you pay for it?"
- When converting someone to a paying client, don't only offer the lifetime discount, also push a referral campaign.
- Plan tiers: Free, Starter, Pro.

**Paid ads and organic growth, later**
- Start paid ads by paying small creators and giving them free access, rather than running ads directly.
- Only move to paid ads once it's clear where the first customers are actually coming from.
- Referral links for small creators can run either before paid ads start or alongside them, both work.
- Start with organic content (Reddit, X) early, per Nate's advice.

**Practical week-by-week plan**
- Week 1: join 2 communities, find 30 people to talk to, contact 5 of them per day.
- Week 2: get some of those people to try the software for free (with the current offer), collect feedback, then ask them to pay using the first-time-client offer. Repeat the loop (get feedback, get data, improve) until it feels ready to open up more.
- Once ready to open up: bring in paid ads and organic content (Reddit, X, Instagram, blogs).

**How to do it, per outreach message (from $100M Leads)**
- Personalize: do a little research on the lead before sending a message.
- Big fast value: initially, while V1 isn't ready, just collect data; once it's ready, give them the amazing offer.
- Follow-up: contact them on various channels (use that as an ice-breaker to open a conversation), follow up the day after, and do the walk-away follow-up 3 days after that.

Keep automation on the side while lead volume is still small early on, add it back later. If the Week 1/2 volume (30 total, 5/day) turns out too low, increase it.

## Marketing plan (rewritten 2026-09-07, Hormozi "More, Better, New" — current authority for outreach volume + testing)

**Mentor context.** Davide's only mentor is Alex Hormozi. He has read *$100M Offers* and *$100M Leads* (the third book, *$100M Money Models*, still to read); Hormozi's videos are treated as restating the books, so either source counts. Marketing and outreach decisions should be checkable against a Hormozi framework, and Davide expects future casdey work in this project to be run the way Hormozi would.

**Source of truth: the `marketing_plan` note on `/admin`'s Marketing tab, since 2026-09-19.** Davide wrote it and edits it there (`npm run hq -- note get marketing_plan` to read it). It was copied word for word from the Google Doc **"Marketing Plan"** (file id `1dd6GLXQ2_4ZKCpY43HtUV9YgJVRWGH5AJiJ-Qfpm4J0`, info@casdey.com Drive), which is retired: rewritten from scratch 2026-09-07 against *$100M Leads*, no longer updated. This section records the history and the decisions; the note wins if they diverge.

**Baseline being changed** (full mechanics: `.claude/skills/gym-outreach/SKILL.md` on branch `gym-outreach-automation`, plus the two live cloud routines): 75 cold emails/day (A/B split — A feedback-driven, B onboarding-driven) + 2 follow-ups per lead; 10 cold DMs/day (first touch references the email) + 2 follow-ups per lead.

**Changes to implement** — Hormozi's "More, Better, New"; **"New" is deferred** (too small to add channels yet):
1. **More volume.** **APPLIED 2026-09-07** (Resend Pro landed the same day, removing the Free-plan 100/day block; product shares the quota, see the Resend bullet under Infrastructure and `web/SAAS_V1_PLAN.md` G1a). Final shape: **first-touch is a fixed 100/day** from its own queue, **follow-ups uncapped** from a separate queue (every due FU1/FU2 the day it's due), total ≈ 100 + due follow-ups (~250-320/day steady state). The doc's original "100 FT + 100 FU" was unstable (100 first-touch spawns ~190 follow-up obligations/day); separating the queues and uncapping follow-ups fixes it — first-touch never competes with the backlog. IG: 10 first-DM + ~10 follow-up drafts/day.
2. **Better — a weekly test loop.** Each week, run one improvement test on one of: first-touch cold email, follow-up emails, DM first touch, DM follow-up. The lever can be the subject line, hook, CTA/offer, how the offer is framed, tone, etc. End of each week, a "meeting" (Davide + Claude): (a) read the results and pick the winner(s); (b) append the outcome to a **cumulative test log** so the next test builds on the current best, not square one; (c) decide the next test to beat the current best. If the current best survives **4 consecutive weekly tests**, treat it as an established winner and move testing effort to another asset.

**Status (2026-09-07): APPLIED.** Resend Pro live + the SaaS published, so the "More, Better" changes went in the same day.
- **Email routine** (`trig_018wp58QLBbeuBPbMA6Fy1sU`, prompt updated via the RemoteTrigger API): fixed **100 first-touch/day** + **uncapped follow-ups** from a separate queue; no per-run ceiling; **transition** cap of 150 follow-ups/run for the 2026-09-08 to 2026-09-15 runs only (backlog drain), uncapped after. Reserved-floor split retired. Sourcing ceilings raised (skip at 300 unsent, source up to 200/run).
- **Week-1 test T1 — first-touch subject line A/B**, live from the 2026-09-08 run: `S1` = "quick question about your lapsed members", `S2` = "win back the members who cancelled", by lead-# mod 4 (0,1→S1 / 2,3→S2), independent of the body A/B. Logged in new `Send Log` column `Subject Variant` (Q). Running CTA A/B (permission vs free-to-try) = test **T0**.
- **Test Log tab** created + seeded (T0, T1) in Casdey-Gym-Leads. Routine does not write it — the weekly review is a **manual chat session** in the marketing chat (Claude computes per-variant numbers from `Send Log`, Davide pastes the row). 4 weekly tests unbeaten → established winner, move on. First review: the Sunday after the 2026-09-08 run.
- **IG routine** (`trig_01La223qWjwxoumG4z8gssL8`), **disabled 2026-09-20, see the DM pause bullet below**: buffer cap 40→60, ~10 first-DM + ~10 follow-up drafts/day (**raised 2026-09-13, Davide's call: 20 first-DM drafts a run, buffer cap 100**, and the daily report tells him to drop back to 10 a day if Instagram shows an action-block warning, since the account is new); follow-ups stay DMs patterned on the email sequence (what the doc's "as emails" meant). `IG Outreach` columns N-S created.
- SKILL.md synced on branch `gym-outreach-automation` (commit `d30423c`). `MARKETING_STAGED.md` deleted (applied).

**Status (2026-09-13): the first Sunday review, now inside `/check-up`. Supersedes the "manual chat session in the marketing chat" line above.** The weekly test review is section 1b of `.claude/skills/check-up/SKILL.md`: the check-up computes each arm's sends, replies and engaged leads, proposes a verdict, Davide decides, and only then is the Test Log row written (`npm run testlog:update` from `web/`). The unattended Sunday routine prepares the review and never writes. What this review decided:
- **Goals (Davide):** 1% **engaged leads** for the week ending 2026-09-20, measured on that week's contacts (about 7 interested gyms from about 700; the all-time rate is dragged by older cohorts), and **2 paying gyms by 2026-10-13**. An engaged lead is a gym interested in the product, **not a reply**; a "yes, send me the video" counts. The 3% target stays on the reply rate, reported separately. Both rates are on `/admin` (Outreach section) and in the check-up.
- **Where it started:** 926 gyms contacted, 12 genuine replies (1.30%), 1 engaged lead (BodyActive, 0.11%). 11 of the 12 replies were a no, a competitor, or confused.
- **T0 closed**, A kept on direction only (3 of 335 against 0 of 290): variant B's free setup failed because handing a member list to a stranger is a hidden cost (*$100M Leads* pg 97). **T1 closed**, no winner, `S1` for every first touch.
- **T2 live from the 2026-09-14 run: A against V.** V sells the result, not the tool ("what if the members who left your gym started paying you again"), says about ten minutes to set up, and gives first: a free general video plus the exact message casdey would send that gym's ex-members, in exchange for an honest opinion. Judged on engaged leads per arm. Davide's framing, checked against *$100M Offers* pg 96 and *$100M Leads* pg 94 and 169-170; it is gentler than Hormozi's own cold email template, which settled the "too salesy" worry. Instagram DMs switched to the same framing outright.
- **No cold calls**, Davide's call, even though Hormozi's "follow up more ways" points there. Logged as a divergence in the Hormozi ledger.
- **The T2 video is live at `https://www.casdey.com/see` (2026-09-13, commits `1652653`, `175f169`).** A 74-second ad-style product film, not a screen recording: animated casdey UI, subtitles and original music synthesised in code, drawn on a canvas and encoded frame by frame with WebCodecs in headless Chrome. The first render was recorded in real time and stuttered, because a slow frame came out late; encoding each frame on its own timestamp fixed it. `/see` is hidden (unlinked, not in the sitemap, `noindex`) and serves a 720p, 15 MB copy from `web/public/video/`; PostHog records `promo_video_played` and `promo_video_completed`. The link is on the `T2 VIDEO URL` line of the email routine's prompt, so yes-reply drafts carry it. The Instagram routine's prompt has the same line, but only for the report's reminder to Davide, who replies to an Instagram yes by hand; the DM drafts themselves stay link-free. The render source and the 1080p master live outside the repo, in that session's scratchpad.
- **Organic Instagram content added as a channel, 2026-09-13, and RETIRED 2026-09-20 after 4 posts (see the retirement bullet below). `content-plan.md` at the repo root is the plan, with the diagnosis at its top.** What follows is the decision as it was made. Davide's call, reasoned through `/hormozi` (ledger entry "The founder's idle week"): Instagram only, faceless for now, one post a day for 100 days (2026-09-16 to 2026-12-24), English, about ex-members and winning them back. It reverses the 2026-09-10 deferral of "New" for content only; the outreach routines, T2 and volume are untouched. The same session wrote the manual playbook for after a gym says yes into the `gym-outreach` skill on branch `gym-outreach-automation` (commit `7586c22`), and corrected that skill's "full offer" section, which still told leads the first week took no card although `CASDEY_PAID_TRIAL` has been on since 2026-09-12.
- **Instagram DMs are PAUSED since 2026-09-20 (Davide's call).** (Posting was retired later the same day, so all of Instagram is now off; the bullet below covers it. At the time of this decision posting was untouched, and it was part of the argument.) The numbers that decided it, live from the sheet: **397 DMs (201 first touches, 127 FU1, 69 FU2) bought 1 reply and 0 engaged leads**, against email's 1,393 gyms contacted, 19 genuine replies (1.36%) and 2 engaged (0.14%). Crucially **the data does not condemn the channel and was not the argument**: at email's engaged rate, 201 DMs would be expected to produce 0.29 engaged leads, so zero is what a working channel looks like at that sample size. The argument is price. Email is automated; every DM is sent by Davide's own hand, and one engaged lead at the demonstrated rate needs about 700 messages, which is 35 days of manual work at 20 a day, while email does 100 a day unattended. That hour is better spent on the US switch. A mechanical doubt supports it: a cold DM from a 12-follower account lands in Message Requests, where it may never be seen, and casdey cannot measure delivery. Reasoned through `/hormozi` (Core Four, "More before New") and logged in the ledger, which also records the restart condition: a believable Instagram presence, or cold email saturating, and then only as a second touch to gyms already emailed, about 5 a day. What this changed, mechanically: the IG routine is disabled (see Stage 1), the drafts already in `IG Outreach` are deliberately left unsent, and `/admin`'s "Send N Instagram DMs" to-do is gated off behind `IG_DMS_PAUSED` in `src/lib/hq-signals.ts`, because its key is per-day and would otherwise nag forever. Turning the channel back on means that flag plus re-enabling the routine.
- **The Instagram content pipeline is built and live, 2026-09-13 (commits `e9db030`, `9cafe31`, `116b76c`; reels `d56cea3`). `content-plan.md` "How posts get made, reviewed and tracked" is the full spec.** Claude writes each week's posts as `content/instagram/<batch>.json` and runs, from `web/`: `ig:render` (reels as a 1080x1920 MP4 plus a cover, drawn by `web/scripts/instagram-reel.html` and encoded frame by frame with WebCodecs and `mp4-muxer` in headless Chrome, so no ffmpeg; carousels as 1080x1350 PNG; output gitignored, as are the stock clips in `content/instagram/footage/` and the music in `content/instagram/music/`), `ig:drive` (uploads to the info@casdey.com Drive folder "casdey Instagram", one folder per post, via the `gws` login), `ig:sync` (writes the `IG Content` tab) and `ig:stage` (`reel.mp4` and `cover.jpg`, or slide JPEGs, to the public Supabase bucket `instagram`, which Instagram fetches from). **Davide approves a week at a time** by setting Status to Approved in `IG Content`; `/api/cron/instagram` (a second daily Vercel cron, `30 10 * * *`, about 12:30 in Italy) publishes approved posts whose planned date has come (a row whose Format is Reel goes out as an Instagram Reel shared to the feed, which is why the route's `maxDuration` is 300 seconds, Vercel Hobby's ceiling with Fluid compute), writes back the posted date and link, and on Saturdays appends followers, reach and accounts engaged to `IG Weekly` (profile visits are no longer offered by Instagram's API). A row goes to "Publishing" before Instagram is called, so a cut-off run can never post twice. **Instagram access:** Meta developer app **"casdey publisher"** (id `27761242693572565`), owned by Davide's personal Facebook developer account, Instagram API with Instagram Login, Standard Access (own account, no app review or business verification), permissions `instagram_business_basic`, `instagram_business_content_publish`, `instagram_business_manage_insights`; `casdey.co` (a Business account) is an Instagram Tester on it, which Meta requires before an unpublished app can connect an account ("Insufficient developer role" otherwise). The token lives encrypted in `public.instagram_account` (migration `0039`, same AES key as the calendar tokens) and the cron renews it weekly; to replace it by hand, Generate token in the app, set `INSTAGRAM_ACCESS_TOKEN` in `web/.env.local`, run `npm run ig:token`. **Verified:** a production run authenticated to Instagram and the sheet and wrote the first `IG Weekly` row (12 followers). Instagram also accepted unpublished Reels containers for two rendered reels (processing finished in about 33 seconds; `media_publish` was never called). **Verified 2026-09-16 to 2026-09-18: batch 1's four reels were all published on schedule** by the cron (confirmed from `IG Content` on 2026-09-19). Batch 1 ended on the 18th and nothing was approved after it, so nothing has gone out since; **on 2026-09-20 the cadence was retired rather than continued**, and `/admin` no longer asks for a batch (see the next bullet). **Instagram DMs stay manual** (the API cannot start a conversation, and reading DMs from strangers needs Meta business verification, which needs a company); every gym that asks for the video on Instagram is logged by Davide in the `Inbound DMs` tab and counts as an engaged lead. The Sunday check-up reads all three new tabs. **Batch 1 was approved on 2026-09-13 as four reels** (001 and the demo 002 on 2026-09-16, 003 on 2026-09-17, 004 on 2026-09-18), after Davide rejected two earlier versions the same day: three branded carousel samples, then POV reels with a casdey logo, gold highlight boxes and the /see film's synthesised music, as "very promotional". He approved the third version saying there is still a lot to improve. The rules that came out of it (give posts are plain Instagram-style text over footage with people, with no logo and no pitch; branding only on the weekly demo reel, which is the ask post; real licensed music from Pixabay, never a track marked "Content ID Registered", never the synthesised music) and the competitor research behind them are in `content-plan.md`, "Formats".
- **Instagram content RETIRED 2026-09-20 after 4 posts, and with the DM pause that takes casdey off Instagram entirely (Davide's call).** What remains is **two pinned posts**, whose only reader is a gym owner who got a cold email and is checking the profile before replying (*$100M Leads* pg 114): `002`, the demo reel, already published, and `005`, a single image saying plainly what casdey does (`content/instagram/pinned.json`). A third, an offer post stating the prices and the guarantee on the dark plane, was made and rejected the same day. Cold email is the single channel again. **The diagnosis is the part worth keeping, because it was not a tooling failure.** Davide offered three candidates: no footage, no access to Instagram's sound library, and Claude being unable to watch video. All three are real, and two things outrank them. First, the format was copied correctly but **the line could not be**: what makes Glofox's POV reel work is a sentence built from details only someone who has watched gym staff would have, and casdey's version was a description of a situation with no sting. Second, **casdey has nothing true to say yet** — no gym has ever imported a member list, so every post is necessarily generic advice, which is the same Perceived Likelihood zero the 2026-09-10 diagnosis named. Of Davide's three, the sound library is a real and already-documented **distribution ceiling** (business accounts get only Meta's Sound Collection, the API cannot attach Instagram audio, a downloaded viral track is muted by audio matching), and the footage point reframes usefully: the problem is not missing clips, it is that stock footage can only ever serve a generic line, which is why better or AI-generated footage (Higgsfield was raised and parked) fixes neither of the top two. Mechanically: **the whole pipeline is left working and untouched**, so pinned posts can still be made and a cadence could restart without rebuilding anything; `/api/cron/instagram` still runs daily but is a no-op with nothing approved, and still writes `IG Weekly` on Saturdays; `/admin` stops asking for batches behind **`IG_CADENCE_RETIRED`** in `src/lib/hq-signals.ts`, the same shape as `IG_DMS_PAUSED`. Logged in the Hormozi ledger, which records this as Core Four discipline restored and reverses the 2026-09-13 partial divergence. **Two renderer faults surfaced making the pinned posts**, both previously unreachable: a `<mark>` is `nowrap`, so a long highlighted phrase runs off the right edge rather than wrapping, and the overflow check only measured height (it now checks the right edge and names the element); and on the inverted plane the eyebrow kept Struck Gold at **3.6:1 on `--deep`**, now remapped to Leaf at 8.5:1, the same swap `.on-deep` makes in the app. Davide's own framing for later, when there is money: go all in on cold outreach until there are paying gyms, then do paid ads properly rather than content badly.
- **Found while building it:** the pricing page's Free column says "Start free" but led straight into the €1 step with no way past it. `/app/onboarding/trial` now has a "Continue on the Free plan" link (Davide chose this over removing the Free button). Also: `web/.env.local` now sets `CASDEY_PAID_TRIAL=true` to match production; without it local pages showed the old free-week copy.

## Offer evolution (beta → V1 → V2)

**Superseded in part 2026-09-03 by the 3-tier move (Track F). READ THIS FIRST.** The phase structure below (beta → V1 → V2, free week → Free plan → early-adopter discount if you upgrade) still holds. What changed: the single "Premium" tier became **Free / Standard / Pro** (see the Price bullet above and `web/SAAS_V1_PLAN.md` §F0), and the lifetime early-adopter discount is now a flat **20% off either paid tier**, not £50/€59. So "upgrade to Premium" below now means "upgrade to Standard or Pro", and "£50/€59" is now "20%". The V2 end-state is Free / Standard / Pro, not "Free vs Premium".

Dictated by Davide on 2026-08-13, to be reflected in both the cold-outreach templates and the waitlist/landing copy. Three phases, same underlying price (see above), different wrapper:

1. **Beta / pre-launch (current phase, while the SaaS is being built).** Cold outreach and the waitlist offer: a free week of Premium once the software ships, after which the account drops to a **Free plan** (deliberately limited, not yet scoped/built). If a beta contact then chooses to upgrade to Premium, they get a **lifetime discount of £50/mo GBP or €59/mo EUR off the standard Premium price**, for as long as they stay subscribed. This lifetime discount is exclusive to people who joined during the waitlist/beta and V1 windows, not a standing offer. The outreach message in this phase must say explicitly that this isn't a sales pitch, casdey genuinely wants their feedback on the product.
2. **V1 launch.** Same offer (free week → Free plan → lifetime discount if they upgrade), just reworded: outreach and copy switch from "coming soon" framing to "it's live now." Trigger to start building V2: the first paying clients land.
3. **V2 launch.** The beta/lifetime-discount offer retires for new signups — existing early adopters who already locked in the lifetime discount keep it. Go-forward, the product is just Free vs Premium, no beta framing.

The Free plan referenced above does not exist in the product yet; scoping and building it is separate work from the current SaaS build (see `web/SAAS_HANDOFF.md`).

## Per-gym sending identity (decided 2026-09-04, Davide: NON-NEGOTIABLE)

**The product promise is that casdey contacts lapsed members _as the gym_. Anything that shows casdey's own identity to a member is a bug, not a detail.** Davide made this call on 2026-09-04 after seeing what a WhatsApp message would actually look like, and said that if it could not be fixed he would question the whole model. It is fixed, and it is now Track G in `web/SAAS_V1_PLAN.md`.

What was wrong, and why the two channels were not equally broken:
- **Email** already sent as `Iron Works Gym <no-reply@mail.casdey.com>`, so an inbox list looked right and replies already went to the gym. casdey's address only showed if a member expanded the header. A polish problem.
- **WhatsApp** sent from ONE shared casdey number. On WhatsApp the business display name belongs to the **number, not the message**, so every gym's members saw a business called "casdey". **No wording change can reach this**, which is what made it a V1 blocker rather than a nice-to-have.

The fix, both channels: the gym brings its own identity and casdey sends *through* it. Migration `0017_per_gym_sending_identity.sql` (applied to the live DB 2026-09-04, verified in a transaction).
- **Email** — the gym verifies its own domain via Resend's Domains API (`src/lib/email/domains.ts`), adds the DNS records casdey shows it, and mail then leaves from `hello@theirgym.com`. Settings → **Sending**. Crucially, **Resend verifies the domain, not casdey**: there is no business-entity requirement anywhere in this flow, which is why the email half shipped immediately. Only a `verified` domain is ever used; `pending` behaves exactly like unset, because unauthenticated mail on the gym's own domain is punished harder than mail from casdey's and burns a reputation that belongs to the customer.
- **WhatsApp** — `gyms.whatsapp_from` replaces the shared `TWILIO_WHATSAPP_FROM`. The gym connects its **own** WhatsApp Business Account and number. `whatsappProvider(from)` cannot be built without naming a sender. This also fixed a real routing bug: inbound replies used to attach to whichever gym had messaged a number most recently, and per-gym numbers let Twilio's inbound `To` identify the gym exactly.

**The honest limit:** WhatsApp onboarding is **manual** per gym (the gym does Meta's "Continue with Facebook" with their own account and number; their opener template is approved under their own WABA, so templates do not carry across gyms). The self-serve version is Meta **Embedded Signup** via Twilio's **Tech Provider programme** — free, but it needs 3-4 weeks of Meta/Twilio approval **and Meta Business Verification of casdey, which requires a registered legal entity**. Davide's position on 2026-09-04: **no Partita IVA until the business earns it.** So self-serve WhatsApp onboarding is V2 (Track G3). Manual is correct while there are zero customers.

## Data sensitivity — critical
This product will handle real dental patient data — health-adjacent personal data. GDPR compliance is a hard requirement for the UK/Ireland market, not optional. Treat any data handling, storage, or processing decisions with this in mind by default, and flag anything that could create compliance risk.

## The 5-stage plan
1. Outreach to test interest — no fixed message count; send as many as possible while staying out of spam. (This originally read "me + my business partner Abhi". **Corrected 2026-09-07: Davide is casdey's sole founder.** Abhi sent roughly five emails at the very start and has had no involvement since; treat him as not part of the business. Nothing should be planned around a second person, and there is no second pair of hands for anything.) Flexible stage: if an idea gets no traction, we change it and repeat. Trigger to move on: 1-3 real paid commitments.
2. Build the SaaS and land first customers, with an offer + guarantee (e.g. money-back if they don't recover the cost). Outreach again, both to earlier interested contacts and new businesses.
3. Collect feedback from first customers, tweak Beta into a 1.0.
4. With 1.0, get testimonials/reviews from first customers (possibly in exchange for something), then push much more outreach. Paid ads stay on hold.
5. Once profit is sufficient (threshold still TBD), start paid ads for momentum.

We are running Stage 1 (outreach) and Stage 2 (build) in parallel: Davide made the explicit call on 2026-08-13 to start building the landing page, waitlist, and SaaS ahead of the plan's original 1-3-paid-commitments trigger, rather than waiting on outreach to convert first. Recorded here as a deliberate decision, not a plan change.

## Stage 1 progress — outreach underway

**2026-09-02 correction (READ THIS FIRST, it overrides the dental bullets below).** Outreach is GYM/fitness now, not dental, and it is live and running. Two truths the stale text below gets wrong:
- The on-`main` skill `.claude/skills/cold-outreach/SKILL.md` and the "Casdey-UK-Dental-Leads" sheet are DENTAL and STALE, they are NOT what runs. The live gym system lives on the `gym-outreach-automation` branch (skill `.claude/skills/gym-outreach/SKILL.md`) and uses the "Casdey-Gym-Leads" sheet (id `1WOAIA1gvK6S1kWe_Vf4-d4XmjhnDLQZLtyU_ezvOu3w`).
- Outreach is TWO Claude Routines (managed cloud-side, edit via the `schedule` skill / RemoteTrigger, not in the repo):
  1. **"casdey gym outreach"** (`trig_018wp58QLBbeuBPbMA6Fy1sU`), fully automated email, daily 02:00 UTC (was 05:05 until about 2026-09-14), **100 first-touch emails/day plus every follow-up due** since 2026-09-07 (was 75, and 50 before 2026-09-02; see the Marketing plan section), sends via Resend from davide@casdey.com. The dental routine "casdey cold outreach — daily" is DISABLED.
- **Both routines, and the Sunday check-up, run on Davide's own Claude plan, so they stop when his weekly usage runs out.** That happened for the first time on **2026-09-17 and 2026-09-18**: no email and no Instagram run on either day (confirmed from the routines' run history). They resumed on 2026-09-19, the Saturday after the weekly reset. The gap counts against that week's 1% engaged-lead goal, so read that week's numbers with it in mind. Protecting the routines' share of the week's usage is now part of planning heavy build weeks.
  2. **"casdey gym IG outreach"** (`trig_01La223qWjwxoumG4z8gssL8`), **DISABLED 2026-09-20 (`enabled: false`), see the Instagram DM pause under the Marketing plan.** What it did while it ran, and what it would do again if re-enabled: semi-automated Instagram, daily 02:00 UTC since 2026-09-13 (was 06:00). Sources gym IG leads and **drafts up to 20 first DMs/day** (10 until 2026-09-13) into the sheet's "IG Outreach" tab; Davide sends them BY HAND (Instagram has no cold-DM API and botting gets accounts banned). casdey's IG account is brand new as of 2026-09-02, warm it before sending at volume. Multi-channel aware: a gym emailed already gets a DM that references the email; anyone who opted out or replied on email is excluded from IG (tracked in the "Emailed Before?" column).

**Performance snapshot (2026-09-02, live sheet):** 505 emails Aug 23 to Sep 2, 301 gyms contacted, 6 genuine replies + 1 opt-out = ~2.0% engaged (target 3%). Opens are unmeasurable (plaintext, no pixel). 5 of 6 replies were CrossFit/community boxes, a strong segment signal.

**Email optimizations applied 2026-09-02 toward 3% (in the routine prompt):**
- **Owner-first:** first-touch sends to named-owner / non-generic emails before generic info@ inboxes (59% were hitting reception); generic inboxes get a "please pass to the owner" line.
- **Segment reweight:** sourcing ~40% CrossFit/boutique/community boxes, ~60% general gyms.
- **Lighter A/B cold CTA:** the "what would you pay / which features" ask moved OUT of the cold email (held for Davide's manual reply). Two rotating first-touch variants, **A** = permission ("mind if I show you?"), **B** = free-to-try ("set it up free on your lapsed list, want in?"). No price/guarantee/link in the cold email; the guarantee is a reply-side move. Tracked per send in a new Send Log "Variant" column.
- **Two follow-ups** (was one), via new Send Log columns "Follow-up 2 Due Date" / "Follow-up 2 Sent (Y/N)"; a one-time backfill gives the ~104 already-once-followed-up leads a final FU2 (inbox-checked first, skips replies/opt-outs/manual follow-ups).
- Daily report now includes a **per-variant reply-rate block**.

**Onboarding of engaged leads:** the plan for turning replies into live V1 users is in `web/SAAS_ONBOARDING.md` (self-serve is the goal, hybrid the fallback).

The dental bullets below are kept for history and are superseded by the above.

- A lead list of UK + EU dental practices exists in a Google Sheet ("Casdey-UK-Dental-Leads", owned by info@casdey.com), sourced from practice websites and directories (not guessed patterns), with owner/principal names researched for independents where confidently sourced.
- Outreach is now a fully automated daily system (a Claude Routine, see `.claude/skills/cold-outreach/SKILL.md`), not a manual batch send: it sources leads, drafts, sends via Zoho, detects replies, and logs everything to the sheet's `Send Log` tab, unattended, once per day, capped at ~100 sends/run.
- As of the last recorded batch (2026-08-13): 87 total sends across three batches, covering UK, Ireland, Netherlands, Germany, France, Belgium, Portugal and Spain. Full batch-by-batch history is in `SKILL.md`, not duplicated here.
- **2026-08-15: zero-reply diagnosis, volume paused, CTA changed.** Live `Send Log` tab checked directly: 216 sends since 07/31, 0 genuine replies, only 2 hard bounces, and Davide confirmed test sends land in the main inbox, not spam, so this reads as a message/offer problem, not a broken pipe or a tracking gap. After reviewing a $0-customer-acquisition playbook (Nate Herk-style deck, not saved to this repo), Davide's call: stop scaling raw send volume and prove the message works on a small hand-picked batch first. Two changes landed in `.claude/skills/cold-outreach/SKILL.md`: the daily/per-run target dropped from ~100/day to ~15-20/day (hand-picked, independents and named-owner leads first), and the cold email's CTA changed from a vague "reply to learn more" to a concrete offer, free calculation of roughly how many of a practice's patients haven't rebooked and what that's likely costing them, if they reply, never an asserted number (stays inside the existing "never invent a fact" rule). Revisit the volume target once this batch shows real engagement.
- Lead tracking uses these statuses: Not contacted / Contacted / Replied / Interested / Committed / Dead. Only "Committed" (a written "I'll pay £X") counts toward the 1-3 commitments the original plan called for, though building has now started regardless (see above).
- Follow-up sequence per lead: send message → if no reply in 4-5 days → ONE follow-up → then stop. As of 2026-08-13 (Davide's explicit go-ahead), follow-up sending is automatic too, same as reply detection.
- Independent practices are prioritized over small chains — they decide faster.

## Stage 2 progress — building
- Landing page (`/`) and waitlist (`/waitlist`) were built and were live in production at `casdey.com`; both are now mid-pivot, see "Niche pivot" above for the current routing and copy state. Brand v4 ("Chalk & Struck Gold", light ground + gold primary + the first logo mark) applied 2026-09-04, replacing v3's dark "Iron & Brass" system, which had itself replaced the v2 light/teal system this bullet originally described. Copy still follows the free-first-week-only rule (no price shown, per the outreach copy's own no-price-in-first-touch logic).
- Waitlist storage is a real Supabase table, `waitlist_signups`, RLS on with only the server-side service role able to read/write. Verified end-to-end on 2026-08-13 with a real test signup (row written, both notification emails sent), and again after the production deploy. **Region correction (found 2026-08-15):** the project's actual region is **eu-west-1 (Ireland)**, not eu-central-1 (Frankfurt) as earlier assumed here and in some marketing copy — still EU, so the residency promise holds, but any "stored in Frankfurt" wording is inaccurate and should say Ireland. **As of 2026-09-09 the table holds 0 rows:** the 3 rows it had were all test signups from Davide's own addresses, removed via a direct `pg` connection over `SUPABASE_DB_URL` — the `@supabase/supabase-js` service-role client cannot DELETE here (its grants are SELECT/INSERT only), so any real removal needs raw DB access; no `DELETE` grant was added.
- The SaaS product (`/app`) has a first working build: accounts (email/password + Google, both fully wired end-to-end as of 2026-08-15 — see below), a **trial → Free → Standard/Pro** billing model (3-tier since 2026-09-03, Track F; see `web/src/lib/plan.ts` and `web/SAAS_V1_PLAN.md` §F0), CSV member import (Mindbody stubbed pending real credentials), lapsed-member detection, a manual price list per gym for revenue estimation, a forward *money-on-the-table* figure on the Overview (the median monthly recurring membership price times the lapsed count, `web/src/lib/opportunity.ts`, added 2026-09-09 in commit `34f0615`; shown on every tier, never feeds the guarantee), and re-engagement campaigns over **email and WhatsApp** (email through Resend with per-gym reply-to; WhatsApp via Twilio + a Claude Haiku reply loop, revived 2026-09-03 as Track E1, Pro-plan-only). Full detail, verified checks, and the manual steps still needed are in `web/SAAS_HANDOFF.md` and `web/SAAS_ROADMAP.md`. It exists in the same deployed app as the marketing pages. **As of 2026-08-17 the env vars `/app`'s email+billing paths need are set in Vercel Production and production has been redeployed, so `/app` now serves at casdey.com** — `NEXT_PUBLIC_SUPABASE_*`, the full `STRIPE_*` set including live keys, `RESEND_API_KEY`, `CASDEY_SENDING_ADDRESS`, and `CRON_SECRET`. Email/password auth is confirmed working in production (a malformed `NEXT_PUBLIC_SUPABASE_URL` in Vercel — it carried a `/rest/v1` suffix, the documented gotcha — was found and corrected 2026-08-17; the Supabase clients now reject that suffix outright), and Supabase Auth's Site URL, still `localhost:3000` with an empty redirect allowlist, was fixed to `https://casdey.com` + a `casdey.com/**` allowlist so OAuth and email links resolve in prod. **What gated calling `/app` fully live is now cleared:** the Google OAuth consent screen was **published to production on 2026-09-03 (plan item B1 done)** — with **no verification review required**, because A6's narrowed scopes (`calendar.app.created` + `calendar.freebusy`) are both non-sensitive and no logo was uploaded (Google only forces verification for sensitive/restricted scopes, a logo, or >10 domains). So real gym owners can now sign in with Google, connected-calendar refresh tokens no longer die after 7 days, and there is no "unverified app" warning. **Correction 2026-09-03: the Calendar-sync channel is now live in production** — `GOOGLE_CALENDAR_CLIENT_ID`, `GOOGLE_CALENDAR_CLIENT_SECRET` and `CALENDAR_TOKEN_KEY` are now set in Vercel Production and deployed (plan item B2 done), so Settings → Booking shows "Connected as info@casdey.com" and booking can read/write Google Calendar in prod (currently the info@casdey.com connection; the consent screen has since been published, so real gym owners can connect too). **Standing rule found while doing this:** because local dev and production point at the *same* Supabase DB, `CALENDAR_TOKEN_KEY` MUST be byte-identical in Vercel and local `web/.env.local` — that key encrypts the calendar tokens at rest, so a mismatched key lets prod show "Connected" yet silently fail to decrypt tokens local wrote (hit and fixed 2026-09-03: a freshly-generated key was replaced with the local value). **Corrected 2026-09-07 (this bullet used to say `TWILIO_*` and `ANTHROPIC_API_KEY` were "not yet in Vercel", which the Vercel bullet below already contradicted):** all three went into Vercel Production and Preview on 2026-09-04 with B8, and the Twilio account is off its trial. The **WhatsApp channel** is still inert in prod, but for one remaining reason only — it needs a **Meta-approved sender and template**, which belong to the gym under its own WABA, not to casdey. Until then it degrades cleanly (settings save, sends error). No longer inert as of 2026-09-04: the **3-tier Stripe prices** are live and all 9 vars are set in Vercel (F2), so a new paid upgrade can complete in prod. **And as of 2026-09-07 the Anthropic API is funded**, so campaign personalisation is live in prod for the first time — see the Anthropic bullet under Infrastructure.
- **Google sign-in (2026-08-15):** fully wired, both sides. App code (`signInWithOAuth` + the `/auth/callback` code exchange) already existed; completed the external config — created a Google Cloud OAuth client (`casdey web`, under `info@casdey.com`) and enabled it in Supabase → Auth → Providers → Google. Confirmed "Enabled" in the Supabase dashboard. **Consent screen published to production 2026-09-03 (plan item B1 done), with no verification review required.** The route there: the requested scope was narrowed in code from `calendar.events` to `calendar.app.created` (plan item A6) — both `calendar.app.created` and `calendar.freebusy` are **non-sensitive**, and Google only forces verification for sensitive/restricted scopes, an uploaded logo, or >10 authorised domains. casdey has none of those (2 domains, no logo left deliberately empty), so the "Push to production" dialog published immediately. The narrowed scope was pushed to prod first (commit `bb99e10`) so what prod requests matches what was declared on Data Access. Also done 2026-09-03: Google Cloud now hard-requires 2-step verification (enforced May 2025), enabled on both `info@casdey.com` and the personal account; the redirect URI `https://casdey.com/api/calendar/google/callback` is whitelisted on the `casdey web` client; the client secret can no longer be viewed in the new console (lives only in `web/.env.local`, ends `xfy7`, add a new one if lost). Reversible via Audience → "Back to testing" if ever needed.
- **Campaign email via Resend (2026-08-15):** the Zoho-only send path (couldn't set a per-practice reply-to, and tripped Zoho's abuse limits when tested — see `web/SAAS_HANDOFF.md`) is fixed. Created a Resend account, verified `mail.casdey.com` as a sending domain (a **subdomain**, deliberately, so Zoho's own MX/SPF/DKIM on the root `casdey.com` are untouched), added the 3 required DNS records at GoDaddy, and set `RESEND_API_KEY` + `CASDEY_SENDING_ADDRESS` locally. Confirmed locally that campaign email now routes through Resend instead of Zoho. `RESEND_API_KEY` and `CASDEY_SENDING_ADDRESS` are now also set in Vercel Production (2026-08-17) and production has been redeployed; no real campaign send has been end-to-end tested in production yet.
- **Per-member personalisation (`src/lib/personalise.ts`), live in prod 2026-09-07.** Distinct from, and not a reversal of, the dropped AI *drafting* below: the gym still writes its own message, and a Claude Haiku call rewrites that message for each member from the handful of facts casdey actually holds (first name, months away, recorded reason for leaving, the offer, the booking link). Opt-in per campaign via `campaigns.personalise`. Three rules hold it up, all because the text goes out under the **gym's** name: it rewrites rather than invents, the gym's offer is reproduced word for word, and **every failure returns null so the gym's own template goes out instead** — which is why a dry API balance had been degrading silently rather than erroring. Two bugs fixed 2026-09-07 while generating the first real samples: a gym's offer lives on the gym row and outlives the campaign that introduced it, so a plain check-in written months later was silently carrying an old discount the gym never put in the message (the guard that stopped the offer being *reworded* also made omitting it impossible); and the campaign's `language` column, plus `languageEndonym()` whose own comment says it exists "for the model to write in", were never passed to the prompt, so an Italian template with an English offer came back in English. Both now verified against the live rows that exposed them.
- **AI-assisted campaign drafting: built, then deliberately dropped (2026-08-15).** Davide decided against embedding AI drafting for a task this small — it's a standing per-use cost for a feature nobody asked for. The language selector and per-practice price list it would have used are kept; only the "draft with AI" option itself was removed. Not on the roadmap to revisit unless the cost/demand picture changes.
- **WhatsApp channel with an AI reply loop (built 2026-08-15 for dental, removed in the gym pivot, revived gym-native 2026-09-03 as Track E1 — see the "WhatsApp removed entirely / Reversed" bullet under the pivot section for the full current story).** A second messaging channel alongside email: one shared casdey WhatsApp number (via Twilio) for every gym, Claude Haiku holding the back-and-forth once a member replies (capped history, a hard per-conversation reply limit — now claimed atomically — before a human takes over), and a `mark_booking_requested` tool call that hands off to staff on clear booking intent. Now a **Pro-plan feature**. The 5 reply-loop audit bugs from the 2026-08-17 stress test were fixed during the revival. Everything on casdey's side of the Twilio boundary (signature verification, idempotency, conversation state, the AI call, STOP-style opt-out) is verified working; actual outbound sending is still blocked on Twilio's trial-account limits (Content Templates and custom webhook config both gated behind an upgraded account), deliberately deferred until the V1-publish decision. **Real bug found and fixed 2026-08-16:** patient phone numbers were never converted to the E.164 format Twilio requires (a practice's CSV export writes local format, e.g. `07700 900123`), breaking both outbound sends and inbound reply routing for standard UK/EU numbers — added `libphonenumber-js`, normalises to E.164 at CSV import time using the practice's own country. Full detail in `web/SAAS_ROADMAP.md` item #2.
- **Casdey-owned booking loop with Google Calendar sync (built 2026-08-15, Google OAuth wired and live-verified 2026-08-16).** The calendar write-access half of "Guarantee mechanics" above: a patient gets a self-serve booking link (`{{booking_link}}`, threaded through campaign templates, the sender, and self-test) that reads the practice's connected Google Calendar for free/busy, lets them pick an open slot, and books it straight into both casdey and that calendar, no staff step. Practices connect their own Google Calendar from Settings → Booking (OAuth, tokens encrypted at rest); booking still works without one connected, it just cannot see appointments made outside casdey. Cancelling from the patient's own manage link removes the calendar event and reverts their status correctly. **Live-tested end-to-end 2026-08-16** against a real Google account: connected a calendar, booked a real slot, confirmed the event landed on Google Calendar, cancelled it, confirmed it was removed. Full detail in `web/SAAS_HANDOFF.md`. Real PMS-diary write (Dentally/EXACT/R4) stays out of scope, same partner-API wall as the import stub — Google Calendar is the one real diary casdey can reach today.
- **The profit-or-nothing guarantee is now real, not just copy (2026-08-15).** **As of Track F (2026-09-03) it is a Pro-plan feature** — a Standard gym pays but has no guarantee; the claim route and the billing-page card both gate on it. A gym gets exactly one lifetime 30-day guarantee window: its first campaign started on or after its first real (non-trial) paid-tier payment (`gyms.premium_started_at`, name kept from the pre-3-tier design). **Corrected 2026-09-12, found on the live paid-week run:** the webhook read the invoice's first line to decide the tier, which on the €1 invoice is the one-off euro, so it fell back to the gym's recorded Pro tier and started the clock on €1. A gym that launched a campaign during its week and then cancelled would have spent its one guarantee on the Free plan. Now only a subscription line that actually charged something starts it (`paidTierOnInvoice()` in `src/lib/guarantee.ts`), and a campaign launched during the paid week that converted still qualifies, with the 30 days starting the day Pro is paid (`qualifyingCampaignsFrom()`), so following the week's checklist is never penalised. The same run proved the renewal-approval path live: the bank challenged the €231.20, the billing page's approve button cleared it, and the subscription went `active` with sending back on; the charge was then refunded in full. If estimated revenue recovered in that window falls short of what was actually paid, the practice can self-serve a full refund of that window's payments from `/app/settings/billing` — one click, no review, safe specifically because the window can never be re-armed. Locked with Davide before building: threshold is revenue-recovered vs. paid (reusing #7's existing formula), refund scope is everything paid in the window (not one invoice, not open-ended), and the claim is fully self-service. Full detail in `web/SAAS_ROADMAP.md` item #8. **Verified end-to-end 2026-08-16** with a real (test-mode) Stripe checkout and refund, which surfaced and fixed two bugs that had made the refund silently non-functional since it was built: the payment window's lower bound was the campaign's start time rather than the first Premium payment (so that triggering payment was always excluded from "paid"), and the Stripe webhook fetched invoices without `expand: ["payments"]`, leaving no payment/charge id for a refund to target. Both fixed. **Hardened further 2026-08-17** after the stress-test audit (see the audit bullet below): an unset appointment value can no longer auto-qualify a full-window refund (it now routes to a `needs_review` state instead of a one-click claim), and the paid/refunded set is bounded to the guarantee window's own billing period so an extra pre-window month a slow-to-launch practice accrued is no longer over-refunded.
- **FIXED 2026-09-12 (commit `f1ee179`): a refund issued from the Stripe dashboard now reaches casdey.** The webhook handles `charge.refunded` (added to the live endpoint's event list the same night) and writes Stripe's cumulative `amount_refunded` onto the matching `subscription_payments` row, set rather than incremented, so redeliveries, partial refunds and the guarantee claim writing first all converge on the same figure. The two refunds made before the fix (Test Gym's €231.20 and the €79.20 C1 walkthrough payment) were backfilled from live Stripe. What follows is the original finding. **Known gap: a refund issued from the Stripe dashboard is invisible to casdey (found 2026-09-07).** `subscription_payments.refunded_minor` is written only by casdey own guarantee-claim route, and the webhook subscribes to six events, none of them refund events. So a manual refund leaves the row reading `refunded_minor: 0`. Low severity and no money is at risk: if a gym then claimed the guarantee, `stripe.refunds.create()` would error on an already-refunded payment, get caught, and mark the claim failed rather than refunding twice. The cost is a confusing claim state plus a paid-total that overstates what the gym actually paid. Fix, if it ever matters, is subscribing to `charge.refunded` and reconciling the row.
- **Client self-test of the outreach (2026-08-15), email side.** A practice can send a campaign to itself before approving it for real patients: "Send me a test" on the campaign review page sends the exact composed message to the signed-in user's own inbox, real reply-to, real working unsubscribe link, only the subject marked so it is not mistaken for a genuine patient reply. Rides the same send pipeline as a real message via one reusable synthetic per-practice patient (`patients.is_test`), excluded from every real-facing number (dashboard stats, patient list, campaign audiences, CSV export). Full detail in `web/SAAS_ROADMAP.md` item #4. Same production gate as the rest of `/app`. Covers email; WhatsApp self-test shipped the same day alongside the WhatsApp channel itself (see the WhatsApp bullet above), and a patient can now also test the in-product booking step for real, via the self-serve booking link (see the booking-loop bullet above).
- **In-app support widget (2026-08-15).** A help launcher bottom-right of every `/app` page opens a searchable panel of curated answers about using casdey (importing, dormancy, sending, the self-test, plans, the guarantee, revenue, rebookings, unsubscribes, data/GDPR), with a plain `mailto:info@casdey.com` fallback when it cannot answer. **Deliberately not AI-backed**, decided with Davide, same standing-cost reasoning that dropped the AI drafting in #1: a curated FAQ does this job without a per-use LLM cost or the `ANTHROPIC_API_KEY` dependency. Full detail in `web/SAAS_ROADMAP.md` item #5. UI only, no schema and no new env vars, but still lives behind the same production gate as the rest of `/app`.
- **Pre-launch stress-test audit + fixes (2026-08-17).** An 8-agent read-only audit (one specialist per domain: auth, billing/guarantee, GDPR, import, WhatsApp, booking, build/config, marketing) ahead of the V1 go-live. Core engineering held up — no auth bypass, RLS on all tables, `tsc`/`lint`/`test`/`build` all green — with risk concentrated in four seams: production config, silent-failure handling on external APIs, the legal documents, and the guarantee's economics. Seven launch-blocker fixes shipped in commit `f30f61b` (pushed/deployed 2026-08-17), all code-only, no schema migration: import batch-poisoning (a single shared/duplicate email no longer drops a whole 500-row batch — dedup on the conflict key + row-by-row retry that reports the bad record) plus strict-UTF-8/Windows-1252 decoding and surfaced CSV parse errors (accented EU names no longer import as mojibake); the guarantee hardening noted above; a real password-reset flow (`/reset-password` + `/api/auth/reset-password` + a "Forgot password?" link — there was none before); an atomic cron send-claim so overlapping drains can't double-email a patient; `siteUrl()` now throws in prod instead of silently using localhost and the Supabase clients reject a `/rest/v1` URL; and em-dash removal from Google Calendar event titles. **Deferred audit items — status as of 2026-09-03:** Calendar free/busy fail-open (**fixed**, A6), Testing-mode 7-day refresh-token death (**fixed** — consent screen published, B1) and calendar scope narrowed (**done**, A6); the adjacent-slot/buffer double-booking race (**fixed** 2026-09-03, migration `0015`, GiST exclusion constraint); the WhatsApp reply-loop fixes — malformed first AI turn, non-atomic reply-cap race, oldest-not-newest history, English-only STOP, no output moderation — (**all 5 fixed** 2026-09-03 as part of the Track E1 revival); the GDPR DPA/`/privacy` corrections (**done** — controller named, false EEA claim dropped, Google + Twilio + Anthropic disclosed as US sub-processors). **Still open:** the Google-calendar mirror reconciliation job (a background job, post-V1). Historical detail in `web/SAAS_ROADMAP.md`.
- **casdey.com published, and V1 declared complete, 2026-09-07.** The `/` → `/waitlist` redirect that had stood since the 2026-08-19 pivot was deleted from `next.config.ts`, so the product is open to anyone. `/waitlist` keeps its own URL deliberately: the cold outreach has linked to it since August. Two copy faults were caught while reading the page that was about to become the front door, both in the places where being wrong costs most. The homepage promised the profit-or-nothing guarantee with no qualifier when it is **Pro-only**, so a €99 Standard buyer was being told they were covered. And the data section claimed member data lives in UK and EU, when it is one eu-west-1 project in Ireland, contradicting the processing terms a gym signs. **Plan item C1 closed the same day** with a real live-mode checkout (Standard EUR monthly, €79.20 after the 20% coupon, on a real card), then a refund and a cancellation, all verified in Stripe and in the database. That was the last open V1 gate.
- **A password change now has to prove who is asking (2026-09-07).** `/reset-password` accepted any valid session. Supabase returns an ordinary session for a recovery code with nothing marking its origin, so the page could not tell a link clicked seconds ago from a browser already signed in, and since there is no other change-password screen anywhere, that route was the whole surface. `/auth/callback` now sets a short-lived HttpOnly marker when the link it exchanged was a recovery one, and only that marker buys the no-old-password path; any other session must produce the current password, verified before anything is written. Also in the same pass: other sessions are signed out after a change (Supabase leaves them alive, so a stolen session used to survive the very change made to revoke it), a confirm field, an honest expired-link state instead of a form that could only fail, and a forgot-password screen that no longer reveals whether an address has an account. `src/lib/password-recovery.ts` is the one place that decides what counts as a recovery.
- **Member-facing gaps found by walking the surfaces D1 never covered (2026-09-07).** The booking confirmation promised a confirmation email unconditionally, and the manage link, the only way a member can cancel, existed nowhere but that email, which is not sent at all when the member has no address on file. Phone-only members have been importable since the readiness pass, so such a member booked, was told to watch an inbox casdey has no address for, and could never cancel. The confirmation now carries the manage link itself and only claims an email when one was sent. Separately, the data processing terms said Anthropic was used only for the WhatsApp channel and not for email, which stopped being true the moment personalisation went live: first name, months away, the recorded reason for leaving and the gym's offer all reach a US sub-processor for email campaigns. That entry now covers both uses.
- **Davide's own walkthrough (plan item D1), 2026-09-06 to 2026-09-07: 70 findings across six batches, all closed.** The final V1 gate, and the only one that could not be delegated: Davide walked `/app` as a gym owner who had never seen it and reported what confused or broke, verbatim. **`web/D1_WALKTHROUGH.md` is the record** — every finding in his own words, a status board per batch, and Claude's reasoning on the four that were not straight yeses (#31 direct integrations, #35 rich text, #57 the admin UI's language, #70 the Anthropic key). Read it before touching the product surfaces it covers. The walk earned its place: it surfaced two failures that no test caught and that would each have stopped a real gym dead. **The send queue had been broken since before batch 1** — `queueCampaign` upserted on `(campaign_id, member_id)` while the index has been `(campaign_id, member_id, step)` since follow-ups shipped, so every approve and every test send failed with a `42P10`. And **migrations `0029`/`0031` created tables with RLS and no grants**, so saved offers and custom cancellation reasons were invisible and two shipped features looked unbuilt (fixed by `0032`). Also out of it: dark mode moved from an inline script to a cookie, because the script rewrote the document before React hydrated and failed hydration on every page; the reasons became the gym's own rows (`0033`); and the Overview charts took a selectable period. **Davide's standing rule throughout, still in force:** his findings are recorded verbatim and never paraphrased.
- **Founder analytics: PostHog + a `/admin` dashboard (2026-09-08, commits `52c6b3c`, `816cecf`, `43ffaea`, pushed and deployed).** casdey now has its own Shopify-style read on visitors, conversion and revenue. **PostHog EU Cloud, cookieless** ("Cookieless server hash mode" enabled in PostHog project settings, which the client flag needs to actually work), Product Analytics + Web Analytics only — no Session Replay, no heatmaps, no Stripe data-warehouse connection. Five server-side events are captured at their real moment of truth: `waitlist_joined`, `gym_signed_up`, `campaign_approved`, `checkout_started`, `checkout_completed`. Wiring: `src/components/posthog-provider.tsx` (client pageview capture), `src/lib/posthog-server.ts` (posthog-node), `src/lib/posthog-query.ts` (HogQL reads). The four `NEXT_PUBLIC_POSTHOG_*` / `POSTHOG_*` vars are in `web/.env.local` and Vercel Production + Preview; a manual `vercel redeploy` was needed because env vars added after a deploy do not apply retroactively. Verified live: real cookieless pageviews from `www.casdey.com` landing in PostHog via the same HogQL path `/admin` uses. The **`/admin`** page (`src/app/admin/*`, gate in `src/lib/admin.ts` — `davide@casdey.com` / `07davide.longo@gmail.com` / `info@casdey.com` (added 2026-09-19), override with `CASDEY_ADMIN_EMAILS`) reads MRR **live from Stripe** via `stripeClient()` (whose key is the live key in Vercel Production; `.env.local` also holds a distinct `STRIPE_SECRET_KEY_LIVE` for ad-hoc cross-checks), never from `gyms.plan_tier`. Every Supabase/Stripe-derived number lives in `src/lib/admin-stats.ts`, every PostHog read in `src/lib/posthog-query.ts`. Shipping PostHog also forced a `/privacy` rewrite — it had claimed "no analytics that profiles you", now replaced with an honest cookieless-analytics disclosure; `/terms/processing`'s sub-processor list was deliberately left alone, since no PostHog event carries member PII.
  - **Expanded 2026-09-08, later same day, and pushed.** Davide asked for the waitlist gone from the page (casdey.com is published, so the waitlist is not a live channel any more — the `waitlist_signups` table and the `/waitlist` form are untouched) and for "as many analytics as possible, genuinely professional". `/admin` is now sectioned (Traffic / Acquisition funnel / Activation / Revenue / Subscriptions & retention / Product reach / Feedback / Test & dev) with a **flexible period selector**: presets Today / 7d / 30d / 12w / 6m / 1y plus a free "last N days/weeks/months" form (URL `?range=today|7d|30d|12w|6m|1y` or `?count=N&unit=d|w|m`; bare legacy `?range=12` still reads as weeks). The whole model is **days**, not weeks (`src/app/admin/parts.tsx` → `periodFrom`); trend charts bucket by day for windows ≤ 21 days and by week beyond, via the new shared `src/lib/admin-period.ts` (`periodBuckets` / `bucketKeyFor`, imported by both `admin-stats.ts` and `posthog-query.ts`). `LineChart` (shared with the per-gym dashboard) gained an optional `periodLabel` prop, defaulting to "twelve weeks" so the gym dashboard is unchanged. New in `admin-stats.ts`: `gymSignupTrend` (waitlist half dropped), `revenueCollected` (gross/net/refunded cash from `subscription_payments`, window vs previous vs all-time), `subscriptionHealth` (status split + scheduled cancellations + trials-ending-soon + trial→paid conversion), `activationFunnel` (signed up → imported → priced → offer → campaign → paying, per-gym probes — fine at casdey's scale, move to an RPC if the gym count ever leaves the tens), `productReach` (members managed, members returned, revenue recovered, campaigns approved, messages sent, bookings — all window vs previous, all gyms), `feedbackSummary` (the D1 in-app feedback box), `testAndDev` (the aggregate of every `is_internal` gym — see the Test & dev bullet below). New in `posthog-query.ts`: pageviews alongside visitors, `topPages` / `topReferrers` / `topCountries` / `deviceMix` (HogQL over `$current_url` / `$referring_domain` / `$geoip_country_name` / `$device_type`), tier breakdown on `checkoutFunnel`. Empty-now metrics (MRR movement, churn rate, ARPA) render honest "—" / "€0" placeholders and fill in as customers arrive. `tsc` / `eslint` / `vitest` (365) / `next build` all green; verified rendering locally against the live DB across several periods.
- **`/admin` gains outreach and real countries; the check-up absorbs the weekly review (2026-09-13, commits `d10ca96`, `b1f1a52`, `006209a`, pushed).**
  - **Outreach section**, read live from the leads sheet by `src/lib/outreach-stats.ts` (pure maths in `outreach-summary.ts`, tested). It shows **engaged leads** (`Status` Interested or Committed) next to the genuine **reply rate** (`Reply?` reads "Replied"), gyms contacted and emails sent for the period. It needs `GOOGLE_SERVICE_ACCOUNT_JSON` (the outreach service account's key file contents), set in Vercel Production and in `web/.env.local`.
  - **Found while building it:** the `Reply?` column holds words ("Replied", "Unsubscribed"), not Y/N, so `check-up-marketing.mjs` had been counting 0 replies.
  - **Countries were empty because cookieless PostHog drops the IP before its GeoIP step runs.** Pinned down by sending a probe event with an explicit `$ip`, which was located fine. Fix: `src/app/api/geo/route.ts` returns Vercel's `x-vercel-ip-country`, and `posthog-provider.tsx` registers it as `visitor_country` before the first pageview; only the two-letter code travels. `topCountries()` falls back to the browser's `$timezone` (`src/lib/timezone-country.ts`) for older visits. `/privacy` says so.
  - **The check-up now carries the weekly test review** (per-arm numbers, a proposed verdict, `scripts/test-log-update.mjs` for the decision), a casdey HQ button, and a "Recommended signals & this week's actions" section judged against Davide's goals. See the Marketing plan status for 2026-09-13.
- **`gyms.is_internal`, migration `0035_internal_gyms.sql` (2026-09-08).** Davide caught `/admin` counting **4 internal test/dev gyms as paying customers** — 3 test-mode Stripe subs left from feature stress-tests, plus 1 live-mode sub that was Davide's own C1-walkthrough account. Root cause: `gyms` had no test/internal flag and local dev shares the production Supabase project, so stress-test rows sit in the same table real gyms will. `0035` adds `gyms.is_internal` (also added to `src/lib/types.ts`) and flags the 4 known rows; `admin-stats.ts` now excludes `is_internal` everywhere. **The same blind spot survived in `web/scripts/check-up-numbers.mjs` until 2026-09-20**, which summed every live Stripe subscription into one MRR figure: Stripe has no concept of an internal gym, so the "Test Admin" walkthrough account reported about €99 of MRR against 0 paying gyms in every weekly check-up, corrected by hand in the written note each time. The Stripe snapshot now returns one row per subscription and sums after the gym rows say which are internal, reporting `internalMrrMinor` separately and flagging any subscription that matches no gym row. That subscription is scheduled to end 2026-10-07, after which the figure resolves itself. The same migration **backfills one stale `gyms.cancels_at` row** (the "Test Admin" gym, whose billing page wrongly showed "Next payment 7 Oct" for a subscription Stripe had already scheduled to end that day): `gyms.cancels_at` is written by the webhook, but that code (commit `ada416d`) deployed 14 minutes *after* that particular cancellation webhook fired, so the row was never populated. Value set to `2026-10-07T19:56:09Z`, confirmed against live Stripe. No code fix needed — every cancellation since is handled correctly. Applied directly via `pg` over `SUPABASE_DB_URL` and verified.
  - **`0036_flag_test_fixture_gym.sql` (2026-09-08, later same day).** `0035` only flagged the four gyms with a Stripe subscription. It missed **"Bridge Street Gym"** — the `test@casdey.com` fixture from `web/SAAS_HANDOFF.md` "Local testing". No subscription, so never a paying-count problem, but it carried fixture data: 12 members, 3 "returned", a cancelled campaign, and one **£120 booked booking that was showing up as real recovered revenue** in the new Product reach section. `0036` flags it. After it, the **only** gym with `is_internal = false` is "BodyActive Skibbereen" (`bodyactiveskibb@gmail.com`), casdey's first genuine signup (2026-09-08). `/admin` now has a **"Test & dev" section** (`testAndDev()` in `admin-stats.ts`) that reports the aggregate of every `is_internal` gym — members, campaigns, messages, bookings — walled off from every business number, so it stays obvious whether test data is leaking. Applied via `pg` over `SUPABASE_DB_URL` in a verified transaction.
- **V1.1 built, 2026-09-12. `web/SAAS_V1_1_PLAN.md` is the board.** V1 asked whether the product works; V1.1 asks whether anyone activates, and it comes out of the Hormozi diagnosis of 2026-09-10 (the binding constraint is leads, and specifically the collapse between a reply and an activated account). Three of the four tracks are closed:
  - **Track I, the lapse window, is LIVE in production.** `gyms.lapsed_after_months` had defaulted to **12** since `0002_saas.sql`, a dental recall cycle that survived the whole pivot, so a new gym's very first screen flagged only members gone a full year: a near-empty lapsed list at the exact moment casdey has to show an owner money it can recover. The default is now **90 days** (migration `0037`), and the window is an explicit choice rather than an inheritance (`gyms.lapse_rule_set_at`), with Settings counting what each candidate window would catch in the gym's own members so the setting can actually be judged. BodyActive had been sitting on the dental default since it signed up. Two bugs came out of verifying it: the "not set yet" notice named the shipped default rather than the gym's own window, and the settings form came out of every save reading "90 months" because React's post-action form reset moves a controlled `<select>` somewhere state cannot see, so re-syncing state on that event provably cannot fix it. The form now refuses the reset, which also fixes the visit-ceiling checkbox that used to tick itself back on for the same reason.
  - **Track H was REDESIGNED twice on 2026-09-12, and is LIVE in production the same day: the setup fee is gone, and Stripe owns day 7.** What ships is a **paid first week** (`CASDEY_PAID_TRIAL`, renamed from `CASDEY_TRIAL_PENALTY`, set in Vercel Production only): **€1 and a card at signup buy seven days of Pro**, keeping the commitment ask, the activation checklist and the day 2/5/6 nudges, and at day 7 the Pro subscription simply begins unless the gym cancelled. Deleted: the €20-per-unfinished-step fee, the €60 cap, the make-good refund and the `/admin` waiver. **Davide's call, and the reasoning is worth keeping:** he had never seen such a fee in any software, which turned out to be a category mismatch rather than squeamishness. Trial With Penalty comes from gym businesses, where an unused trial consumes a coach's hour and the fee recovers a real loss; a dormant SaaS trial costs casdey nothing, so the fee had no cost basis, and an earlier claim here that €60 against "€72 of Pro consumed" was proportionate used list price rather than cost. Legal research then found that **Ireland, where casdey's only customer is, never adopted Cavendish v Makdessi** (Sheehan v Breccia, still bound by Pat O'Donnell [1998]) and applies Dunlop, under which a deterrent clause is the classic unenforceable penalty, while Germany's §307 BGB polices B2B standard terms and voids an unfair one entirely rather than reducing it. **What the redesign keeps is the property the diagnosis actually cared about: day 7 forces a decision**, which the old free week never did, and which is exactly how BodyActive drifted. Full reasoning in the Hormozi ledger, logged as a deliberate divergence. `trial_penalties` is left in the DB unused, because Davide's own follow-up idea is that a future penalty could cost functionality rather than money. **The marketing copy follows the flag as of 2026-09-12**: every public price claim (hero, header, closing band, offer panel, pricing page and table, footer, login form) branches on `paidTrialEnabled()`, with the repeated strings in `src/lib/offer-copy.ts` and the prose branched in place. `SiteHeader`, `PricingTable` and `AuthForm` are client components, so they take the flag as a required prop from their server parent. The offer panel quotes the **list** price (€289), and a launch-offer bar above the header (`src/components/announcement-bar.tsx`, gated on `earlyAdopterProgramActive()`) carries the 20% discount, on Davide's correction. **Second redesign, same evening (commit `bc1997e`): Stripe now owns the day-7 charge.** Signup is one Checkout session in subscription mode: the Pro price, a one-off €1 line and a 7-day Stripe trial. The card is authenticated on-session, and Stripe bills day 7 itself, so casdey's day-7 job no longer creates or charges anything. This came out of the live run: taking the €1 alone and creating the subscription a week later looks to an issuer like a fresh unattended charge for 231 times the approved amount, and the first real European card was challenged. `cancelTrial()` now cancels in Stripe before writing casdey's own flag. The authentication email comes from the `invoice.payment_action_required` webhook. `trial_converted_at` is stamped by the webhook when the subscription goes `active`. The launch coupon rides on the Checkout session as `casdey_early_20pct_plans`, restricted to the two paid products, so the page shows €231.20 rather than €289 without discounting the euro (commit `a918841`, caught by Davide on the live page). A challenge at the first renewal is expected rather than exceptional, because Stripe's recurring exemption wants a fixed amount and casdey's series is €1 followed by a few hundred. So the billing page shows an "Approve €X payment" button, reading the open invoice live via `src/lib/payment-action.ts` (commit `8e0e89c`), and Davide chose to keep the euro with that trade on the table. **Live run, 2026-09-12:** a real Revolut card refused the off-session exemption on both architectures. €2 was taken in total (two €1 signups), the test subscription was cancelled and its invoice voided, and every gym from the run is `is_internal`. **The recovery path was then proven live too, later the same night:** Test Gym bought the week again, the trial was ended early through the Stripe API, the bank challenged the €231.20, the billing page's "Approve €231.20 payment" button cleared it, and the subscription went `active` with `trial_converted_at` stamped and sending back on. Every webhook arrived. The €231.20 was then refunded in full and the subscription cancelled; the whole test cost about €5.60 in Stripe fees (1.5% + €0.25 processing, 0.7% Stripe Billing, fees not returned on refund). **To rerun the paid week on an existing internal test gym**, no new email account is needed: clear `trial_card_setup_at`, `trial_payment_method_id`, `trial_commitment_at`, `trial_ends_at`, `trial_closed_at`, `trial_converted_at`, `trial_last_nudge_day`, `stripe_subscription_id`, `plan_tier` and `current_period_end`, set `subscription_status` to `none`, and keep `stripe_customer_id`; `/app/onboarding/trial` then opens again. After ending a Stripe trial early, also close the week in the DB (`trial_ends_at` and `trial_closed_at` to now), because nudges key off `trial_ends_at` and do not notice Stripe ended it. **V1.1 software was declared complete on 2026-09-12**, on Davide's scoping call that Track K is operational work outside his control rather than part of the build.
  - *(Superseded 2026-09-12, kept because the plan Track H section still describes it.)* **Track H, Trial With Penalty, is BUILT AND DEPLOYED BUT DORMANT.** €1 and a card at signup, an affirmative commitment, three activation steps (import, prices, first approved campaign), and at day 7 either conversion to Pro, release with nothing owed, or **€20 per unfinished step capped at €60** before dropping to Free. Nudges on days 2/5/6, an automatic refund if a step is finished within 7 days of being billed, and a waiver in `/admin`. Migration `0038`. Verified end to end in Stripe **test** mode. It is gated on **`CASDEY_TRIAL_PENALTY`, which is not set in Vercel**, so nothing about signup has changed in production. **The flag was Claude's own addition, not part of the agreed plan**, which assumed the mechanism would simply ship and run; the reason was that it takes money off a real card and the live Stripe path had never been exercised. Turning it on is one Vercel variable plus a redeploy, and the two things worth doing first are in the plan's "Track H, what is still owed".
  - **Track J, the reply-side guarantee**, is on branch `gym-outreach-automation` (commit `0694b35`). The plan called for porting the Standard-versus-Pro framing into the outreach skill, but nearly every figure in that section was stale (it still promised the free week "once the software ships", quoted £250/€290 and the £50/€59 discount, and called the paid tier "Premium"), and it described a guarantee casdey never built: "100% refund plus free software until the condition is met". Rewritten against what the product actually does.
  - **Track K, turning gym #1 into a case study, is open and is Davide's.** The plan calls it the highest-leverage single item in the diagnosis, because Perceived Likelihood is casdey's weakest Value Equation term and currently a structural zero: no case study, no testimonial, no number from a real gym.
- **The Track H 3-D Secure bug, found 2026-09-12 and fixed the same day (commit `dfd4049`).** A day-7 conversion whose off-session charge needs 3-D Secure leaves the Stripe subscription `incomplete`, which `effectivePlan()` read as **Free**, while the job stamped `trial_converted_at` and reported success. So a gym that did everything right landed on the Free plan with an unpaid subscription, casdey's own records saying it converted, and nothing telling it to authenticate. Stripe's test cards never trigger 3-D Secure, which is exactly why test-mode verification could not surface it. The fix: `conversionResultFor()` in `src/lib/trial.ts` is now the one place that decides whether a created subscription is a conversion, something needing authentication, or a refusal; `convert()` stamps `trial_converted_at` only on a real conversion and emails the owner the hosted invoice link otherwise; and `effectivePlan()` keeps an `incomplete` subscription on its paid tier instead of dropping it to Free, with sending still blocked because `capabilities()` clears that gate only for `active`. **The general lesson, worth more than the bug:** a Stripe call returning an object is not the same as money having moved, and the test cards cannot tell you the difference. **Overtaken the same evening:** the live run hit a real challenge, and day 7 was handed to Stripe (see the Track H bullet above), so `convert()` no longer charges and the authentication email moved to the webhook. The `effectivePlan()` handling of `incomplete` still stands. One thing left at the time and **fixed 2026-09-12 (commit `f1ee179`)**: `/admin` bucketed an `incomplete` gym under "free" in its status counts. It now sits with `past_due` under "Awaiting payment" and counts as subscribed and paying.
- **BodyActive Skibbereen: casdey's only real signup, and there is an open written commitment to it.** The contact is **Jonathan Davis ("JD"), owner**, at `bodyactiveskibb@gmail.com`; he runs **LegitFit**, so CSV import is the only path for him (see `SAAS_V1_PLAN.md` E2). He replied 2026-09-12 saying he is extremely busy and will look properly next week. Davide's follow-up to him is set for Tuesday 2026-09-15 or Wednesday 2026-09-16, deliberately not the Monday. **Update 2026-09-18: JD asked for a call to go through the platform, then did not show up.** Davide replied by email suggesting he skip the call and do the setup himself first, since the Overview page lists every step, attaching a PDF guide Davide wrote on exporting the member CSV from LegitFit, and asking him to reach out once he has questions. No reply yet. Two product fixes came out of Davide's own run-through before that call: saving a new service beside existing ones had always failed, and password-reset links failed when opened on another device (both fixed and live 2026-09-18). **A local verification run of the new day-7 job then emailed him in error**, telling him he had agreed to a £20-a-step setup fee he had never agreed to: he signed up weeks before Track H existed and has no card on file, so he can never be charged one. The bug was real rather than a test accident (nudges did not check for a card, so every pre-Track-H gym mid-trial would have got the same false email once the flag went on) and is fixed, with a regression test naming the incident. **Davide then emailed JD** confirming it was a mistake, that his original terms stand (no card, no fee, no deadline, free week then the Free plan, 20% off for life), and promising that **whenever he is ready a fresh full week goes on his account starting that day**. That promise is manual: it is one update to `trial_ends_at` and nothing does it automatically. His original week was due to expire **2026-09-15**; on 2026-09-12 it was pushed out to **2026-09-26** (direct PATCH over the Supabase REST API, one row, verified) so that he cannot walk into a degraded account before he has even looked. **The second half of the promise is still owed and still manual: when he actually replies, reset `trial_ends_at` to a full 7 days from that day.** The extension is a holding measure, not the fresh week itself. Note `trial_card_setup_at` is null on his row, which is the thing that guarantees he can never be charged a setup fee whatever happens to `CASDEY_PAID_TRIAL`: `trialOutcome()` releases a gym with no card, and `nudgeDue()` skips it.
- **Two fixes found by Davide's own run-through before JD's call (2026-09-18, live).** Saving a service failed whenever the list held a saved service and a new one together: a mixed batch is sent with the union of its columns, so the new row's missing id reached Postgres as an explicit null (fixed with `defaultToNull: false` in `settings/services/actions.ts`). And a password-reset link only worked in the browser that asked for it, because the PKCE exchange needs a cookie that browser holds: opened on a phone, it failed as "expired". The reset email now links to a new route, `/auth/confirm`, which verifies the email's token hash on the server, and the Supabase **Reset password** template points there. `/auth/callback` still serves Google sign-in and signup confirmation.
- **The US market, product side (2026-09-19, live). `IMPROVEMENTS.md` "#3 US market, working notes" is the detailed record.** A US gym could not sign up before this: no US country, no USD, and every path reading a Stripe currency assumed "pounds, otherwise euros", so the first dollar payment would have been recorded as euros, while three database checks would have refused it outright after the card was charged (migration `0040`). Now: `src/lib/countries.ts` has the US with seven timezones, `currencyFor`, `currencyFromStripe` and `dateOrderFor`; CSV import defaults to month first for US gyms and reads the order from the file when a date settles it (`detectDateOrder`); public pages show the visitor's own currency from Vercel's `x-vercel-ip-country` (`src/lib/visitor.ts`); `/admin` counts USD. **The outreach half is BUILT and deliberately OFF, 2026-09-20; it is gated on the postal address, which is bought but not yet usable.** Details in `.claude/skills/gym-outreach/SKILL.md` ("The US switch"), on branch `gym-outreach-automation`. The shape: the routine prompt gets a **US MODE** block whose one switch line reads `US SENDING: OFF`, and flipping it to ON sends all 100 first-touch emails a day to US gyms, keeps every European follow-up running so no sequence is abandoned, schedules each send for about 9am in the gym's own timezone (`scripts/resend-send.js` now takes a `scheduledAt` and passes Resend's `scheduled_at`; European sends pass nothing and go immediately), and puts a CAN-SPAM footer with the postal address and an opt-out on every US email, enforced by two hard pre-send gates. Two columns were added to the `Leads` tab for it: `Country` (V) and `Send Zone` (W, the IANA zone); a blank `Country` means Europe. **The block is not in the live routine prompt yet, on purpose**, since the API rewrites the prompt wholesale and retyping 26,000 characters of a live sending prompt is the real risk; the exact text to paste sits in SKILL.md, so flipping it on is a copy-paste plus a read-back.
- **The US postal address: mailbox 389 at 1550 Wilson Blvd, Ste 700 PMB 389, Arlington, VA 22209, rented 2026-09-20 from Anytime Mailbox ($4.99/mo Bronze, operator TKO Suites, `1550@tkosuites.com`). NOT USABLE YET.** CAN-SPAM requires a valid physical postal address in every commercial email, B2B included, at up to $53,088 per email, and there is no workaround: hiding it in an image, white text or behind a link is a non-disclosure with the same penalty, and it would trip spam filters as well. The research worth keeping: `16 CFR 316.2(p)` accepts "the sender's current street address" with **no country qualifier**, so Davide's Italian street address would have been legally defensible, but the US-registration requirement does attach to the box options, which means an **Italian PO box would not qualify at all**. He chose the mailbox over publishing a home address to about 3,000 strangers a month, which is irreversible. **What still blocks it: USPS Form 1583**, mandatory US postal regulation before any mail-receiving business may accept mail for someone, needing two IDs and an online notary session. Davide's passport has expired, so the pair is his **carta d'identità elettronica** (photo ID, carries his address, printed in Italian and English) plus his **tessera elettorale** (a voter card, which is on the accepted list by name, and carries the same address); utility bills and bank statements are explicitly rejected, so the documents he lacks were never usable. He emailed TKO Suites on 2026-09-20 to confirm the pair is acceptable before booking the notary. **The $4.99 bills monthly from the day of purchase whether or not the form is ever completed**, so if they say no and no accepted document exists, cancel the subscription rather than let it run.
- **Cold-email deliverability, verified 2026-09-20 and healthy.** Two replica first-touch emails (subject S1, variant V body) were sent through the live outreach path to two Gmail addresses: both landed in the **inbox, Primary tab, not Promotions and not spam**, and Gmail marked one Important. The headers show the full chain passing: **SPF pass** (`send.casdey.com` via Resend/SES eu-west-1), **DKIM pass** on `casdey.com` itself, **DMARC pass** against the live `p=quarantine` policy. The caveat worth remembering before reading too much into it: both addresses belong to Davide, who has corresponded with `davide@casdey.com` before, so this proves the authentication chain rather than how a cold stranger's inbox judges casdey. For continuous evidence, **Google Postmaster Tools** (one DNS TXT record at GoDaddy) would report the real spam rate and domain reputation across all Gmail recipients; worth doing before US volume, not set up yet.
- Two Claude Code sessions worked this in parallel deliberately (2026-08-13): one on marketing pages/deploy, one on the SaaS build. Both stayed additive (new files/routes only, zero edits to each other's files), which is what made same-repo, same-branch parallel work safe — no folder copies or extra branches needed.
- GitHub repo: **`casdey-hq/casdey`**, branch `main`, connected to Vercel. Moved 2026-09-07 from the personal account `07davidelongo-eng` into a new free organisation `casdey-hq` (the name `casdey` itself was already taken on GitHub). Old URLs still redirect, but the canonical remote is `https://github.com/casdey-hq/casdey.git`.

## Outreach copy conventions
- The key opener question: "of the day-to-day admin your practice software handles, what's the one thing it doesn't do well?"
- Guarantee wording: "if it doesn't recover more than it costs, you don't pay."
- Cold emails are sent in plain text — no logo, no image attachments (spam risk).
- `casdey.com` is now live (deployed 2026-08-13, see Infrastructure below). **Done:** the casdey.com/waitlist link and the beta offer from "Offer evolution" above (free week of Premium once the software ships → Free plan → lifetime £50/€59 discount if they upgrade, framed explicitly as feedback-seeking, not a sales pitch) are both folded into the cold-outreach routine's email template, see `.claude/skills/cold-outreach/SKILL.md`. Follow-up sending is now also automated (see Stage 1 above). The next copy flip, from "once the software ships" to "it's live now," is gated on V1 launch and not yet due.
- Supporting docs already created: a 4-email outreach sequence and a one-page Service Agreement (used once a prospect says they're interested, sent for signature).
- **Never use em dashes (—) as punctuation** (as a substitute for commas/parentheses/asides) in any casdey copy — cold emails, follow-ups, this file, anything. Use commas or separate sentences instead. Normal hyphens in compound words (follow-up, list-building, drop-off) are fine and unaffected by this rule.
- Email sign-off format: `Davide @casdey` (single line, no line break between name and company, no "Best,"/"Regards," preamble).

## Infrastructure already in place
- Domain: casdey.com, registered at GoDaddy.
- Email: Zoho Mail (free plan), not Google Workspace — blocked there due to no Partita IVA yet. MX/SPF/DKIM configured and verified.
- Two users: davide@casdey.com (Davide) and abhi@casdey.com. **The abhi@ mailbox is dormant as of 2026-09-07** — Abhi is no longer involved in casdey (see Stage 1), so nothing routes to him and nobody reads it. The seat still exists on the free plan and is worth reclaiming if Zoho's user limit ever binds.
- info@casdey.com is a shared Group (not an alias) — mail delivered to both our inboxes, both can send from it. Zoho treats it as a group, not a mailbox: it has no API/IMAP account of its own, so programmatic access goes through davide@casdey.com's account using send-as.
- Google Workspace signup for casdey.com is still blocked (no P.IVA), but info@casdey.com does have a plain Google Account (likely a byproduct of the earlier interrupted signup attempt) — this is what the `gws` CLI and Google Sheets/Drive access authenticate as. **The `gws` CLI's Google Cloud project `casdey-gws-cli` was published to production on 2026-09-13**, after its login expired (`invalid_grant`) while uploading Instagram posts: in Testing mode Google kills refresh tokens after 7 days. Publishing needed the Branding page filled (home page casdey.com, privacy `/privacy`, authorised domain casdey.com); no verification was required, and the consent screen still says "Google hasn't verified this app", which is harmless for an internal tool. The login was redone afterwards with Drive, Sheets, Docs and Slides only (Gmail, Calendar and Tasks were left unticked). Scripts read its credentials with `gws auth export --unmasked`; without `--unmasked` the export prints shortened placeholders and Google answers `invalid_client`. A separate Google service account (`casdey-routine@casdey-gws-cli.iam.gserviceaccount.com`) exists for unattended Sheets access from the outreach Routine, see `.claude/skills/cold-outreach/SKILL.md`.
- **Stripe: activated for live payments (2026-08-17, confirmed via dashboard, done in a session not otherwise recorded).** Account `acct_1Tz17NDGwemFDmSP` — Account status shows no active tasks and capability "Payments: Active" (plus Payouts/Transfers), business details filled in, and a real EUR bank account (Revolut, ••••8098) attached for payouts. Test-mode prices for all four plan variants were already created (see `web/SAAS_HANDOFF.md`). **Live-mode setup completed the same day:** `stripe-setup.mjs` deliberately refuses to run against a live key (live pricing is meant to be a manual dashboard decision, not a script side effect), so the live `casdey` product, its 4 prices, and the 2 lifetime-discount coupons were created by hand in the Stripe dashboard to match the test-mode spec. A live webhook endpoint ("casdey production", `https://casdey.com/api/stripe/webhook`) was also created, listening to exactly `checkout.session.completed`, `customer.subscription.created`/`updated`/`deleted`, `invoice.paid`, and `invoice.payment_failed`. All resulting `STRIPE_*` env vars (secret key, 4 price ids, 2 coupon ids, webhook signing secret) are now set in Vercel Production — see the Vercel bullet below for what that does and doesn't mean yet. **Superseded 2026-09-03 by Track F (3-tier pricing):** the code no longer reads the old 4 price ids or the 2 fixed-amount coupons. It now needs **8 price ids** (`STRIPE_PRICE_{STANDARD,PRO}_{EUR,GBP}_{MONTH,YEAR}`) plus **one flat-20% coupon** (`STRIPE_COUPON_PERCENT`). `scripts/stripe-setup.mjs` was rewritten to create the test-mode versions (2 products "casdey Standard"/"casdey Pro", 8 prices, 1 coupon); **Done 2026-09-04 (F2).** The live catalogue was created with `npm run setup:stripe -- --live` (the script's blanket live-key refusal became an explicit opt-in flag, since the point was to stop an *accidental* live run, and creating catalogue entries charges nobody): live now holds **casdey Standard** and **casdey Pro**, their 8 prices, and the `casdey_early_20pct` 20%-forever coupon. All 9 vars are set in **Vercel Production and Preview**, and the **6 retired vars were removed** (the 4 old price ids, which nothing reads, and the 2 fixed-amount £50/€59 coupons, which would over-discount Standard if they ever became reachable), leaving exactly 11 `STRIPE_*` vars in Production. Verified with `npm run check:stripe` against the live key. **Still owed (as of 2026-09-04):** the *test-mode* catalogue, because the only Stripe key on the dev machine is the live one, so `web/.env.local` has no test prices and a local checkout would charge a real card. **Update 2026-09-08:** `web/.env.local` now also carries a distinct **`STRIPE_SECRET_KEY_LIVE`** (added so `/admin` can read live MRR without disturbing whatever `STRIPE_SECRET_KEY` points at), and that session's notes referred to `STRIPE_SECRET_KEY` as the *test-mode* key. **Confirmed 2026-09-12: it is.** `STRIPE_SECRET_KEY` in `web/.env.local` begins `sk_test_`, the test-mode catalogue does exist (`STRIPE_PRICE_PRO_{EUR,GBP}_MONTH` and the `casdey_early_20pct` coupon all resolve against it), and a local checkout therefore charges nobody. Verified by driving a real test-mode checkout, subscription and refund end to end. The live webhook endpoint is unchanged, but **its event set gained `invoice.payment_action_required` on 2026-09-12**, for the paid week's authentication email, **and `charge.refunded` later the same night**, so a refund made in the Stripe dashboard reaches casdey. That makes eight events. **USD, 2026-09-19:** four more prices (`STRIPE_PRICE_{STANDARD,PRO}_USD_{MONTH,YEAR}`) were created with `npm run setup:stripe -- --live` (the existing eight were matched and left alone) and in test mode, and set in Vercel Production and Preview, so the catalogue is now **12 prices**. `web/scripts/price-spec.mjs` and `src/lib/pricing.ts` must agree on every amount, which `stripe.test.ts` checks. **Also 2026-09-12:** `STRIPE_COUPON_PERCENT` now points at `casdey_early_20pct_plans`, a 20%-forever coupon restricted to the two paid products. It exists in both modes and is set in Vercel Production and Preview, so it can ride on the paid-week Checkout session without discounting the €1 line. **A revenue bug in that hand-entry seam was found and fixed 2026-09-04 (commit `223b821`):** the webhook resolved a gym's tier by reverse-matching Stripe's price id against those 9 env vars, so a single missing or mistyped `STRIPE_PRICE_STANDARD_*` var resolved to nothing, left `plan_tier` null, and `plan.ts` reads a null tier on an active subscription as **`pro`**, silently handing a €99 Standard gym the WhatsApp channel and the refundable profit-or-nothing guarantee. Checkout now also stamps the chosen tier into the Stripe subscription's metadata and the webhook falls back to it when the price lookup fails; the price id stays authoritative when it does resolve. This limits the blast radius of getting F2's env vars wrong, it does not remove the need to get them right.
- Twilio: account created 2026-08-15 on **info@casdey.com** (established 2026-09-04 by checking the Zoho mailbox; there is no Twilio mail in the personal Gmail). **Sign in with "Continue with Google"** on the info@casdey.com Google Account, not a password: a Google notice the same day, "You shared some Google Account data with Twilio", shows the signup used Google SSO. Account SID starts `ACd3a30e46…` (full value in `web/.env.local`). **Upgraded off the trial 2026-09-04 (B8 done)** — `type: Full`, $20 balance, verified via the Twilio API rather than the dashboard. The earlier "still on the free trial tier" claim here is superseded. What blocks WhatsApp sending now is a **Meta-approved sender and template**, not billing, and that belongs to the gym under its own WABA (see the per-gym sending identity section).
- **Anthropic (the API behind personalisation and the WhatsApp reply loop): account is the `casdey` org on `info@casdey.com`, funded 2026-09-07.** Reached at console.anthropic.com, which redirects to platform.claude.com, and signed into with **"Continue with Google"** on the info@casdey.com Google Account, same as Twilio. The org holds exactly one key, `casdey-whatsapp…` created 2026-08-15, whose value is `ANTHROPIC_API_KEY` in `web/.env.local` and in Vercel Production + Preview. **The trap worth recording:** Davide's personal Google account (`07davide.longo@gmail.com`) has its own separate Anthropic org, "Davide's Individual Org", which holds **zero** API keys. Buying credit there funds nothing casdey uses and leaves the identical "credit balance is too low" error in place, so always confirm the account chip reads **casdey** before spending. Anthropic is strictly prepaid, minimum purchase **US$5**, credits expire one year after purchase; $5 was bought on 2026-09-07 and is ample, since both features run on Claude Haiku with hard output caps (600 tokens for personalisation, 300 for a WhatsApp turn) at roughly two tenths of a cent per message. Auto-reload is off. **Funded 2026-09-07 with the $5 minimum**, which is ample: both features run on Claude Haiku with hard output caps, roughly two tenths of a cent per message. Verify with **`npm run check:anthropic`**, which probes the real API: a set key is not a working one, and a funded org is not necessarily the org that owns the key.
- **Anytime Mailbox (the US postal address for CAN-SPAM): mailbox 389, Arlington VA, $4.99/mo, bought 2026-09-20 on `info@casdey.com`, pending USPS Form 1583.** Full story, including why the address is required and which Italian IDs work, in the US bullet under Stage 2. Scanned all 48 mainland states before choosing: Arlington at $4.99 is the cheapest in the country and also the most credible-looking, being across the river from Washington DC.
- Resend: account created 2026-08-15, sending domain `mail.casdey.com` verified (region eu-west-1/Ireland, matching Supabase). API key is Sending-access-only, not Full access. Set locally and, as of 2026-08-17, also in Vercel Production (`RESEND_API_KEY`, `CASDEY_SENDING_ADDRESS`). Used two ways: casdey's own campaign sends go through `RESEND_API_KEY` directly, and **as of 2026-08-17 Supabase Auth's transactional emails (signup confirmation, password reset) route through Resend via custom SMTP** (`smtp.resend.com:465`, user `resend`, sender `no-reply@mail.casdey.com`, key entered in the Supabase dashboard). This fixed a real blocker: Supabase's free-tier shared SMTP was silently failing to deliver confirmation emails, so no email/password signup could complete. Supabase auto-bumped its auth-email rate limit to 30/hr on enabling custom SMTP (raise before real volume). **A third use was added 2026-09-04 and needs a SECOND key: per-gym sending (Track G1) calls Resend's Domains API, which the Sending-access-only key is not allowed to touch** — every `/domains` call answers `401 restricted_api_key`, so G1 could never have worked in prod or locally, and the gym saw a misleading "try again shortly". Domain management now reads **`RESEND_ADMIN_API_KEY`** (Full access), falling back to `RESEND_API_KEY`; the two are kept separate so the key in the hot send path cannot list or delete other gyms' sending domains. **`npm run check:resend`** probes both keys against the real API and exits non-zero on a scope mismatch — the general lesson being that an API key's *scope* is invisible until something calls what it cannot do, so a credential is not verified by being set. Done the same day: `casdey-domains-admin` (Full access) created, set locally and in Vercel Production + Preview, `check:resend` green. **The bigger finding from driving the UI: casdey is on Resend's Free plan, which caps 3 domains and 100 emails/day.** Two domains are casdey's own, so exactly one gym could have its own sending domain, and the 100/day cap makes casdey unusable for even one real gym (a single win-back campaign exceeds a day's allowance). **Resend Pro ($20/mo) was bought on 2026-09-07, so this is resolved** — the 100/day cap is gone and domains went 3 → 10. Note **10, not unlimited**: two are casdey's own, so about **8 gyms** can have their own sending identity before the paid domain add-on is needed. What follows is the pre-upgrade reasoning, kept because it explains why the daily cap mattered more than the monthly allowance — see `web/SAAS_V1_PLAN.md` G1a for the plan/cost table. **Critically, the live gym cold outreach shares this same Resend account and quota** (measured 2026-09-04: 75 sends/day against the 100/day cap, ~2,250 against 3,000/month), so product sends and outreach compete for one pool and the first gym's campaign would consume the outreach's entire day. Davide's call 2026-09-04: features first, subscribe later, but the trigger is **first gym campaign or outreach above ~90/day**, not "V1 published". Also fixed: a 403 from Resend means either "this key may not manage domains" or "you are at your plan's domain limit", and the first cut mapped both to the key error, telling a gym sending was "not switched on for this deployment" when it was a billing ceiling. **Account ownership (2026-09-07):** Resend is organised as a **team**, and domains, API keys and billing all belong to the team rather than to a login. The team was named after Davide's personal account and is now named **casdey**, with `info@casdey.com` added as a second **Admin** (the role that can 'update payment'; Member cannot) and set as the billing email; the Italian billing address matters because VAT treatment is fixed at the first charge. Adding a member is how to bring a business identity onto a service like this: nothing was migrated, no domain was re-verified, no key was rotated and outreach never paused. Resend's Free plan does **not** gate team members, despite this being undocumented on their pricing page. **DNS finding, same day:** the root `casdey.com` SPF authorises Zoho only (`include:zohomail.eu`) and MX still points at Zoho, while Resend puts its own SPF on `send.casdey.com` and `send.mail.casdey.com` as the envelope-sender domain, with DKIM at `resend._domainkey.<domain>`. So the root domain being verified in Resend does **not** conflict with Zoho, which is worth knowing before anyone 'fixes' it. **DMARC (updated 2026-09-08): reports are in fact arriving.** `_dmarc.casdey.com` still carries `rua=mailto:dmarc_rua@onsecureserver.net` (GoDaddy's default placeholder), but a check of the info@casdey.com mailbox over the Zoho Mail API (reusing `sendMail`'s OAuth flow in `src/lib/zoho-mail.ts`) found real Google DMARC aggregate reports being delivered there — 4 copies of one report covering 2026-09-07 — so GoDaddy's placeholder either forwards to the domain's registered contact or the record was already redirected elsewhere. That report is healthy: effectively all legitimate mail (Resend / AWS SES) passes DKIM+SPF cleanly, with only 2 single-message spoofing attempts (negligible background noise). No action needed; pointing `rua` straight at `info@casdey.com` at GoDaddy is still a worthwhile tidy-up. **Corrected 2026-09-09:** that tidy-up was applied (`rua` → `mailto:info@casdey.com`) and promptly flooded both info@ inboxes with aggregate reports, burying real outreach replies. `rua` was then removed entirely — `_dmarc.casdey.com` is now `v=DMARC1; p=quarantine; adkim=r; aspf=r;`, protection kept, no reporting address — and a Zoho filter "DMARC reports" (order #1, files to a folder and marks read) catches any stragglers. Re-adding a digest address later is a one-line DNS edit.
- **Webhook URLs must use `www.casdey.com`, never the apex `casdey.com` (found the hard way 2026-09-07).** Vercel serves the canonical site from `www`, so the apex answers **308** and redirects. Browsers follow that and nobody notices; machine callers do not. Stripe treats any 3xx as a failed delivery and does not retry to the new location, so the live webhook endpoint, registered on the apex since 2026-08-17, silently failed **every** delivery: a real €79.20 payment completed, Stripe recorded five 308 errors, and the gym stayed `plan_tier: null` / `subscription_status: none` with no signal anything was wrong. Any gym that had paid before this would have been charged and left unentitled. Fixed by repointing the endpoint (same signing secret, no env change), and **`npm run check:stripe` now POSTs an unsigned body to the registered URL and fails on a 3xx**, because everything else in that script reads Stripe configuration rather than asking whether a delivery survives the trip, which is why it passed while nothing worked. The same applies to Twilio inbound and anything else that calls casdey back.
- GitHub: **`casdey-hq/casdey`**, branch `main` (moved from `07davidelongo-eng/casdey` on 2026-09-07). The org `casdey-hq` is free, registered to `info@casdey.com`, and "belongs to a personal account" rather than a business, because casdey has no legal entity yet. **Transferring the repo detaches Vercel's GitHub App**, which is installed per-account: the deploy hook broke immediately and `vercel git connect` failed with "Failed to connect… make sure you have access" until the Vercel app was installed on the `casdey-hq` org (scoped to the `casdey` repo only). After reinstalling, `vercel git connect` succeeded and a deliberately empty commit was pushed to prove a push still reaches production — it built and went Ready in 23s. Verify that way rather than trusting the CLI, which reported "already connected" through the whole 2026-08-19 outage. **Account hardening, same day:** the GitHub account had **no password and no 2FA** and was reachable only through "Sign in with Google" on the personal Gmail, which also owns GoDaddy — so one Gmail was the single credential behind the domain, the deploy pipeline and (via GitHub OAuth) Supabase. A password and authenticator-app 2FA are now set, and `info@casdey.com` is a second verified email, so password resets and security alerts can reach either address. **Recovery codes for GitHub and for the personal Google account are the only remaining way back in if either is lost, and casdey is a one-person business** (see Stage 1), so there is no second human fallback.
- Vercel: set up 2026-08-13. Team/project both named `casdey`, Root Directory `web`, `main` = production. Deployed at `casdey.vercel.app` and at the custom domain `casdey.com` (see below). Production env vars started as the waitlist-safe set only (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, the `ZOHO_*` vars, `WAITLIST_NOTIFY_TO`, `CASDEY_FROM_ADDRESS`, `NEXT_PUBLIC_SITE_URL`); **as of 2026-08-17, Davide added the full remaining set needed to go live** — `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, the complete `STRIPE_*` set (live secret key, 4 prices, 2 coupons, webhook secret), `RESEND_API_KEY`, `CASDEY_SENDING_ADDRESS`, and `CRON_SECRET`. `web/vercel.json` (added 2026-08-17) registers a cron hitting `/api/cron/send`, **dropped from hourly to once daily (`0 3 * * *`) on 2026-08-19** because the team turned out to be on the Hobby plan, which caps cron jobs at once/day (see the outage note below); revert to hourly if/when the team is confirmed on Pro. Production has since been redeployed and `/app` serves at casdey.com again; the Google OAuth consent screen was **published to production 2026-09-03 (B1 done, no verification needed)** and the Calendar env vars are now set in Vercel, so no *external* gate remains on `/app` itself. **Env-var state as of 2026-09-04:** the 3-tier Stripe set is **done** — all 9 vars (`STRIPE_PRICE_{STANDARD,PRO}_*` + `STRIPE_COUPON_PERCENT`) are in Vercel Production and Preview and the 6 retired ones were removed (F2). **WhatsApp partially wired 2026-09-04 (B8 done):** `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN` and `ANTHROPIC_API_KEY` are now in Vercel Production and Preview. **`TWILIO_WHATSAPP_FROM` is deliberately NOT set** — the value in `web/.env.local` is Twilio's shared *Sandbox* number, not one casdey owns, and since `whatsappProvider()` needs all three vars, omitting it keeps the channel honestly disabled rather than reporting "enabled" while messages silently reach nobody. WhatsApp sending is now blocked on a **Meta-approved sender + template approval**, not on billing; the inbound webhook is configured on the sender, so it lands in that same step. `/app`, existing paying gyms and new paid upgrades are all fine. **`CASDEY_PAID_TRIAL` (renamed from `CASDEY_TRIAL_PENALTY`) has been SET in Vercel Production since 2026-09-12** (not Preview). It is the switch for the V1.1 **paid first week** (a card and €1 at signup buying seven days of Pro, then automatic conversion), so production signup now takes a card; see the Track H bullet under Stage 2. **A flag change deploys with `npx vercel --prod --force` from the repo root:** `/terms/refunds` is statically prerendered, `vercel redeploy` reuses the build cache and keeps serving the stale page, and running the CLI from `web/` fails with a Root Directory error. **Plan confirmed Hobby, 2026-09-03 (plan item B5 done).** The Vercel plan badge next to the `casdey` project reads "Hobby", settling the earlier uncertainty: Hobby allows each cron job to run once a day but up to 100 jobs per project, so **since 2026-09-19 the campaign drain has five daily schedules on the same path** (`0 3`, `0 8`, `0 13`, `0 16`, `0 19` UTC) and each gym only sends between 8am and 8pm in its own timezone (`src/lib/send-window.ts`), which gives every timezone a gym can pick a morning run. The route reads Vercel's `x-vercel-cron-schedule` header so the trial job still runs only on `0 3 * * *`. Before that, sends went out once a day at 03:00 UTC: about 4am in Europe and 11pm in New York. Since 2026-09-13 `vercel.json` carries a second once-daily cron, `/api/cron/instagram` at `30 10 * * *` (the Instagram publisher, see Marketing plan); Hobby runs each within its scheduled hour, not at the exact minute. This supersedes the old "Pro Trial running 13–27 Aug, card Visa ••••7600" claim, which was wrong (the 2026-08-19 Hobby-only cron error already contradicted it). `web/SAAS_HANDOFF.md` carries the same B5 confirmation.
- **Deploy pipeline outage found and fixed, 2026-08-19.** The GitHub↔Vercel git integration had silently gone "sourceless": Vercel's own project record still showed it as connected (`vercel git connect` even reported "already connected"), but the webhook binding was dead, sometime between the last real deploy on 2026-08-17 and 2026-08-19. Every push to `main` in that window, several of them earlier the same day, including the unpublish redirects and the gym `/waitlist` rework, was not actually deploying despite pushing cleanly to GitHub, `casdey.com` kept serving 2026-08-17's code the whole time. Confirmed by fetching the live site directly, not through Google, whose stale cache is what first surfaced the problem. Fixed with `vercel git disconnect` immediately followed by `vercel git connect`, both run from the repo root, not `web/`: the CLI's git-repo detection doesn't reliably walk up from the Vercel Root Directory subfolder and fails there with a false "No local Git repository found". Verified with a throwaway empty commit that auto-deployed within a minute of pushing, no manual `vercel deploy` needed. The Hobby-plan cron limit above was a second, unrelated blocker surfaced by the same investigation. **Stray artifact from troubleshooting (resolved 2026-09-03):** an accidental empty Vercel project named `web` (distinct from the real `casdey` project) was created when `vercel git connect` was first run from the `web/` subdirectory, before the repo-root fix was found. It held no deployments and was deleted via `vercel project rm web` on 2026-09-03 (plan item B6 done); `vercel projects ls` now shows only `casdey`.
- **Push discipline (Davide's standing rule, 2026-08-14):** because `main` auto-deploys to production on push, committing locally is always fine, but pushing to `main` needs fresh explicit confirmation every time, even if a similar push was already approved earlier in the same or a prior session. Enforced by `.claude/skills/push-github/`. **A hole in that rule, found 2026-09-12: the gate protects the act of pushing, not the commits sitting on `main`.** Three V1.1 commits that were deliberately held unpushed went to production inside a *different* session's push (a `/check-up` fix committed on top of them), so they deployed without the confirmation ever being asked for. Nothing was harmed, but the lesson is general: any commit left on `main` is one unrelated push away from being live, and concurrent sessions are normal here. Work that must not deploy yet belongs on a branch, not on `main`. **GitHub push and Vercel deploy are not separable right now** — Vercel has no commit-message flag to skip a deploy; the only way is a one-time "Ignored Build Step" setting in Vercel's project settings, which Davide was offered and declined on 2026-08-15 (wants things left as-is). Until that changes, pushing to `main` on GitHub *is* deploying casdey.com to production, no way around it.
- `casdey.com`: DNS moved from GoDaddy's parked-page defaults to Vercel on 2026-08-13. Two records changed at GoDaddy (`A @ → 216.198.79.1`, `CNAME www → c7edcf5d9dc7d3ff.vercel-dns-017.com.`); the existing Zoho MX/SPF/DKIM records were left untouched and email still works. SSL is auto-provisioned by Vercel. DNS propagated within minutes for this test but can take up to 48h globally.
- Supabase: project `casdey`, region **eu-west-1 (Ireland)** — corrected 2026-08-15; earlier docs said eu-central-1 (Frankfurt), confirmed wrong via the Supabase dashboard. Still EU, so residency holds, but any "Frankfurt" wording elsewhere (including marketing copy) should be fixed to "Ireland". Used so far for the waitlist (`waitlist_signups` table) and now the SaaS's own data too — **migrations through `0041` applied to the live project** (`0040` lets three currency columns accept `usd` for the US market and `0041` adds casdey HQ's own tables for `/admin`, both 2026-09-19; `0039` the Instagram publisher's `instagram_account` table and public `instagram` storage bucket, 2026-09-13; anon and authenticated had Supabase's default REFERENCES/TRIGGER/TRUNCATE on the new table, which RLS does not govern, so the migration revokes them) (`0002`–`0010` pre-pivot; `0011` dental→gym rename; `0012` at-risk campaigns; `0013` cancellation reason; `0014` WhatsApp channel revival; `0015` booking overlap guard; `0016` `plan_tier` for 3-tier pricing — those three applied 2026-09-03; `0017` per-gym sending identity, applied 2026-09-04; `0018`–`0021` the offer builder, feedback box, calendar scope fix and the import hardening from the 2026-09-05 readiness pass; `0022`–`0027` the lapse-rule, follow-up, services, offer-variant and personalisation work; `0028`–`0033` from the D1 walkthrough, 2026-09-06/07; `0034` `gyms.cancels_at`, 2026-09-07; `0035` `gyms.is_internal` + a one-row `cancels_at` backfill, 2026-09-08; `0036` flags "Bridge Street Gym" (the `test@casdey.com` fixture) as internal too, 2026-09-08; `0037` the gym-native lapse window (90-day default + `gyms.lapse_rule_set_at`) and `0038` Trial With Penalty (the `trial_*`/`activated_*` columns + the `trial_penalties` table), both 2026-09-12 — all in verified transactions over `SUPABASE_DB_URL`). **`0032` and `0033` are worth knowing about specifically:** `0029` and `0031` had created tables with RLS on and no grants, so saved offers and custom reasons were invisible to the app and two shipped features looked unbuilt; `0032` adds the missing grants, and a new table with RLS should be checked for grants from now on. `0033` makes the cancellation reasons the gym's own rows, defaults seeded once per gym, so `DEFAULT_REASONS` is seed data rather than a runtime fallback. Free tier. Auth transactional email now goes through custom SMTP → Resend (see the Resend bullet above), not Supabase's shared sender.

## Legal/tax context (Italy)
- No Partita IVA yet — first clients will be invoiced via prestazione occasionale.
- Partita IVA to be registered once revenue is consistent (regime forfettario).
- **Corrected 2026-09-12: prestazione occasionale probably no longer fits casdey.** It requires the activity to be non-habitual. Recurring subscriptions, with a published site and daily outreach, are habitual and organised, which makes a Partita IVA obligatory regardless of the amount earned (Agenzia delle Entrate interpello 63/2024 treats repetition over time, even with one client, as a strong indicator). This needs a commercialista rather than more research. **Parked, on Davide's explicit call: revisit once money is coming in.** Detail in `web/SAAS_V1_1_PLAN.md`, Track H item 3.

## Open questions before building the SaaS
Raised 2026-08-13. Most were answered the same day and the first SaaS build reflects the answers (see `web/SAAS_HANDOFF.md` for the full assumption list):
- MVP scope: **answered** — CSV/spreadsheet upload is the working, universal path; Dentally is stubbed (interface implemented, reports "not connected" until real API credentials exist), not a guess. A **manual** price list per practice now exists (2026-08-14, Settings → Service prices) and feeds the revenue-recovered estimate on the dashboard, which the guarantee refund (2026-08-15, see below) is judged against. Calendar write-access is now built (2026-08-15/16, see the booking-loop bullet above) — casdey can book and cancel appointments in a practice's connected Google Calendar. Automatic price-list read-access (from the practice's own software, rather than the existing manual Settings → Service prices entry) is still **not yet built**.
- Auth: **answered** — Supabase Auth, same project as the waitlist DB (email/password + Google, both fully wired as of 2026-08-15).
- Billing flow: **answered 2026-08-14, revised to 3 tiers 2026-09-03 (Track F).** The SaaS implements **trial → free → Standard/Pro** (see `web/src/lib/plan.ts` and `web/SAAS_V1_PLAN.md` §F0): a new signup gets a 7-day free week granting the full Pro feature set with **no card taken** (casdey-managed, not a Stripe trial), then drops to Free (import + see lapsed, 50-member cap, cannot send), and upgrades to Standard (€99/mo, email only, 200 members) or Pro (€289/mo, + WhatsApp + guarantee, 2,000 members) via Stripe with a lifetime **20%** early-adopter discount applied automatically. The paid tier is stored on `gyms.plan_tier` (migration `0016`), written by the Stripe webhook from the subscription price. Code done, and as of 2026-09-04 the live Stripe prices/coupon and all 9 env vars are set too (plan item F2 done). The earlier "card at signup, auto-charged after trial" design, and the intermediate single-"Premium"-tier design, were both replaced. **Reopened and partly reversed 2026-09-10/12 by V1.1 Track H:** Davide's call was to take a card and €1 at signup after all, and to auto-convert at day 7, which is close to the design this bullet says was replaced. The setup-fee half of Track H was dropped on 2026-09-12, leaving the €1 paid week and the day-7 conversion, and **that has been live in production since 2026-09-12** (`CASDEY_PAID_TRIAL` set), with Stripe billing day 7 itself. The no-card, casdey-managed week described above now runs only with the flag off. See `web/SAAS_V1_1_PLAN.md` Track H before changing signup, and note that gyms who signed up under the no-card terms keep them.
- Messaging channel: **answered, then reopened.** Email was the v1 channel; **WhatsApp was revived 2026-09-03 (Track E1)** after an engaged lead asked for it, and is a Pro-plan feature (Twilio + Claude Haiku reply loop). SMS is still out. The first campaign a gym sends still requires explicit approval before it goes out.
- Still open: how the free-first-week promise gets fulfilled operationally for any real signup that arrives before the product is solid. `web/SAAS_HANDOFF.md` calls this out explicitly: the gap stays manual, Davide handles it personally if someone signs up early.
- Free plan limits: **answered 2026-09-03 (B7/A8).** Free = import + see lapsed, hard **50-member cap** (net-new, at import), and only the first **5** members revealed by name. (What "lapsed" means on that screen changed 2026-09-12, see Track I above: the default window is 90 days, not 12 months.) Standard 200, Pro 2,000. Set in `web/src/lib/plan.ts` (`MEMBER_LIST_LIMIT` / `MEMBER_IMPORT_LIMIT`).
