---
name: gym-outreach
description: Runs casdey's gym/fitness-studio cold-outreach workflow — sourcing UK/EU gym and studio leads, drafting personalized cold emails and follow-ups, and sending them via Resend. Fully automated as of 2026-08-28 (no human approval gate); the daily Routine sources, drafts, sends a fixed 100 first-touch/day plus every due follow-up (raised from 75/day on 2026-09-07 after Resend Pro), and reports. Use when asked to run outreach, draft cold emails, source new gym leads, or check outreach status/tracking. Supersedes the retired dental cold-outreach skill.
---

# casdey gym/fitness-studio outreach

Operating spec for casdey's cold-outreach system, targeting UK/EU gyms and fitness studios (the niche pivot from dental, see CLAUDE.md's "Niche pivot under consideration" and "Go-to-market plan for the niche pivot" sections, both required reading before running this). Invoked as `/gym-outreach`.

## Current state — READ FIRST (2026-09-02 / 2026-09-03 / 2026-09-06 / 2026-09-07 / 2026-09-13 changes)
- **T2, the outcome-led email, live from the 2026-09-14 run (Sunday review, 2026-09-13, Davide's call). Supersedes the variant B, subject-test and "feedback-first for everyone" bullets below.** Both routine prompts were rewritten via the RemoteTrigger API the same day; they stay authoritative.
  - **Goals the routines now report against:** 1% **engaged leads** for the week, measured on that week's contacts (gyms first contacted in the last 7 days that are now `Interested`/`Committed`, over gyms first contacted in the last 7 days; about 7 from about 700), and 2 paying gyms by 2026-10-13. An engaged lead is a gym interested in casdey, **not a reply**; a "yes, send me the video" counts and is logged `Interested`. The reply rate (target 3%) is reported separately and never called "engaged" any more.
  - **T0 closed** (A kept on direction only; variant B's "I'll set it up free on your lapsed list" got 0 replies in 290 and is retired: handing a member list to a stranger is a hidden cost, *$100M Leads* pg 97). **T1 closed**, no winner: every first touch now uses `S1` verbatim; `S2` is retired for new sends but still matched for replies.
  - **T2 = A against V**, by lead-# parity (even A, odd V), `Variant` column `A`/`V`, judged on engaged leads per arm. **V sells the result, not the tool** ("what if the members who left <gym> started paying you again, without you chasing a single one?"), says about **ten** minutes to set up (never five, the product's own trial page says ten), and **gives first**: a free 2-minute video plus the exact message casdey would send that gym's ex-members, with only an honest opinion asked in return, CTA "Want me to send it?". Sources: *$100M Offers* pg 96, *$100M Leads* pg 94 and 169-170. V's follow-ups repeat the offer in one line; A's stay as they were. No price, guarantee, link or trial mention in either.
  - **Replies to a yes** must deliver both promised things in one email: the video link (the `T2 VIDEO URL` line in the email routine's prompt, set 2026-09-13 to `https://www.casdey.com/see`, a hidden noindex page serving the 74-second promo film; if that line is ever reset to `not set yet`, drafts fall back to a `[VIDEO LINK]` placeholder that blocks sending) and a 60-90 word win-back message written for their gym. Engaged leads lead the report and the push notification, because a yes goes cold fast.
  - **Instagram DMs switched to the same outcome-led, give-first framing outright** (no A/B, 0 replies in 105 to protect). Step 0b re-drafts every unsent DM and unsent follow-up draft once.
  - **No cold calls** (Davide, 2026-09-13): outreach stays email and Instagram.
  - The Test Log holds T0 and T1 closed and T2 running; the review now happens inside `/check-up` on `main` (see that skill's "The weekly test review").
- **Schedule moved to 04:00 CEST for both routines, 2026-09-13 (Davide's call).** Was 07:00 CEST (`0 5 * * *` UTC) for "casdey gym outreach" and 08:00 CEST (`0 6 * * *` UTC) for "casdey gym IG outreach"; both are now **`0 2 * * *` UTC = 04:00 CEST**, so the 5-hour Claude usage window they consume resets by ~9am, before Davide starts his own work rather than mid-session. The "casdey weekly check-up" routine (a separate skill, see `web` repo root `.claude/skills/check-up/`) moved the same way, `0 8 * * 0` → `0 2 * * 0`. All three now fire at the same instant on Sundays; harmless, since each runs as an independent cloud session in its own environment. Every "07:00 CEST"/"08:00 CEST"/"`0 5 * * *`"/"`0 6 * * *`" mention below this banner is historical (true on the date it describes), not current.
The daily email Routine's own prompt was rewritten via the RemoteTrigger API on **2026-09-02**, patched **2026-09-03**, **2026-09-06**, and rewritten again **2026-09-07** (the "More, Better" marketing-plan changes — see CLAUDE.md "Marketing plan (rewritten 2026-09-07...)"). On COLD EMAIL COPY, VOLUME, FOLLOW-UPS, PART A drafting and PART B assembly/gates, **the Routine's live prompt is authoritative and overrides the older section text below** wherever they disagree. The material changes since the 2026-08-28 full-automation baseline:
- **Volume (2026-09-07, supersedes 75/day and the reserved-floor split below): FIRST-TOUCH is a fixed 100/day**, every day, the growth lever, never reduced by follow-up load. **FOLLOW-UPS are uncapped** — every due FU1/FU2 goes out the day it's due, drawn from a separate queue. Total ≈ 100 + due follow-ups (~250-320/day at steady state). No overall per-run ceiling. Enabled by the Resend Pro upgrade (2026-09-07); Free's 100/day cap had made this impossible. Every "50" and "75" below is stale.
  - **TRANSITION cap:** for runs dated **2026-09-08 through 2026-09-15 only**, follow-ups are capped at 150/run (oldest-due first, remainder rolls forward) to drain the existing backlog smoothly instead of one spike. From the 2026-09-16 run onward: no follow-up cap.
- **RESERVED-FLOOR SPLIT (2026-09-06) — SUPERSEDED 2026-09-07.** Kept for history: it capped follow-ups at 50 of 75 with a 25 first-touch floor, because a 75/day ceiling forced first-touch and follow-ups to compete. With first-touch now a fixed 100 taken from a separate queue and follow-ups uncapped, there is no competition and no floor/ceiling to tune. (The old warning about an unbounded follow-up queue only applied while a hard total cap starved follow-ups; it doesn't apply now that every due follow-up is sent.)
- **Subject-line A/B test T1 (2026-09-07, live from the 2026-09-08 run).** Two fixed first-touch subject lines, assigned by lead-# mod 4 (0,1 → `S1` = "quick question about your lapsed members"; 2,3 → `S2` = "win back the members who cancelled"), independent of the body A/B so both read marginally. Used verbatim, lowercase, never personalised. Logged in a new `Send Log` column **`Subject Variant`** (column Q). Follow-ups stay on the thread and get a blank Subject Variant. STEP R reports S1 vs S2 sent/replies/rate. This is the first test in the weekly A/B loop from the marketing plan; the running CTA A/B (permission vs free-to-try) is test **T0**.
- **Test Log tab (2026-09-07)** in `Casdey-Gym-Leads` — the cumulative ledger of every weekly A/B test (T0, T1, …): columns Test ID / Week started+ended / Asset / Component / Hypothesis / Variant A-C / Sends+Replies+Rate per variant / Winner / Decision / Weeks-unbeaten / Status / Notes. Created + seeded manually 2026-09-07 (T0 running, T1 running). The routine does NOT write it — the weekly review is a manual chat session (Claude computes from `Send Log`, Davide pastes the row). 4 weekly tests unbeaten → established winner, move on.
- **Sourcing ceilings raised (2026-09-07)** to feed 100 first-touch/day: skip-sourcing threshold 150 → **300** unsent leads; per-run sourcing cap 90 → **up to 200** new leads.
- **Two follow-ups per lead, not one.** FU1 at 4-5 days, then FU2 (lightest touch) 4-5 days after FU1, then stop forever. New `Send Log` columns: **`Variant`**, **`Follow-up 2 Due Date`**, **`Follow-up 2 Sent (Y/N)`**.
- **Follow-up eligibility bug found and fixed, 2026-09-13 — read "Follow-up policy" below for the full rewrite.** The 2026-09-11 run sent **150 duplicate follow-ups** (111 as FU2, 39 as FU1) to leads whose 2-touch sequence was already complete, because eligibility was decided by reading the `Follow-up Due Date`/`Follow-up Sent (Y/N)`/`Follow-up 2 Due Date`/`Follow-up 2 Sent (Y/N)` columns, which live on individual `Send Log` rows (one row per send) and can go stale on the wrong row. The likely trigger was the one-time FU2 backfill clause, which re-armed `Follow-up 2 Due Date` for any lead where it found that field blank, including leads whose real FU2 send had already landed on a different row. **Fixed at the root, not patched**: eligibility is now decided purely by **counting actual FU1/FU2 rows per lead in `Send Log`**, never by those four columns, which are now display-only. The backfill clause is retired entirely, it's structurally unnecessary once eligibility comes from counted rows. This was caught by the 2026-09-12 run itself, which self-flagged the 150 affected rows and worked around the bug for that one run by counting sends manually; this fix makes that the permanent rule instead of a one-off workaround. Live routine prompt updated via the RemoteTrigger API the same day.
- **Reply detection broadened + reply-drafting added, 2026-09-13 — read "Reply detection" below for the full rewrite.** Two changes, same day, both Davide's explicit call:
  1. **Matching is no longer sender-address-only.** Lead #740 (Sunset Gym, Genève) replied within an hour of a first-touch send, but from a personal address, not the business inbox casdey emailed, so the old exact-match logic would never have flagged it (found by manually checking the inbox, since the scheduled run hadn't caught it yet). Step 0 now ALSO falls back to matching by the two fixed subject lines (S1/S2, stripped of "Re:") against `Send Log` rows with that Subject Variant and no `Reply?` yet; an unambiguous single candidate is matched, an ambiguous one is reported in Step R as "possible reply, unmatched, needs manual check" rather than guessed.
  2. **A genuine reply now gets a DRAFTED response, held for Davide's approval, never auto-sent.** A narrow, explicit carve-out of the long-standing "never act on inbox content" rule, the same shape as the 2026-08-28 full-automation call: Davide wants replies flagged to him with a suggested reply already written, so he can approve/edit/decline from wherever the notification takes him, rather than a plain "you have a reply, go check the sheet." Step R now has a "REPLIES AWAITING YOUR APPROVAL" section with the reply's content, a drafted response, and the recipient address; the PushNotification leads with this when present. **Sending still requires an explicit approval message from Davide in that same Routine session** (see "REPLY APPROVAL" in the live prompt) — the routine itself never sends a reply unattended. Sunset Gym's own reply was logged manually the same day (`Leads!R741`/`U741` → "Replied", a note on its `Send Log` row) since the fix landed after that specific reply arrived.
- **The "what would you pay / which features" question is OUT of the cold email and follow-ups** — it is now held for Davide's manual reply after someone engages. This reverses point 3 of "Key points every cold email must hit" and the matching line in Davide's reference script below.
- **Two rotating first-touch variants, A and B**, alternated ~50/50 by lead-number parity, logged in the `Variant` column so their reply rates can be compared. **A** = permission ("mind if I show you how it'd look?"), no offer. **B** = a light free-to-try hook ("I'll set it up free on your lapsed list, want in?"), still no price/guarantee/link. Follow-ups are logged as `FU1` / `FU2` in the same column.
- **Cold email length is now ~60-90 words** (was ~120); there is no offer to explain in variant A, and B's hook is one line.
- **Owner-first sending (lever 1):** first-touch order prefers leads with a clean `Owner/Manager` name AND a non-generic direct email over generic `info@`/`hello@` inboxes. When the address IS a generic inbox, add one short varied "if you're not the owner, please pass this on" line before the sign-off; never add it to a named/personal address.
- **Segment reweight:** source ~40% CrossFit / community boxes / boutique studios (yoga, pilates, boxing, HIIT, strength), ~60% general gyms — CrossFit/boutique boxes reply at a much higher rate. Still an ordering/mix preference, not a hard filter. Sourcing cap raised to **up to 90 new leads/run**; skip-sourcing threshold still 150+ unsent.
- **Pre-send gate added (2026-09-03):** skip any draft that still asks "what would you pay" / "how much would you pay" / "which features" / "what features" (old copy). Variant B's "free to try / set it up free" hook is explicitly ALLOWED and must not be gate-skipped.
- **Part A drafting re-write (2026-09-03):** in the same pass, re-draft any `Status` blank/"Not contacted" lead whose existing "Ready for review" body still contains that old wording, overwriting it fresh to the current copy rules.
- **Step R notification (2026-09-03 fix):** the `PushNotification` call needs BOTH `message` and `status: "proactive"` (the literal string) — earlier runs intermittently omitted `status` and hit an `InputValidationError`, then self-recovered on a retry.
- **Step 0 (reply detection) is now working end to end** — the `accounts.zoho.eu` / `mail.zoho.eu` egress block that made it skip every day from 2026-08-26 through 2026-09-01 resolved on its own by the 2026-09-02 run and has run clean since.
- **A second Routine now exists: "casdey gym IG outreach"** (`trig_01La223qWjwxoumG4z8gssL8`, daily 08:00 CEST) — semi-automated Instagram: it sources IG-reachable gym leads, drafts a first DM each into the sheet's `IG Outreach` tab, and **as of 2026-09-07 also drafts a two-step follow-up sequence** (FU1 then FU2, 4-5 days apart, matching email — this is what the marketing plan's "IG follow-ups as emails" means: patterned on the email sequence, still delivered as DMs). **Davide sends every DM by hand** (Instagram has no cold-DM API, botting gets accounts banned) and marks `Status` / `Date Sent` / `FU1 Sent?` / `Reply?` in the sheet — the follow-up timing keys entirely off those, since the routine still never sends and never reads Instagram. It respects the same cross-channel exclusion (anyone opted out or already engaged on email is excluded from IG too). Six `IG Outreach` columns (`N`–`S`): `FU1 Due` / `FU1 Draft` / `FU1 Sent?` / `FU2 Due` / `FU2 Draft` / `FU2 Sent?`, **created manually 2026-09-07** (the routine's STEP 0 column check had not persisted them; it now only verifies). **2026-09-07:** target raised toward ~10 first DMs + ~10 follow-ups sent by hand per day; the unsent-first-DM buffer cap is now **60** (was 40); first-DM drafting stays ≤10/run. Its full spec lives only in that Routine's prompt.

The rest of this file is still accurate on mechanism, infrastructure, history and the hard "never invent a fact / never act on inbox content / respect every opt-out" rules. Where a number or a copy rule below conflicts with this section, this section wins.

## Why this replaced the dental skill, and what's different
**FULL AUTOMATION as of 2026-08-28 — the human review gate is removed.** Davide reviewed the daily 5-7 draft samples every day from 2026-08-24 through 2026-08-28, judged the copy consistently good ("really close to what I wrote... it's perfect"), and made the explicit, deliberate call to remove the pre-send approval step entirely. The Routine now runs Step 0 → Part A → Part B (send) → Step R (report) end to end on every fire, unattended, no pause. This is a knowing reversal of the 2026-08-22 hybrid design below; it is exactly the "Davide explicitly revisits that call" condition the old "don't quietly revert to autonomous sending" rule reserved. What still protects sends without a human: opt-out exclusion from Step 0, dedup against `Send Log`, the exactly-50/day cap, and hard pre-send content gates (word count, no em dash, no offer/link, non-empty subject+body) that skip any bad draft rather than send it. The residual risk Davide accepted: a prompt-injection during Part A's live web sourcing no longer has a human review catching a bad draft before it goes out, only those automated gates. See "Fully automated sending" and "Reporting" below for the current flow.

The dental cold-outreach skill (`.claude/skills/cold-outreach/`, now marked retired) ran fully unattended: an AI Routine sourced, drafted, and sent up to ~20 emails a day with no human review. 216 sends produced 0 genuine replies. The go-to-market plan drafted afterward (CLAUDE.md) concluded that volume without a human in the loop was itself part of the problem, not just the message, and calls for a manual/relationship-first approach for gyms: small batches, real review, direct conversations, feedback collected at every step. Davide confirmed this on 2026-08-22 with a **hybrid** model (superseded 2026-08-28, see the banner above — kept here as the design history):
- **Automated**: lead sourcing and first-draft email copy.
- **Manual (until 2026-08-28)**: the actual send. Every batch was reviewed (and could be edited) by Davide before anything went out.

Email/community-DM outreach (Instagram, Facebook/Slack groups) that the go-to-market plan also calls for is Davide's own manual track, outside this skill's scope — this skill only covers the email channel, where casdey already has sending infrastructure.

**Enforcement mechanism changed 2026-08-23 — read this carefully, it's a real posture shift, not just a cleanup.** From 2026-08-22 to 2026-08-23 the "manual send" rule above was backed by a hard technical gate: sourcing/drafting ran in a cloud Routine environment that physically had no `RESEND_API_KEY` in it, so even a successfully-injected unattended run couldn't have sent anything, the credential simply wasn't reachable. On 2026-08-23 Davide asked to merge the two Routines that existed briefly that day (one sourcing+drafting, one review+send) back into a single Routine for UI convenience, after being told explicitly what that costs: the single merged Routine's environment now holds `RESEND_API_KEY` alongside the web-browsing/sourcing capability, so the "never send unattended" rule is now enforced entirely by the prompt's Part A / Part B split (see "Routine status" below), not by credential absence. Davide made this call knowingly. If this skill is ever revisited and someone wants the harder guarantee back, split the Routine again and give the sourcing half no sending credential, per the 2026-08-22 design.

## Which environment am I running in?
- **Local/interactive session on Davide's desktop**: `.env.local` (in `web/`) has `RESEND_API_KEY`, and `~/.config/casdey/service-account.json` has the Google service-account key. Use `GOOGLE_SERVICE_ACCOUNT_FILE` pointing at that path for Sheets access.
- **Unattended or manual (the single merged Routine, see "Routine status" below)**: credentials come from process env vars directly, set as secrets in the Routine's cloud-environment settings (`casdey-Outreach-Send`, id `env_01Wmn5uCu7qGFvpVuiacSs3z`): `GOOGLE_SERVICE_ACCOUNT_JSON` (Sheets), `RESEND_API_KEY` + `CASDEY_OUTREACH_SENDING_ADDRESS` (sending), and, **as of 2026-08-25, `ZOHO_CLIENT_ID` / `ZOHO_CLIENT_SECRET` / `ZOHO_REFRESH_TOKEN` / `ZOHO_ACCOUNT_ID` / `ZOHO_API_DOMAIN` / `ZOHO_ACCOUNTS_DOMAIN`** (read-only inbox access for reply-detection, see "Reply detection" below). This reverses the 2026-08-23 design, which deliberately kept `ZOHO_*` out of this environment so the Routine could never touch the real inbox at all. Davide asked explicitly, 2026-08-25, to make reply-checking part of the automatic daily flow instead of a manual local-only step, after using a local session to run it by hand once; this trades the "Routine literally cannot read the inbox" guarantee for convenience, the same kind of call already made once for `RESEND_API_KEY` (see the enforcement-mechanism note above). Access stays read-only regardless (the prompt only ever calls the message-list/message-content read endpoints, never send/reply), so the blast radius of a prompt-injected sourcing run gaining this credential is "can read the inbox," not "can send as davide@casdey.com" or "can act on what it reads" (still hard-forbidden, see "Reply detection" below). **Gotcha hit and fixed while wiring this up, 2026-08-25**: `ZOHO_API_DOMAIN` and `ZOHO_ACCOUNTS_DOMAIN` are stored as full URLs already (e.g. `https://mail.zoho.eu`), not bare domains, prepending another `https://` when building a request URL silently breaks DNS resolution instead of erroring clearly, the Routine's prompt now says this explicitly. **As of 2026-08-23: this environment's network egress policy blocked `api.resend.com`, fixed the same day** by switching Network access from "Trusted" to "Custom" with `api.resend.com` allowlisted, see "Routine status" below for how to edit that setting and the exact history; if reply-detection starts failing with an egress error against `accounts.zoho.eu`/`mail.zoho.eu`, the same Custom-allowlist fix likely applies, those hosts weren't needed (and so weren't allowlisted) before this change. Its GitHub App integration still only has read access to this repo, so a cloud session here can review/approve live and (as of the fix) should be able to call Resend directly, but still cannot push a code change, that still fails with a clear policy-denial error. Until Davide fixes the GitHub App's write scope (see "Routine status" below), treat any repo push as something only a local/interactive session can finish, even if a cloud session did the reviewing.
- **Adding the `ZOHO_*` secrets to the cloud environment is a step only Davide can do**, not something a Claude session (local or cloud) should ever do on their behalf, even local Bash/PowerShell access to the real values in `.env` doesn't change that: entering API keys/tokens into any settings field is off-limits for Claude to do directly. In the environment picker (composer or a Routine's Edit dialog), hover the environment name, click the gear icon for "Update cloud environment," and add the same six `ZOHO_*` values already present in the repo's root `.env` (same names, same values, EU data center). Until that's done, the Routine's Step 0 (see "Reply detection" below) will find the credentials missing and skip itself, saying so plainly, exactly as designed, not a bug.

## Resources
- **Lead list**: Google Sheet "Casdey-Gym-Leads" (id `1WOAIA1gvK6S1kWe_Vf4-d4XmjhnDLQZLtyU_ezvOu3w`), created 2026-08-22, shared Editor with `casdey-routine@casdey-gws-cli.iam.gserviceaccount.com`. Tabs:
  - `Leads` — columns: `#`, `Gym/Studio Name`, `City`, `Studio Type` (Independent/Chain/Franchise), `Phone`, `Address`, `Email`, `Owner/Manager`, `Email status` (`confirmed` vs `VERIFY`), `Notes / Chain`, `Instagram/Social`, `Other Channels Contacted`, `Channel Notes`, `Assigned To`, `Draft Subject`, `Draft Body`, `Draft Status` (blank / "Ready for review" / "Sent" — added 2026-08-22, see "Drafting" below), `Status` (Not contacted / Contacted / Replied / Interested / Committed / Dead), `Date Contacted`, `Follow-up Sent`, `Reply?`. The three social/channel columns (added 2026-08-22) are for Davide's own manual outreach (Instagram DM, community/Facebook/Slack groups, per the go-to-market plan) — this skill only ever sources them for reference and never sends anything outside email itself.
  - `Send Log` — columns: `Lead #`, `Gym/Studio Name`, `Recipient Email`, `Date Sent`, `Salutation Type`, `Opening Line Used`, `Follow-up Due Date`, `Follow-up Sent (Y/N)`, `Notes`, `Resend Email ID`, `Opened?`, `Clicked?`, `Last Event At`, `Variant`, `Follow-up 2 Due Date`, `Follow-up 2 Sent (Y/N)` (last three added 2026-09-02 — `Variant` records `A`/`B` for a first-touch send or `FU1`/`FU2` for a follow-up; the two `Follow-up 2` columns drive the second follow-up, see "Current state" and "Follow-up policy"). One row per send. Check this before ever drafting to a lead, to prevent duplicates.
  - `How to use` — brief pointer back to this file.
- **Sending**: Resend API (not Zoho — the dental skill's sender). **Root `casdey.com` is its own Resend-verified domain**, separate from `mail.casdey.com` (the product's campaign-email domain) — added 2026-08-22 specifically so outreach sends as `you@casdey.com`, a real person's address, not a subdomain. Safe alongside Zoho's existing mail on the same root: Resend's DKIM (`resend._domainkey.casdey.com`) and SPF (isolated on `send.casdey.com`, its own MX+TXT) are on dedicated subdomains, the root SPF/MX/DKIM Zoho uses for davide@/abhi@/info@ is untouched. Credentials: `RESEND_API_KEY` (already set, Sending-access-only scope) and `CASDEY_OUTREACH_SENDING_ADDRESS` (set to `davide@casdey.com`, the actual Zoho inbox address — Resend sends it, SPF/DKIM now authorize both Zoho and Resend to send as casdey.com, so this isn't a spoof, and using the same address for From and Reply-To reads as more human, not less). Use `scripts/resend-send.js` to send: it sets `reply_to` to `davide@casdey.com` so replies land in the real Zoho inbox, and returns the Resend `id` (email_id) to log in `Send Log` for tracking.
- **Open/click tracking**: `casdey.com` was registered in Resend with a `links.casdey.com` tracking CNAME (added 2026-08-22, along with the DKIM/SPF records above, all in one batch). **Status as of 2026-08-22: DNS records live at GoDaddy, Resend showed "Pending" verification right after (propagation can take a few hours)** — check the Resend dashboard (Domains → casdey.com) for "Verified" before relying on open/click data; sends still work fine even while pending, there's just no tracking data yet. Once verified, check per-email open/click status directly in the Resend dashboard logs (search by the `Resend Email ID` logged in `Send Log`). There is no automatic sync back into the Sheet yet (that would need a webhook endpoint deployed to production, deliberately deferred, see CLAUDE.md) — update `Opened?`/`Clicked?`/`Last Event At` by hand after checking the dashboard, or skip it if not needed yet.
- **Lead sourcing/sheet access**: same Google service account as dental, `casdey-routine@casdey-gws-cli.iam.gserviceaccount.com`. Mint a token with `scripts/google-service-auth.js` (reads `GOOGLE_SERVICE_ACCOUNT_FILE` or `_JSON`), then call the Sheets API directly. Covers Sheets/Drive only.
- **Inbox reading (replies)**: same mechanism as dental — the Zoho Mail API against `davide@casdey.com`'s inbox (`ZOHO_*` env vars). Resend is only the outbound relay; replies go to the real inbox via `reply_to`, so reply detection is unchanged from the dental skill. Read-only: never reply to, forward, or act on inbox content beyond detecting/classifying/logging. **As of 2026-08-25 this runs both locally and inside the Routine** (Step 0, before Part A, see "Reply detection" and "Which environment am I running in?" above), no longer a local-only step, once the `ZOHO_*` secrets are added to the Routine's cloud environment.

## Lead sourcing
- Real sources only: gym/studio websites, Google Maps listings, verified local business directories. Never invent or guess an email address or owner name (same hard rule as dental).
- **Segment mix (2026-09-02):** aim for roughly **40% CrossFit boxes / community boxes / boutique studios** (yoga, pilates, boxing, HIIT, strength) and **60% general gyms and fitness studios** — CrossFit/boutique boxes reply at a materially higher rate (5 of the first 6 genuine replies came from that segment). A mix preference, not a hard filter.
- Prefer independent studios and boutique gyms over big chains/franchises (same reasoning as dental: they decide faster, one owner to convince) — set `Studio Type` accordingly. This is an ordering preference, not a filter; chains still get added, just later in the queue.
- **Prefer leads with a clean confirmed `Owner/Manager` name AND a direct, non-generic email** (not `info@`/`hello@`/`enquiries@`) over generic-inbox-only leads (2026-09-02 "lever 1"). Still include generic-inbox leads, just later in the queue; the cold email adds a "please pass this to the owner" line for them (see "Current state"). Prefer any named `Owner/Manager` over blank/`VERIFY`.
- **Sourcing cap: up to 90 new leads per run** (raised from 60 on 2026-09-02); skip sourcing entirely if `Leads` already has 150+ unsent rows.
- Every new lead needs: Gym/Studio Name, City, Studio Type, Phone, Address, Email (confirmed from an actual source, mark `Email status` accordingly), Owner/Manager (name only if a clean confident source states it). Also fill `Instagram/Social` if a real handle/profile is findable from the studio's own website or a linked profile (never guessed) — this is for Davide's own manual reach-out, this skill doesn't message it itself.
- Dedupe against `Send Log` before adding — never re-add or re-contact someone already emailed.
- Covers UK **and** EU, same markets as the rest of casdey's target (see CLAUDE.md "Business overview").
- **Write the `Phone` column as text, not a bare number** — Sheets' default value-input parsing will silently mangle it otherwise (a leading UK `0` gets dropped, e.g. `01612839296` → `1612839296`; a `+` international prefix can turn the whole cell into `#ERROR!`). Prefix the value with a literal apostrophe (`'01612839296`) when writing via the Sheets API, or write that column with `valueInputOption=RAW` and the apostrophe still included — either way, always re-read the row after an append and fix any mangled phone cells before finishing the run, don't assume the first write landed clean. (Caught and fixed live in the first real run, 2026-08-22 — see "Routine status" below.)

## The cold email
First-touch email, sent during the pre-launch/beta phase — the gym SaaS build is still local, not deployed (see CLAUDE.md).

**Superseded in part 2026-09-02 — see "Current state" at the top.** Since 2026-09-02 there are TWO rotating first-touch variants (A permission / B light free-to-try), the email is ~60-90 words, and the "how much would you pay / which features" question is NO LONGER in the cold email or follow-ups (held for Davide's manual reply). The rest of this section — feedback-first framing, no price/guarantee/link, tone guardrails, salutation rules — is unchanged and still fully in force.

**Style rewritten 2026-08-23 on Davide's explicit direction, permanent, applies to every future cold email and follow-up under this skill, not a one-batch tweak.** The earlier offer-led version (open with the pain point, then lead with the free-week/Free-plan/lifetime-discount offer and a waitlist link) read as too salesy. Davide reviewed a live batch and asked for a plain, honest, feedback-only ask instead, no offer anywhere in the first-touch email. This also brings the email back in line with what CLAUDE.md's go-to-market plan actually specified for this stage ("cold outreach purely to collect data: ask things like 'what do you think of this tool?' and 'how much would you pay for it?'") — the offer-led version had drifted from that.

**Davide's reference script** (inspiration for tone and structure, never copy-paste verbatim, personalize and vary per lead):
> I'm going around asking gym owners for some feedback on an idea I'm working on... Just feedback, I don't want to sell you anything. I just need to know if it's a good or terrible idea.
>
> I want to develop a software for gym owners that aims to bring back the customers that stopped coming and turns them into paying clients again... automatically.
>
> If you think that's good, it would really help if you also told me how much would you pay per month for something like this, and what features would you find useful in it.
>
> Thanks a lot for your time, it means the world to me.
>
> Davide @ casdey

**Key points every cold email must hit** (any order/phrasing, no fixed template, vary structure and opening line per lead so it doesn't read as mail-merged). **Point 3 changed 2026-09-02 — see the strike-through:**
1. Explicit feedback framing, stated plainly and early: going around asking gym owners for feedback on an idea, not selling anything, just want to know if it's a good or terrible idea.
2. What casdey does, described generally: software for gym owners that brings back customers who stopped coming and turns them into paying clients again, automatically, reaching out in the gym's OWN name and booking them straight back in with no manual chasing by the gym. **Don't narrow this to just "books them into a class"** — casdey does both winning back a lapsed/cancelled membership and rebooking a class or session, state it generally enough to cover both, don't pick one specific mechanic as if it were the whole thing.
3. ~~The ask: if they think it's a good idea, what would they pay per month for something like this, and what features would they find useful.~~ **REMOVED 2026-09-02:** the pay/features question is no longer in the cold email or follow-ups — it is held for Davide's manual reply once a lead engages. Replace it with exactly ONE low-friction CTA: variant A asks permission to show them how it would look for their gym; variant B offers to set casdey up free on their lapsed list ("want in?"). See "Current state" for the two variants.
4. A brief, genuine thank you.
5. A one-line, low-key opt-out ("let me know if you'd rather I not follow up again," varied per email) — still required for PECR/GDPR risk reduction covering EU, even though the rest of the email is casual and offer-free.

**Hard rule, not a style preference — none of this belongs in the cold email or follow-up any more:**
- No offer at all: no free week, no Free plan, no lifetime discount, no standard price, no guarantee, no scarcity.
- No waitlist link (https://casdey.com/waitlist). The entire offer, link included, is held back for the manual follow-through once someone replies positively, see "The full offer" below, which now covers the beta offer too, not just the standard price/guarantee.
- No "reply with roughly how many members you've got and I'll work out the cost" calculation hook, that belonged to the old offer-led approach. It is NOT replaced by a pay/feature ask any more (that was removed 2026-09-02); it's replaced by the single permission/free-to-try CTA per the two variants.

**~60-90 words as of 2026-09-02** (was ~120; there's nothing to explain in variant A and variant B's hook is one line). Professional but casual, matching gym/fitness culture, no superlatives, no hype, no persuasion stacking, there's no pitch left to stack. Use whatever specific detail is available (city, studio type, class-based vs open-gym) naturally, but never assert facts about their business. Pre-send gate still allows up to ~130 words before skipping.

**Tone guardrails, carried over from dental (still fully in force, same reasoning)**:
- Don't assert facts about the recipient's own business ("that's revenue you've already earned") — presumptuous, we don't know their numbers.
- Don't manufacture social proof ("a lot of London studios we've looked at...") — reads as a stock cold-email tactic.
- Don't stack multiple persuasion techniques in one email, there's no persuasion left to stack, this is a plain, honest ask, not a pitch.
- Write like a specific person is asking a genuine, slightly casual question, not a script executing a formula.

## Salutation rules (apply exactly, from the `Owner/Manager` column)
1. Blank/empty → "Hi there" or "Hi [Gym/Studio Name] team" (vary)
2. Contains "VERIFY", "verify", "no named owner found", "corporate", "group", or any hedge/uncertainty qualifier → "Hi [Gym/Studio Name] team" — never use a name from that cell even if one is present
3. Only use "Hi [FirstName]" when the field is a clean, confident single name with no qualifier — gym/fitness culture reads as more first-name-casual than dental, unlike the dental skill's "Dear Dr [Surname]" convention, don't carry that formality over
4. Always personalize [Gym/Studio Name] and [City]

## Style rules
- Never use em dashes (—) as punctuation. Use commas or separate sentences. Normal hyphens in compound words are fine.
- Sign-off: `Davide @casdey` — single line, no line break, no "Best,"/"Regards," preamble.
- Plain text only, no logos, no image attachments.

## The full offer (reference only — NEVER put this in a cold email or follow-up)

**Rewritten 2026-09-12 (V1.1 Track J).** Everything below is for Davide to walk
a lead through by hand once they reply with genuine interest. None of it goes
in a cold email or a follow-up, and the reason is not caution, it is where the
books put it: a guarantee is a Perceived Likelihood element ($100M Leads pg 89)
and belongs wherever the offer is made. The cold touch deliberately makes no
offer, so there is nothing for a guarantee to reverse the risk of. What touch
one carries instead is Big Fast Value ($100M Leads pg 169-171), which since
T2 (2026-09-14) is variant V's free video plus a message written for the gym.
(Variant B's free setup on their lapsed list was retired 2026-09-13 after 0
replies in 290.)

Davide is casdey's sole founder. Earlier versions of this section said
"Davide"; Abhi has had no involvement since the first few emails, so
there is no second pair of hands for any of this.

**The product is live.** casdey.com was published 2026-09-07, so the old "once
the software ships" framing is dead. Signup is
`https://casdey.com/login?mode=signup`. Do not send the waitlist link any more:
it still works and the outreach has pointed at it since August, but a lead who
replies today should go straight to signup.

### What they get, in order

- ~~**Free week**, the full Pro feature set, seven days, **no card taken**. Then
  the account settles onto the Free plan by itself.~~ **Stale since 2026-09-12:
  it is a €1 paid week now, see "Not in play" below.**
- **Free plan**, indefinitely: import the list and see who has gone quiet and
  roughly what it is worth a month. It cannot send. That gap is the pitch, and
  it is deliberate.
- **Standard, €99/mo** (£89; €990/yr): email win-back and at-risk check-ins,
  200 members. No WhatsApp, no guarantee.
- **Pro, €289/mo** (£249; €2,890/yr): everything, including the WhatsApp
  channel and the profit-or-nothing guarantee, 2,000 members.
- **Early adopter: a lifetime 20% off** either paid tier, for as long as they
  stay subscribed. Exclusive to people who came in during this window.

### How to frame the choice on the reply

Do not say "we offer a guarantee". Frame it as the choice between the two
tiers, per the Prepay + Guarantee pattern ($100M Leads pg 101):

> Same product on Standard and Pro. Standard is €99 a month, cancel any time.
> Pro is €289 and it carries this: if casdey doesn't recover more than it cost
> you over your first 30 days, you refund yourself the whole thing from your
> billing page. One click, no review, no email, no argument. That risk sits
> with me, not you. The guarantee is basically the only difference between the
> two.

Two rules on top of that:

- **State their actual lapsed-member number next to the price**, taken from
  their own Free-plan screen once they have imported. Numbers, not adjectives.
  The whole reason the Free plan can import is so this sentence exists.
- **The guarantee rewards the more committed buyer, and saying so plainly is
  the point.** Pro gets it, month-to-month Standard does not. Do not soften
  that or offer it on Standard as a closing move.

### What the guarantee actually is, mechanically

Say this accurately or not at all, because it is self-served and it pays on the
first click. Checked against `src/lib/guarantee.ts` and `/terms/refunds`:

- **Pro only.** Standard has no equivalent.
- **One 30-day window per gym, ever.** It opens on their first campaign on or
  after their first real Pro payment, and can never be re-armed.
- It compares revenue casdey recovered over that window against what casdey
  charged over the same window. Not opens, not replies.
- If it falls short they claim a **full refund of the payments taken in that
  window** from their own billing page. Nobody at casdey reviews it.
- The old wording, "100% refund plus free software until the condition is met",
  was never built. Do not use it.

### Not in play

- **Bonuses and scarcity**: only if Davide explicitly revives them for this
  niche. Not carried over from dental, and V1.1 deliberately parks them.
- **The setup fee** (the €20-per-unfinished-step idea from V1.1 Track H) was
  dropped on 2026-09-12 and never went live. Never mention it.

**CORRECTED 2026-09-13: signup takes a card and €1 now. The "Free week, no card
taken" bullet above is stale.** `CASDEY_PAID_TRIAL` has been set in production
since 2026-09-12. What to tell a lead today:
- **€1 and a card buy seven days of Pro**, the full feature set.
- **On day 7 Pro simply begins** (€231.20/mo with the early-adopter 20%)
  unless they cancel inside the week, in which case the €1 is all they pay.
- **They can skip the week** and start on the Free plan without paying:
  the paid-week step has a "Continue on the Free plan" link.
- Gyms that signed up before 2026-09-12 (BodyActive) keep the old no-card terms.
Check `CASDEY_PAID_TRIAL` in Vercel before describing signup any other way.

## After a yes: the manual playbook (added 2026-09-13)

For Davide, sent by hand. The routine drafts only the **first** response to a
yes (the video link, the message written for their gym, a line asking for an
honest opinion, and the ten-minute setup offer). Everything after that is here.
Written before T2's yeses arrive because, per *$100M Leads*, a sale at this
price takes 2 to 3 conversations, and the answers to common concerns should
exist before anyone asks (content feeds sales, pg 143). **Answer the same day:
a yes goes cold fast.**

**Principles**
- **ACA on every reply** (*$100M Leads*, warm outreach): acknowledge what they
  actually said, compliment it, then ask one question that moves toward setup.
- **One ask per message.**
- **Done-for-you setup is fine now.** It failed as a cold give (variant B, 0
  replies in 290) only because a stranger was asked for a member list. A gym
  that asked for the video is no longer a stranger.
- **A screen share with a gym that asks for one is not a cold call.** Davide's
  no-calls rule (2026-09-13) is about cold follow-up.
- **Price only when they ask, or when the next step is signup**, because the
  card step would otherwise come as a surprise.
- **No invented numbers or results.** casdey has no case study yet. If asked,
  say so, and lean on the Pro guarantee and on Davide doing the setup himself.

### Message 2: they watched and liked it
> Hi <name>, thanks for watching, and glad the message for <gym> made sense.
> <one line on the specific thing they said>. Want me to set it up with you?
> Export your member list from <their software> as a CSV, and we do the rest
> together on a 15-minute screen share, or over email if you'd rather. The
> first week of Pro is €1, and if you cancel inside the week that's all you pay.
> Davide @casdey

### Message 2b: yes to the video, then silence (2 days after the video went out)
> Hi <name>, did you get a chance to watch? Even a "not for us" helps me a lot.
> And if the message I wrote for <gym> needs changing, tell me what and I'll
> rewrite it.
> Davide @casdey

### Message 2c: they watched and are not convinced
Do not argue. Acknowledge, then one question:
> Thanks, that's genuinely useful. What would it need to do to be worth it for <gym>?

Log the answer in `Notes`. This is the data the feedback trade was for.

### Message 3: the walk-away (3 days after the last unanswered message)
> Hi <name>, I'll leave it here so I'm not filling your inbox. If winning back
> ex-members moves up the list, just reply to this and I'll pick it up.
> Davide @casdey

Then stop. (The walk-away follow-up comes from the go-to-market notes in `CLAUDE.md`.)

### Answers to what they will ask

Facts only, each checked against the product on 2026-09-13.

| They say | The answer |
| --- | --- |
| "How much?" | Standard €99 a month (email win-back, 200 members), Pro €289 (adds WhatsApp, 2,000 members and the guarantee). Early gyms get 20% off for life, so €79.20 or €231.20. Frame it as the choice in "How to frame the choice on the reply" above. The first week of Pro is €1. |
| "We already use Mindbody / Glofox / LegitFit" | casdey doesn't replace it. Gym software is built around active members; casdey only deals with the ones who left. They export a CSV and casdey takes it from there. |
| "Will my members see casdey?" | No. Messages go out in the gym's name and replies go to the gym. With their own domain verified, the email comes from their own address. |
| "What about GDPR and member data?" | Member data stays in the EU (Ireland). The gym stays the controller and casdey is the processor, under the terms at `casdey.com/terms/processing`. Every email carries an unsubscribe link. |
| "Does it work? Who else uses it?" | Honest: casdey is new and they would be one of the first gyms. That is why Davide sets it up personally, and why Pro carries the guarantee: if it doesn't recover more than it cost in the first 30 days, they refund themselves from their billing page. |
| "I don't have time" | Their part is the export plus about ten minutes. Davide does the rest. |
| "We don't have many cancelled members" | The Free plan shows exactly how many, and roughly what they're worth a month, without paying anything. Signup is `https://www.casdey.com/login?mode=signup`, then "Continue on the Free plan". |
| "Send me more info" | Usually a polite stall. One line, then the next step: "The video is most of it. The quickest way to see it on <gym>'s own numbers is the Free plan: import your list and it shows who's gone quiet." |

**Paid-week mechanics, for when they ask:** €1 and a card at signup; on day 7
Pro begins unless they cancel from the trial panel on their casdey dashboard;
cancelling inside the week costs nothing beyond the €1.

## Drafting
Writing the actual email text is automated (runs in the Routine's Part A, and can also be run live). Each time this runs:
1. Read `Leads` for rows with `Status` blank/"Not contacted" and `Draft Status` blank (no draft yet).
2. For each, write `Draft Subject` and `Draft Body` following every current rule ("The cold email" as amended by "Current state", "Salutation rules", "Style rules"): explicit feedback framing, the general "brings back lapsed/cancelled members as paying clients, in the gym's own name, no manual chasing" description (not narrowed to class rebooking), **one CTA per the assigned variant (A permission / B free-to-try, ~50/50 by lead-# parity), no pay/features question**, the opt-out line, the pass-to-owner line if the address is a generic inbox, no offer and no waitlist link anywhere, correct salutation from `Owner/Manager`, no em dashes, ~60-90 words, varied phrasing so it doesn't read as mail-merged across the batch.
3. Set `Draft Status` → "Ready for review".
4. **Re-draft stale non-compliant drafts (added 2026-09-03):** in the same pass, for any lead with `Status` blank/"Not contacted" and `Draft Status` "Ready for review" whose current `Draft Body` still asks "what would you pay" / "how much would you pay" / "which features" / "what features" (pre-2026-09-02 copy), overwrite `Draft Subject` + `Draft Body` fresh to the current rules and leave `Draft Status` "Ready for review".
5. Also draft follow-ups: for leads whose `Follow-up Due Date` (FU1) or `Follow-up 2 Due Date` (FU2) has passed with no reply and no opt-out (per `Send Log`/`Reply?`), write the follow-up into the same `Draft Subject`/`Draft Body` columns (same content rules as the cold email, referencing the first email since Resend/email doesn't thread like a real reply), `Draft Status` → "Ready for review". FU2 is the lightest touch ("last one from me, no worries if the timing's off").
6. **Anti-duplicate rule (there's no human review to catch this now):** no two drafts anywhere in `Leads` may share a byte-identical `Draft Subject` or `Draft Body`. Vary opener / middle sentence / closing line per lead. After drafting, scan for identical subjects or bodies and regenerate the collisions before finishing. (The follow-up template rotation produced byte-identical drafts on 2026-08-27 and 2026-08-28, caught both times only by the human sample — that safety net is gone.)
7. Never touch `Send Log` during drafting. Drafting is a distinct step from Part B sending.

## Fully automated sending
As of 2026-08-28 there is **no human approval gate** (see the banner under "Why this replaced the dental skill"). Part B runs automatically on every Routine fire, right after Part A. The Routine's own prompt is authoritative on the exact mechanics; this is the summary (**"50" is now "75" everywhere below — raised 2026-09-02**):
1. **Assemble exactly 75 via the RESERVED-FLOOR SPLIT** (2026-09-06; fewer only if BOTH queues are genuinely exhausted, and never source more mid-run to top up):
   (a) **Follow-ups, capped at 50:** due follow-ups (FU1 and FU2 together, oldest due-date first), take at most 50. Any due follow-up beyond the 50th rolls to the next run(s), still oldest-first, never expires.
   (b) **First-touch fills the rest of the 75:** (75 − follow-ups sent) drafts with `Draft Status` = "Ready for review" and `Status` blank/"Not contacted", ordered by lever 1 (clean owner name AND/OR non-generic direct email first, then generic-inbox leads), oldest lead # first, A/B by lead-# parity. Because (a) is capped at 50, this is **≥25** whenever 25+ eligible drafts exist.
   (c) **Auto-balance:** if fewer than 50 follow-ups are due, first-touch takes the freed slots (up to 75); if fewer than 25 first-touch drafts exist, follow-ups may take more than 50 (up to 75). The split only bites while both queues are backed up. Do not move the 50/25 without re-checking the math — see the "Current state" bullet.
2. **Hard pre-send gates — skip (do not send, leave `Draft Status` "Ready for review", record as skipped in the report) any draft that:** exceeds ~130 words; contains an em dash; contains any offer term (free week, Free plan, lifetime discount, a price/number-with-currency, the guarantee, scarcity) or the waitlist link — **note: variant B's light "free to try / set it up free" hook is ALLOWED and must not be skipped**; **asks "what would you pay" / "how much would you pay" / "which features" / "what features" (old copy, added as a gate 2026-09-03)**; has an empty subject or body; has a recipient already in `Send Log`; belongs to a lead whose `Status` is "Dead" or `Reply?` is "Unsubscribed"; or has a body byte-identical to another draft already in the day's batch (keep the first, skip the rest). If a first-touch draft is skipped, pull the next eligible one so the batch still targets 75 genuine sends.
3. Send each surviving email via `scripts/resend-send.js`. **Never more than 75 in a run, ever.**
4. Log every actual send immediately to `Send Log`: lead #, recipient, date, salutation type, opening line used, follow-up due date (4-5 calendar days out, skip to the next weekday if it lands on a weekend), the `Resend Email ID`, and `Variant` (`A`/`B` for first-touch, `FU1`/`FU2` for a follow-up). On an FU1 send also set that lead's `Follow-up 2 Due Date` (4-5 days out, next weekday); on an FU2 send set `Follow-up 2 Sent (Y/N)` → "Y".
5. Update `Leads`: `Status` → "Contacted", `Date Contacted`, `Draft Status` → "Sent"; for a follow-up also set the relevant `Follow-up Sent` flag.
6. If Step 0 (reply detection) failed on this run (missing creds or egress block), **still send the full 75** — Davide's explicit call, 2026-08-28. The accepted downside: any opt-out received since the last successful Step 0 is not reprocessed that day, so a follow-up could reach someone who asked to stop. (Step 0's Zoho egress has run clean since 2026-09-02, so this path is currently dormant.)

`Draft Status` = "Ready for review" is now a legacy label meaning only "drafted, not yet sent" — nobody reviews it.

## Follow-up policy
**Two follow-ups per lead as of 2026-09-02, then stop forever** (was one).

**Eligibility rewritten 2026-09-13 — count rows, never trust the date/flag columns alone.** The original design (below, kept for what NOT to do) computed eligibility from `Follow-up Due Date` / `Follow-up Sent (Y/N)` / `Follow-up 2 Due Date` / `Follow-up 2 Sent (Y/N)`, columns living on individual `Send Log` rows (one row per send). On 2026-09-11 that let 150 already-fully-sequenced leads look eligible again (111 got a duplicate FU2, 39 a duplicate FU1), because one of those fields had gone stale on the wrong row and the one-time backfill clause re-armed it. The current rule, live in both this file and the Routine's own prompt:

For every Lead #, look at **all** existing `Send Log` rows for that lead and count how many have `Variant` = `FU1` and how many have `Variant` = `FU2` (there is always at most one first-touch row, `Variant` `A` or `B`):
- **FU1 eligible** only if: zero existing FU1 rows for that lead, the first-touch row's `Date Sent` is 4+ calendar days ago (next weekday if that lands on a weekend), and `Reply?` is blank and `Status` isn't "Dead"/"Unsubscribed".
- **FU2 eligible** only if: exactly one existing FU1 row and zero FU2 rows, that FU1 row's `Date Sent` is 4+ calendar days ago (same weekend rule), and `Reply?`/`Status` clear as above.
- A lead with **both** an FU1 row and an FU2 row already logged is never eligible again, full stop, regardless of what any of the four date/flag columns say on any row for that lead.
- `Follow-up Due Date` / `Follow-up Sent (Y/N)` / `Follow-up 2 Due Date` / `Follow-up 2 Sent (Y/N)` are now **display-only**: still written on every send so a human glancing at the sheet sees a next-due date, but never read back to decide anything. Counting rows is the only source of truth.
- **The one-time FU2 backfill clause is retired.** It's structurally unnecessary now: a legacy lead with one logged FU1 row and no FU2 row is picked up correctly by the ordinary FU2 rule above the moment 4+ days have passed, no separate backfill pass needed, and there's no blank-field state left for a backfill to (mis)re-arm.
- Never a third follow-up, enforced structurally (a lead with both rows present can't become eligible again, not by a counter or flag that could reset).

**What NOT to do (the pre-2026-09-13 design, kept as the failure case, not a valid alternative):** do not decide FU1/FU2 eligibility by reading `Follow-up Due Date`/`Follow-up Sent (Y/N)`/`Follow-up 2 Due Date`/`Follow-up 2 Sent (Y/N)` as the primary signal, and do not re-arm `Follow-up 2 Due Date` for any lead found with a blank one without first confirming via row-count that it hasn't already received both follow-ups. Both of those are exactly what produced the 2026-09-11 duplicate-send bug.

Same content rules as the cold email (no price/guarantee/link, varied, short). Drafted automatically per "Drafting" above, sent automatically per "Fully automated sending" (no review gate since 2026-08-28); first-touch and follow-ups now draw from separate queues (2026-09-07), so this eligibility rewrite doesn't interact with the first-touch volume at all.

## Reporting
As of 2026-08-28, every automated run ends with **Step R** — a report posted in the Routine session, plus one short push notification. Two Markdown tables:
- **Table 1, today's batch:** one row per send attempt — Lead #, Gym/Studio, City, Recipient, Type (first-touch A / first-touch B / FU1 / FU2), Result (`Resend Email ID` on success, or `SKIPPED - <reason>` for a gated draft). Skipped drafts appear as their own rows.
- **Table 2, all-time** (recomputed fresh from `Send Log` + `Leads` each run): Total emails sent, First-touch sent, Follow-ups sent, Genuine replies, Engaged-lead rate (replies ÷ total sent, %), Opt-outs, Leads in sheet, Drafts still "Ready for review". Plus a one-line by-date send breakdown.
- **Variant block (added 2026-09-02):** join `Send Log` `Variant` to `Leads` `Reply?` and report, first-touch only — Variant A sent / A genuine replies / A reply-rate %, same for B, plus a one-line read on which is winning ("too early to tell" until each has a meaningful sample).
- Then a plain-English paragraph: today's count, skips and why, engaged-lead rate vs the 3% target, whether Step 0 ran clean or was blocked, and how many review-ready drafts remain (flagged if under 75).
- Then exactly ONE `PushNotification`, under ~30 words. **The call needs BOTH fields: `message` (plain text, no tags) and `status` set to the literal string `"proactive"`** — a call missing `status` fails with an `InputValidationError` (seen intermittently on 2026-09-03 runs, which then self-recovered on a retry).
The report lands only in the session transcript + push (Davide's call, 2026-08-28 — not written to a sheet tab, not emailed).

## Reply detection (drafting since 2026-09-13; sending still requires Davide's approval, see below)
Identical mechanism to the dental skill: read `davide@casdey.com`'s inbox via the Zoho Mail API (`ZOHO_*` env vars, `GET /api/accounts/{ZOHO_ACCOUNT_ID}/messages/view` or equivalent against `ZOHO_API_DOMAIN`), not a Gmail connector, not Resend (Resend has no inbox, replies bypass it entirely via `reply_to`). **Gotcha**: `ZOHO_API_DOMAIN` and `ZOHO_ACCOUNTS_DOMAIN` are stored as full URLs already (e.g. `https://mail.zoho.eu`), don't prepend another `https://` when building a request URL, that silently breaks DNS resolution rather than erroring clearly (hit and fixed 2026-08-25).

**Matching broadened 2026-09-13 — sender-address match is no longer the only path.** The original design only matched an inbox message to a lead if its sender address exactly equalled a `Send Log` Recipient Email. That silently missed a lead who replies from a different address than the one casdey emailed, confirmed live 2026-09-13 when lead #740 (Sunset Gym, Genève) replied within an hour, but from a personal address rather than the gym's own inbox. Step 0 now also tries a **subject fallback**: strip a leading "Re:"/"Fwd:" from the inbound subject and compare it to the two fixed first-touch subject lines (`quick question about your lapsed members` / `win back the members who cancelled`) used verbatim since the 2026-09-08 subject test. A match there, cross-referenced against `Send Log` rows carrying that Subject Variant with no `Reply?` yet, identifies the lead even when the sender address differs. If exactly one candidate fits, it's treated as a match (noted in that lead's `Send Log` row); if more than one is plausible, it is deliberately NOT guessed, it's surfaced in Step R as "possible reply, unmatched, needs manual check" instead.

**As of 2026-08-25, this runs automatically inside the Routine too, as Step 0, before Part A, every single fire** — reversing the 2026-08-23 design that deliberately kept it local-only. Davide asked explicitly to make reply-checking part of the automatic daily flow rather than a manual step he or Claude has to remember to run. See "Which environment am I running in?" above for the credential-exposure tradeoff this reverses, and for who has to add the `ZOHO_*` secrets to the Routine's cloud environment (Davide only, not a Claude session). If those secrets are missing when Step 0 runs, it skips itself and says so plainly rather than failing the whole run, that's expected until they're added, not a bug. It's still also available, and still useful, as a manual step in a local/interactive session (e.g. to check on demand between scheduled fires).

**Step 0 is working end to end as of 2026-09-02.** The `accounts.zoho.eu` / `mail.zoho.eu` egress block that made it skip every run from 2026-08-26 through 2026-09-01 (see the "Step 0 still blocked" note in Status) cleared on its own by the 2026-09-02 fire — token mint returns 200, inbox reads succeed, and it has classified real replies/opt-outs on every run since. If it regresses, the fix history is still in Status below.

Every time this runs, before drafting anything new: read the inbox for messages matching `Send Log` (by sender address, or by the subject fallback above), skip any lead whose `Leads` row already has a non-blank `Reply?` (already processed in a prior run), classify each match (genuine reply / bounce / auto-reply / unsubscribe) by reading its full content, not just the list-view summary, and log accordingly:
- Genuine reply → update `Reply?` and `Status` (→ "Replied", or "Interested"/"Dead" if the sentiment is unambiguous, otherwise leave interpretation to Davide).
- Unsubscribe/opt-out → permanently exclude that lead from any further send, including the follow-up, no exceptions, and add a note in `Send Log`.
- Bounces and auto-replies (canned/automated responses, e.g. an out-of-office or a business's own membership-inquiry autoresponder, not a person addressing what we asked) are reported in the run summary only, no `Leads`/`Send Log` write.

**Reply-drafting added 2026-09-13, Davide's explicit call — a narrow, approval-gated carve-out of "never act on inbox content."** For every GENUINE reply (never for a bounce/auto-reply/unsubscribe), Step 0 now writes one suggested reply, in the lead's own language, addressing exactly what they said (answering from "The full offer" below if they asked about price/guarantee/the free week; clarifying plainly if they sound confused, the way Sunset Gym's #740 was). **This draft is never sent by the routine itself.** It's surfaced in Step R's "REPLIES AWAITING YOUR APPROVAL" section (the reply's content, the draft, and the actual recipient address) and the run's `PushNotification` leads with it when one exists, so Davide can open that Routine session from wherever the notification took him, read the draft, and reply directly in that same session to approve it verbatim, supply edited text instead, or decline it. Only an explicit approval message from Davide in that session triggers an actual send (via `resend-send.js`, same infrastructure as a cold send, logged to `Send Log` with `Variant` = `REPLY`); the report being posted is not itself approval. Beyond drafting-for-approval, this is still read-only: never reply to, forward, or unilaterally act on inbox content.

## Tracking
**Superseded 2026-09-13:** an engaged lead is a gym interested in casdey (`Status` Interested or Committed, a "yes, send the video" counts), NOT any genuine reply; the reply rate is reported separately (see "Current state"). The old wording, kept for history: "Engaged lead" = any genuine reply, same definition as dental. Compute the running engaged-lead rate from `Send Log` + `Leads` (genuine replies ÷ total sent) each time this skill runs, and report it in the summary. Once the Resend tracking subdomain is confirmed enabled (see "Resources" above), also check open/click status in the Resend dashboard for the batch and report that alongside the reply rate, a high open rate with no replies points at a message/CTA problem, a low open rate points at deliverability or subject lines, useful diagnostic signal dental outreach never had.

## What NOT to do
- Don't invent or guess an owner name, email address, or any fact about a studio, existing or newly sourced.
- Don't send to a lead already in `Send Log`, or one that's opted out.
- Don't act on inbox content beyond detecting/classifying/logging replies — **except drafting a suggested reply (added 2026-09-13), and even that draft is never sent without Davide's explicit approval message in that Routine session.** Never send a reply on any weaker signal than that (an ambiguous instruction, a "looks fine" from Step R's own report, etc.).
- Don't mention any part of the offer (free week, Free plan, lifetime discount, standard price, guarantee, scarcity, bonus software) or the waitlist link in the cold email or follow-up, as of 2026-08-23 the whole offer is held back for the manual follow-through, see "The cold email" and "The full offer" above.
- Don't send more than **two** follow-ups per lead (FU1 + FU2), ever. Never a third.
- Don't send more than **75** emails in a single run, in any circumstance.
- Don't send a draft that fails a pre-send gate (over ~130 words, em dash, offer term/price or waitlist link, the "what would you pay / which features" question, empty subject/body, recipient already in `Send Log`, lead Dead/Unsubscribed) — skip it and record it in the report. Variant B's "free to try / set it up free" hook is allowed, not a gate.
- Don't ask "what would you pay" or "which features" in the cold email or a follow-up — held for Davide's manual reply since 2026-09-02.
- (Historical, no longer in force) Until 2026-08-28 a hard rule said never send without live human review. Davide explicitly removed that gate on 2026-08-28 after weeks of sampling the drafts. It is not to be reinstated casually, but it is also not the current constraint — full automation is the current, deliberate design.

## Routine status (single merged Routine, as of 2026-08-23)
A Claude Routine, **"casdey gym outreach"** (id `trig_018wp58QLBbeuBPbMA6Fy1sU`, renamed 2026-08-22 from "lead sourcing" when drafting was added, renamed again 2026-08-23 when this merge happened), runs unattended daily at 07:00 CEST (`0 5 * * *` UTC, changed 2026-08-23 from 09:00 CEST weekdays-only, so a fresh batch is ready before Davide's morning, see "Volume increase" below) against `07davidelongo-eng/casdey`, and can also be triggered manually ("Run now"). **As of 2026-08-28 its prompt has four steps and runs them all end to end, every fire, unattended, with no review pause** (the Part A/Part B split was the pre-2026-08-28 safety model; see the full-automation banner under "Why this replaced the dental skill"):
- **Step 0 — reply detection.** Added 2026-08-25 (see "Reply detection" above). Runs first, every fire, read-only. As of 2026-08-28, if it fails (missing creds or the `accounts.zoho.eu` egress block), it logs why and the run continues to Part B anyway — a Step 0 failure no longer reduces or pauses sending. Egress fixed and running clean since 2026-09-02.
- **Part A — sourcing + drafting.** Runs every fire. Skips sourcing (logs why) if `Leads` already has 150+ unsent rows; otherwise sources **up to 90** new leads (~40% CrossFit/boutique, ~60% general, owner-first), then drafts for every undrafted lead, re-drafts any stale "what would you pay" drafts, and drafts every due follow-up (FU1/FU2).
- **Part B — sending (no approval gate).** Runs automatically right after Part A, every fire. Assembles **exactly 75** via the reserved-floor split (≤50 due follow-ups oldest-first, then first-touch owner-first/by-lead-# fills to 75 so first-touch ≥25; auto-balances when either queue is short — see "Fully automated sending" step 1), applies the hard pre-send gates, sends via `resend-send.js`, logs to `Send Log` incl. `Variant`, updates `Leads`. Never more than 75 per run.
- **Step R — report.** Posts the two-table report + variant block + one push notification (`message` + `status: "proactive"`) per "Reporting" above.

**The 75/day change and the 2026-09-02 copy rewrite** (two variants, no pay/features question, owner-first, two follow-ups, the new `Send Log` columns) were made to the Routine's prompt via the RemoteTrigger API on 2026-09-02; the 2026-09-03 patch (re-draft clause, the "what would you pay" pre-send gate, the `PushNotification` two-field reminder) went in the same way. Schedule unchanged: `0 5 * * *` UTC (07:00 CEST daily).

**Second Routine — "casdey gym IG outreach"** (`trig_01La223qWjwxoumG4z8gssL8`, `0 6 * * *` UTC / 08:00 CEST daily, same repo + branch, same cloud environment). Semi-automated Instagram. Each fire: **Step 0** provisions the six follow-up columns and sweeps every `IG Outreach` row for follow-ups due (see below); then, if the unsent-first-DM buffer is under 40, **Part A** sources up to 10 IG-reachable gym leads/run (prioritising gyms already emailed with no reply, then not-yet-emailed, then web-sourced) and **Part B** drafts one first DM each (standalone or "I also emailed you" variant); then **Step R** reports, including a "follow-ups ready to send by hand" table. **It never sends and never reads Instagram** — Davide sends every DM (first touch, FU1, FU2) by hand from casdey's IG account and marks the sheet, because Instagram has no cold-DM API and botting gets accounts banned. It enforces the same cross-channel exclusion as the email routine (Dead / opted-out / already-engaged leads off-limits on IG too), only ever reads the `Leads` tab, and only writes the `IG Outreach` tab (new lead rows plus follow-up due-dates/drafts). Its full spec lives only in that Routine's prompt; there is no separate skill file for it.

**IG follow-up sequence (added 2026-09-07).** Two follow-ups per lead, FU1 then FU2, then stop — same policy as email, 4-5 calendar days apart (next weekday if it lands on a weekend). Six columns on the `IG Outreach` tab, `N`–`S`, routine-provisioned (Step 0a) if missing: `FU1 Due`, `FU1 Draft`, `FU1 Sent?`, `FU2 Due`, `FU2 Draft`, `FU2 Sent?`. Mechanics (Step 0b, runs every fire regardless of the buffer cap): when Davide marks the first DM `Status`=Sent + `Date Sent`, the routine sets `FU1 Due` = that + 4-5 days; once `FU1 Due` passes with `Reply?` still blank, it writes `FU1 Draft` (a 25-45 word nudge). Davide sends it and puts the date in `FU1 Sent?`, which arms `FU2 Due`; same again for `FU2 Draft` (lightest touch, clearly the last). The routine only ever writes the `*Due`/`*Draft` cells and only fills a blank draft; **it can't verify a send or see a reply**, so the sequence is only correct if Davide keeps `Status`/`Date Sent`/`FU1 Sent?`/`Reply?` current. Any `Reply?` value, or the gym going off-limits on email, halts further drafts for that row.

**Volume increase, 2026-08-23 (history — superseded 2026-09-02, target is now 75/day, see "Current state").** Davide's explicit call at the time: target moved from ~5 reviewed/sent per day to **~50 per day**, to reach ~350 sends over the coming week and get a real read on how this niche performs. Same review-gated system as before, no autonomous sending, this only changes batch size. Sourcing (up to 60/run) and the skip threshold (150+ unsent) above were both raised accordingly so there's always enough drafted inventory to support a 50-a-day review pace without Part A becoming the bottleneck. **Schedule also changed, same day, twice:** the Routine originally fired weekdays only at 09:00 CEST (250/week at 50/day, short of the 350 target), so Davide asked for weekend runs added; the cron became `0 7 * * *` (every day, 09:00 CEST), giving 7 × 50 = 350/week exactly. Then Davide asked to move the fire time earlier, to 07:00 CEST, so a fresh batch is ready before he wakes up rather than around when he does; the cron is now `0 5 * * *` UTC (07:00 CEST). Both changes, and the Routine's own prompt text (which used to say "weekday schedule"), were updated via the RemoteTrigger API, not just this file.

**History of this Routine, in order:**
- **2026-08-22, created**: sourcing + drafting only, environment `casdey-Outreach` (id `env_016U3DW3QNJS1t7AyukzK5RN`), deliberately no `RESEND_API_KEY` at all, the hard version of the safety gate, see the enforcement-mechanism note above.
- **2026-08-23, briefly split further**: Davide wanted to do the review-and-send step inside the Routines UI instead of a local chat session, so a second Routine ("casdey gym outreach — review & send") was created with its own new, minimal environment `casdey-Outreach-Send` (id `env_01Wmn5uCu7qGFvpVuiacSs3z`: `GOOGLE_SERVICE_ACCOUNT_JSON`, `RESEND_API_KEY`, `CASDEY_OUTREACH_SENDING_ADDRESS`, deliberately no `ZOHO_*`, on-demand only, no schedule).
- **2026-08-23, merged back**: Davide found managing two Routines annoying and asked to merge them into one, after being told explicitly what that trades away (see the enforcement-mechanism note above). The original Routine was repointed at the new `casdey-Outreach-Send` environment (dropping `casdey-Outreach`, which is now unused/orphaned, still exists, nothing points to it), its prompt rewritten into the Part A / Part B structure above, and the second Routine was deleted (it had no runs yet, nothing lost). One Routine, one environment, from this point on.

**Gotcha found the same day: resuming an old session does NOT pick up a repoint.** A cloud Routine session's sandbox binds to whatever `environment_id` the trigger had *at the moment that session was created*, changing the trigger's environment afterward (like the merge above) only affects *future* runs, not sessions already open. Davide resumed the pre-merge sourcing session (`cse_01NskWCwnz5YSnB1rY1e1v68`, created 2026-08-22 21:09, before the 2026-08-23 10:37 repoint) from the Routines UI and asked it to review+send; it still only had the old `casdey-Outreach` box's vars (`GOOGLE_SERVICE_ACCOUNT_JSON` + leftover `ZOHO_*`, no `RESEND_API_KEY`), so Part B failed outright, correctly, rather than silently. **The fix is always the same: don't resume an old chat after changing a Routine's environment or credentials, click "Run now" for a genuinely fresh session instead.** That old session should be treated as dead and left alone.

**Second gotcha found the same day: a fresh cloud session can review live but can't finish the send.** A genuinely fresh run (`cse_01WupRUgA1mD7PviE7UUR5kL`, triggered 2026-08-23 11:45 via the API) correctly picked up the merged environment, ran Part A, and correctly entered Part B when Davide replied live. But when it actually tried to act, both real actions failed on infrastructure walls specific to that cloud environment, not on anything wrong with the review itself: `git push` was denied ("Claude doesn't have GitHub access... for your organization", the Claude GitHub App integration on this repo is read-only for cloud sessions) and calling Resend was rejected at the network egress gateway (`403` on `api.resend.com`, independent of whether the API key is valid). The first 5 real sends that day (see "Status" below) were finished from a local session as the workaround.

- **Resend egress block: fixed 2026-08-23.** `casdey-Outreach-Send`'s Network access was "Trusted" (verified package sources only, not arbitrary APIs), which is what silently blocked `api.resend.com` and also explains several `WebFetch` "EGRESS_BLOCKED" errors on individual gym websites during sourcing that same run (tolerated via `WebSearch` fallback, so left alone). Changed to **Custom** with `api.resend.com` added to Allowed domains (default package-manager list kept). To edit: in the environment picker (composer or a Routine's Edit dialog), hover the environment name, a small gear icon appears, click it for "Update cloud environment", there's no other path to this found in the UI. Not yet re-verified with an actual cloud-session send since the fix, first real cloud-originated send will be the confirmation.
- **GitHub App write access: still open, unfixed.** Cloud sessions on this repo can only read via the Claude GitHub App integration, so a cloud session still can't `git push` on its own. Needs Davide (org admin) to grant "Contents: Read and write" at https://github.com/apps/claude/installations/select_target, or reconnect GitHub with broader scope at https://claude.ai/customize/connectors?auth_start=github&auth_start_force=1. Until then, any repo push from a cloud session still needs to be finished from a local/interactive session, same as the Resend workaround above.

**Branch note (important):** this skill's own files live on a dedicated branch, `gym-outreach-automation`, not `main`. `main` still carries the unpushed gym SaaS rebuild (commit `25af6aa`) that Davide deliberately kept local, not deployed, pushing this skill to `main` would drag that rebuild into production too, which wasn't the ask. So `gym-outreach-automation` was branched off the local `main` (meaning it *does* contain the SaaS rebuild in its own history, just not on `main` itself, and pushing a non-`main` branch doesn't trigger a Vercel deploy either way) and only today's outreach-specific commit was pushed. The routine's `job_config.ccr.session_context.sources[0].git_repository` is pinned to `revision: "gym-outreach-automation"`, and its own instructions also unconditionally `git checkout gym-outreach-automation` as a first step (belt and suspenders, in case the default clone ever ignores the revision pin). **If this skill is ever merged/rebased onto `main` for real** (i.e., once the gym SaaS rebuild itself gets pushed), update the routine to drop the revision pin and the explicit checkout step, both become unnecessary at that point.

**First real run, 2026-08-22 (manually triggered test, not the scheduled fire): success.** `Leads` was empty, so it sourced rather than skipped: 14 real, independent gyms/studios across UK + EU (Manchester, Bristol, Edinburgh x2, Cardiff, Leeds, Brighton, Dublin, Amsterdam x2, Barcelona x2, Lisbon, Berlin), every email confirmed on-site, no invented facts, only one owner name used (a source that actually named one). Scope held: `Send Log` untouched, nothing drafted or sent, nothing committed/pushed. Caught and self-corrected the phone-formatting bug now documented above before finishing. Two earlier attempts before this one failed for infrastructure reasons, not sourcing-logic reasons, both fixed same day: the first ran before this skill existed on any pushed branch at all (see "Branch note" above); the routine's cloud environment also came pre-loaded with old dental-era `ZOHO_*` env vars sitting in plaintext (same box used for `GOOGLE_SERVICE_ACCOUNT_JSON`, "Environment variables" in the environment's settings, not a real secrets vault, that's just how this UI works, worth knowing before adding anything sensitive there). This run happened under the pre-merge, sourcing-only version of the Routine described above.

## Status
Built 2026-08-22, merged into a single Routine 2026-08-23 (see "Routine status" above, including the enforcement-mechanism tradeoff Davide knowingly accepted). Sourcing confirmed working via a real test run (14 leads, 2026-08-22, pre-merge). Drafting confirmed working via the scheduled Routine fire on 2026-08-23: Part A sourced 15 more leads (44 total) and drafted all of them, ending with the Part A summary as designed; Davide then replied live in that same session, which correctly triggered Part B for the first time. 44 leads sourced as of 2026-08-23.

**Cold email style rewritten 2026-08-23, live during Part B's first real review batch.** Davide reviewed the first 5 drafts (still under the old offer-led copy) and judged them too salesy, and objected to the offer being shown in the cold email at all. He supplied his own reference script (plain feedback ask, no offer, no link, asks what they'd pay and what features they'd want) and asked for this to be a permanent change to how this skill drafts, not a one-batch edit. "The cold email" and "The full offer" sections above were rewritten accordingly (offer and waitlist link now fully held back for the manual follow-through; the "books into a class" language generalized to cover both membership win-back and class rebooking, since casdey does both). All 44 queued drafts were regenerated to the new style before any were sent. `CASDEY_OUTREACH_SENDING_ADDRESS` set both in `.env.local` and in the `casdey-Outreach-Send` cloud environment, Resend tracking subdomain verified.

**First 5 real sends, 2026-08-23.** Leads #1-5 (Muscle Manor, Ministry of Fitness, Perpetua Fitness, CrossFit Skirmish, Energy Gym) were reviewed and approved live by Davide in the cloud session, then actually sent from a local/interactive session (per the "Second gotcha" above, the cloud session couldn't finish the send itself). All 5 sent successfully via Resend, logged to `Send Log` with their Resend Email IDs, and the corresponding `Leads` rows updated (`Status` → "Contacted", `Date Contacted` → 2026-08-23, `Draft Status` → "Sent"). Follow-up due 2026-08-27 for all 5 if no reply. 39 leads remain "Ready for review", not yet sent.

**Volume increased to ~50/day, 2026-08-23 (same day, Davide's explicit call).** After the first 5-email batch, Davide decided the go-to-market plan's original ~5/day pace is too slow to read how the gym niche is performing, and moved the target to ~50 reviewed/sent per day, aiming for ~350 over the coming week. Same review-gated system, nothing autonomous changed, only batch size: "Routine status" above now targets ~50 for Part B review batches, and Part A's sourcing cap was raised from 15/run to 60/run (skip threshold raised from 50+ to 150+ unsent rows) so there's enough drafted inventory to support it. Only 39 drafted leads exist as of this change, well short of a 50-lead batch, so the next run needs Part A to source a fresh batch before Part B has enough to review. The weekday-only schedule (Mon-Fri) capped out at 250/week at this pace, not 350; flagged to Davide, who asked for weekend runs to close the gap (same day) — the cron became `0 7 * * *` (every day, 09:00 CEST), giving 7 × 50 = 350/week exactly. Davide then asked to move the fire time earlier still, to 07:00 CEST, so leads are ready before he wakes up rather than around 9am; the cron is now `0 5 * * *` UTC (07:00 CEST), see "Routine status" above.

**First full 50-lead day, 2026-08-24, and the sample-review process it established.** The scheduled Routine fired unattended that morning, ran Part A (sourced 56 new real leads across new UK/Ireland/NL/BE/DE/ES/PT/other-EU cities via parallel research, drafted all of them, 100 leads total in the sheet, 95 "Ready for review"), and correctly stopped there per the Part A/Part B split, nobody was watching. Davide later opened a live session and asked to review a **casual spread of 6-7 drafts rather than all ~50**, so he could sanity-check the batch without reading every one; he was shown 7 (lead #10, #24, #38, #52, #67, #81, #95, chosen to span old/new sourcing batches and different countries). He approved the tone/content as-is ("really close to what I wrote... it's perfect," no edits requested) and gave two standing instructions, both now reflected above: (1) **this sample-based review (5-7 casually picked, not all ~50) is the permanent default review process** for the daily 50-lead batch, not a one-off, see "Review-gated sending" above; (2) **exactly 50 must be sent per day, not more, not less.** Leads #6-#55 (the next 50 in queue after the first 5) were then sent, all 50/50 successful via Resend, logged to `Send Log` and `Leads` updated (`Status` → "Contacted", `Draft Status` → "Sent", `Date Contacted` → 2026-08-24, follow-up due 2026-08-28). Running total: **55 sent** (5 on 2026-08-23 + 50 on 2026-08-24), **45 leads remain "Ready for review"** heading into the next day, short of another full 50-batch, so the next run's Part A needs to source/draft more before Part B can hit 50 again. One sent lead (#38, Fitness UP) is a **Chain**, not independent, flagged to Davide inline but not excluded, since the sourcing preference for independents is an ordering preference, not a hard filter (see "Lead sourcing" above) and Davide didn't ask for it to be skipped.

**Note on this update itself:** this SKILL.md edit was made from a cloud Routine session (`casdey-Outreach-Send`), which per "GitHub App write access: still open, unfixed" above can only read this repo, not push. The edit exists in this session's working tree only; someone needs to commit and push it from a local/interactive session (or fix the GitHub App's write scope) for it to actually persist past this session, otherwise the next scheduled Routine fire (a fresh clone) won't see it.

**Third day of sends, 2026-08-25, running total 105.** `Send Log` shows leads #56-105 sent 2026-08-25 (50 more, same review-gated flow), bringing the running total to **105 sent** across three days (5 on 08-23, 50 on 08-24, 50 on 08-25).

**Reply detection run manually, 2026-08-25, and moved into the automatic daily flow the same day.** A local/interactive session checked `davide@casdey.com`'s inbox by hand (the Routine had no `ZOHO_*` credentials at this point, see "Reply detection" above) against all 105 sends so far: 2 inbox messages matched a known recipient. Lead #52 (The Box Portsmouth) was a genuine reply, an opt-out request, already fully handled by Davide by hand before this check ran (`Leads` Status → Dead, Reply? → Unsubscribed; `Send Log` note added) — verified correct, nothing further needed. Lead #11 (UFit Fitness, `membership@ufit-fitness.com`) was a canned auto-responder from their own membership-cancellation inbox, not a person replying to the actual ask, classified as auto-reply, no sheet write per spec. No new genuine replies, no bounces, no new opt-outs. Engaged-lead rate: 0/105 so far, though the one real signal (an opt-out) is a decline rather than silence, not the same zero-signal picture the dental batch had.

Davide then asked, explicitly, for reply-checking to become part of the Routine's automatic daily flow rather than a manual step someone has to remember to run. This is a direct reversal of the 2026-08-23 design (which kept `ZOHO_*` out of the Routine's environment specifically so it could never touch the real inbox), made knowingly, the same kind of tradeoff already accepted once for `RESEND_API_KEY`. Done same day: the Routine's prompt was updated via the RemoteTrigger API to add **Step 0 — reply detection**, running before Part A on every fire, unattended or manual (see "Routine status" above for the mechanics, "Reply detection" above for the full spec); this SKILL.md was updated to match.

**Confirmed done, 2026-08-25: Davide added the `ZOHO_*` secrets and the Zoho network allowlist entries to `casdey-Outreach-Send` by hand.** Via the environment's gear icon → "Update cloud environment" (a local session navigated the browser there but deliberately did not type any credential values in, that part is Davide-only, entering API keys/tokens into any settings field is off-limits for Claude to do directly, even with local read access to the real values in `.env`). Two boxes on that panel, easy to mix up: "Environment variables" (`.env` format, needs `KEY=value`) got the six `ZOHO_CLIENT_ID` / `ZOHO_CLIENT_SECRET` / `ZOHO_REFRESH_TOKEN` / `ZOHO_ACCOUNT_ID` / `ZOHO_API_DOMAIN` / `ZOHO_ACCOUNTS_DOMAIN` lines; "Allowed domains" (bare hostnames, no `=`, no `https://`) got `accounts.zoho.eu` and `mail.zoho.eu` added alongside the existing `api.resend.com`, needed because this environment's Network access is "Custom" (see the Resend egress-block fix above), so Zoho's hosts needed the same explicit allowlisting Resend's did. Confirmed live in the browser (both boxes screenshotted with the new values present) and saved. Step 0 is expected to actually run, not skip, starting with the next fresh session against this environment, currently the scheduled 2026-08-26 07:00 CEST fire (today's 2026-08-25 07:14 fire already happened before this was added, so it ran without Step 0, same as every day before now).

**Third gotcha found the same day: an already-running cloud session does not pick up a live secrets/allowlist edit either, same root cause as the 2026-08-23 repoint gotcha above, now confirmed to extend past just `environment_id` changes.** The cloud Routine session that had been reviewing/discussing this very change (still open, from before the `ZOHO_*` secrets and domain allowlist were saved) does not gain them retroactively, its sandbox's process env and egress policy were both fixed at that session's own start, exactly like the pre-merge sourcing session in the 2026-08-23 gotcha couldn't see a repointed `environment_id` either. Any prompt or summary that session produces about Step 0 "now working" should be treated the same way as before: not proof, since that same session still cannot reach `accounts.zoho.eu`/`mail.zoho.eu` or read the new vars no matter what it reports. **The fix is the same fix as before, just generalized**: don't trust an already-open cloud session to reflect any credential or environment-settings change made after it started, whether that's a repoint (2026-08-23) or an in-place secrets/allowlist edit on the same environment (2026-08-25). Click "Run now" for a genuinely fresh session, or just wait for the next scheduled fire, either way provisions a new sandbox that reads the environment's current state.

**Step 0 still blocked on the Zoho egress, confirmed 2026-08-26 and 2026-08-27.** Both mornings' fresh scheduled runs reported the `ZOHO_*` env vars present but `accounts.zoho.eu` rejected at the network egress gateway (`403 on CONNECT`, `connect_rejected` / policy denial). `api.resend.com` egress works fine from the same fresh sessions (Part B sent 50/day both days), which proves the Custom-allowlist mechanism itself works — the Zoho hosts just aren't in the effective list. Diagnosis: the 2026-08-25 "Allowed domains" edit for `accounts.zoho.eu` / `mail.zoho.eu` did not actually persist (the env-vars box and the allowed-domains box save independently; the vars landed, the domains didn't), or the entries went in with a stray scheme/slash/whitespace and were dropped on normalize. **Fix (Davide only, Claude can't type into settings fields):** reopen `casdey-Outreach-Send` → gear → "Update cloud environment" → "Allowed domains", confirm `accounts.zoho.eu` and `mail.zoho.eu` are each present as bare hostnames alongside `api.resend.com`, delete+re-add them cleanly, save that section explicitly, reopen to verify it stuck. Then a fresh "Run now" or the next scheduled fire confirms. Until fixed, Step 0 keeps skipping and (as of the 2026-08-28 full-automation change) sending proceeds anyway.

**FULL AUTOMATION, 2026-08-28 (Davide's explicit call).** After reviewing the daily 5-7 draft sample every day from 2026-08-24 to 2026-08-28 and finding the copy consistently good, Davide removed the human approval gate entirely. The Routine's prompt was rewritten via the RemoteTrigger API into a four-step end-to-end pipeline — **Step 0 (reply detection) → Part A (sourcing + drafting) → Part B (sending, no approval) → Step R (report)** — that runs unattended on every fire. Key decisions locked the same day: (1) Part B sends automatically, exactly 50/day, due follow-ups first then first-touch in lead-number order; (2) hard pre-send gates skip any bad draft (word count, em dash, offer/link, empty subject/body, dupe recipient, Dead/Unsubscribed lead) rather than send it; (3) if Step 0 fails (the egress block above), send the full 50 anyway — opt-outs since the last good Step 0 just don't get reprocessed that day; (4) Step R posts a two-table report (today's batch + all-time totals) in the session plus one push notification, nothing written to a sheet tab or emailed. Schedule (`0 5 * * *`, 07:00 CEST daily) unchanged. The 2026-08-28 07:00 fire had already run under the old review-gated prompt before this change; the first fully-automated fire is 2026-08-29 07:00 CEST.

**2026-09-02 — 75/day, A/B variants, owner-first, two follow-ups, IG routine added.** All via the RemoteTrigger API, no repo change at the time (this file was synced 2026-09-03). Performance to date had been ~505 sends over Aug 23–Sep 2, ~301 gyms, 6 genuine replies + 1 opt-out (~2.0% engaged, target 3%), 5 of 6 replies from CrossFit/community boxes. Changes: (1) **volume 50 → 75/day**; (2) **first-touch A/B split** — A permission, B light free-to-try — logged in the new `Send Log` `Variant` column, reported per-variant in Step R; (3) **the "what would you pay / which features" ask pulled out of the cold email**, held for Davide's manual reply; (4) **owner-first sending** (lever 1) plus a pass-to-owner line for generic inboxes; (5) **segment reweight** ~40% CrossFit/boutique / ~60% general; (6) **two follow-ups** (FU1 + FU2) with the new `Follow-up 2 Due Date` / `Follow-up 2 Sent (Y/N)` columns and a one-time FU2 backfill for legacy leads; (7) sourcing cap 60 → 90/run; (8) email length ~120 → ~60-90 words. Also created the **"casdey gym IG outreach"** Routine (see "Routine status"). Step 0's Zoho egress started working again on this run and has been clean since.

**2026-09-03 — two patch fixes + this file synced.** From a local session (the RemoteTrigger update path was permitted here, unlike the cloud runs): (1) **Part A now re-drafts** any stale "Ready for review" first-touch draft still carrying the old "what would you pay / which features" wording — 193 such drafts were sitting in the sheet, written before the 2026-09-02 rule and not caught by Part A (their `Draft Status` wasn't blank) or Part B (no gate for that phrase); left unfixed they would have started going out non-compliant once the follow-up backlog cleared (~4-5 days out). (2) **Part B gained a pre-send gate** for that same phrasing, as a backstop. (3) **Step R's `PushNotification` instruction now spells out both required fields** (`message` + `status: "proactive"`) — the 2026-09-03 email and IG runs had both hit an `InputValidationError` on the first call for omitting `status`, then self-recovered on a retry; the notification always did ultimately send, and the run itself was unaffected. The IG routine got fix (3) too. No change to volume, schedule, automation posture, copy rules, or the cross-channel exclusion. This SKILL.md was then updated on the `gym-outreach-automation` branch to match.

**2026-09-06 — reserved-floor split (25 first-touch / 50 follow-up ceiling).** Context: the 2026-09-03 run's report flagged that from 09-03 to 09-06 the follow-up backlog (accumulated FU1s + the one-time FU2 backfill of ~150 legacy leads) consumed the entire 75/day cap every day, so **zero first-touch went out for 4 days** and the A/B variants had no samples. 193 first-touch drafts sat ready and not shrinking. Davide's call: guarantee first-touch a floor instead of "follow-ups first, always". Applied to the Routine prompt via the RemoteTrigger API (VOLUME, PART B, the FU2-backfill note, and STEP R all updated): follow-ups take **≤50/75**, first-touch fills the rest (**≥25** with drafts available), excess follow-ups roll forward oldest-first, and it auto-balances toward 75 when either queue is short. The 50/25 was chosen against the arithmetic that each first-touch spawns ~2 follow-ups over ~10 days (so ~26/day first-touch is the steady state at a 75 cap) and **verified by simulation from the real current backlog**: daily total never exceeds 75, follow-up carryover stays bounded (~30–90, draining over ~2 months), first-touch holds at ~25/day (~13 A / ~13 B). The same sim showed a 50/50 split (≈37 each) makes the follow-up queue grow without bound and never recover. STEP R now reports the first-touch/follow-up split and the rolled-over follow-up carryover every run so the balance can be watched. Schedule, automation posture, copy rules and the cross-channel exclusion unchanged. Also noted: with two follow-ups per lead, ~25/day is the sustainable first-touch ceiling at a 75/day cap — pushing new-outreach volume higher means going back to one follow-up per lead, not moving the split.

**2026-09-11 — duplicate-follow-up bug, found by the 2026-09-12 run's own eligibility audit, fixed 2026-09-13.** The 09-11 run sent **150 duplicate follow-ups** (111 as FU2, 39 as FU1) to leads whose 2-touch sequence was already fully complete. Root cause: eligibility was decided by reading `Follow-up Due Date` / `Follow-up Sent (Y/N)` / `Follow-up 2 Due Date` / `Follow-up 2 Sent (Y/N)`, columns that live on individual `Send Log` rows (one row per send), so the same per-lead state could exist correctly on one row while a stale/blank copy sat on another; the one-time FU2 backfill clause then re-armed `Follow-up 2 Due Date` for a lead it read as blank, without checking whether that lead's FU2 had already gone out on a different row. Davide raised this the next day, via a run report he'd copied in from the routine's own self-flagged `[DATA ISSUE]` notes on the 150 affected rows, and asked for it fixed immediately given the reputational/deliverability risk of re-touching leads who'd already gotten their full sequence. **Fixed at the root** (see "Follow-up policy" above for the full rule): eligibility is now decided purely by counting actual FU1/FU2 `Send Log` rows per lead, the four date/flag columns are demoted to display-only, and the one-time backfill clause is retired outright since counted-row eligibility makes it unnecessary. This is the same workaround the 2026-09-12 run improvised for itself (it counted rows manually that one day and correctly excluded 290 already-sequenced leads) turned into the permanent rule instead of a one-off. Updated the same day, both here and in the live Routine prompt via the RemoteTrigger API; no repo data cleanup was needed for the 150 stale rows because the new logic never reads the fields that went stale.

**2026-09-13 — Sunset Gym (#740), the sender-address matching gap, and reply-drafting-with-approval.** Davide asked, in the same session as the follow-up bug above, whether genuine replies get handled automatically, and mentioned a confused French reply he'd seen personally that nobody had answered. That reply was traced to **lead #740, Sunset Gym (Genève)**: sent 12 Sept via first-touch variant A, replied within about an hour (`fredorudolf@gmail.com`, subject `Re: quick question about your lapsed members`, confused that casdey "reintegrates clients into the schedule" despite them having cancelled). It hadn't been auto-classified for two independent reasons: that day's scheduled run hadn't fired yet when the reply arrived, and even the next run would have missed it, because the reply came from a **personal address, not the business inbox casdey emailed** (`sunset.gym.geneve@gmail.com`), and Step 0 only ever matched by exact sender address. Logged manually the same day (`Leads!R741`/`U741` → "Replied", a note on its `Send Log` row) since the fix below landed after the fact.

Two changes, both Davide's explicit call, applied to the live Routine prompt via the RemoteTrigger API and synced here (see "Reply detection" above for the full rewrite): (1) **matching broadened** with a subject-line fallback (the two fixed S1/S2 subjects, stripped of "Re:", cross-referenced against `Send Log`) so an off-address reply like this one gets caught going forward, reporting an ambiguous case rather than guessing; (2) **reply-drafting added**, a narrow carve-out of "never act on inbox content": every genuine reply now gets a suggested response drafted (never sent) and surfaced in Step R's "REPLIES AWAITING YOUR APPROVAL" section, with the `PushNotification` leading with it when present. Davide's own framing: he wants to be flagged wherever the notification takes him, read the draft there, and tell the session whether to send it, edit it, or skip it, the same pattern the product's WhatsApp reply loop already uses elsewhere, just human-gated instead of autonomous. Sending happens only on an explicit approval message in that same Routine session (see "REPLY APPROVAL" in the live prompt); Step R posting the report is not itself approval. This is a genuine, deliberate reversal of "read-only," recorded here the same way the 2026-08-28 full-automation call was, not a quiet scope-creep.
