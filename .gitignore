# Secrets. .env holds the older Zoho/Google CLI keys, .env.local the full part 1
# service set (Supabase, Stripe, Resend, PostHog, Anthropic...). Never commit.
.env
.env.*
!.env.example

# Google service-account keys are private keys, never commit one.
*-service-account*.json
casdey-gws-cli-*.json

node_modules/
.next/
.vercel
.DS_Store

# Alex Hormozi book PDFs and their OCR text: copyrighted, local reference only.
# The distilled notes beside them in references/ ARE committed.
.claude/skills/hormozi/references/books/
